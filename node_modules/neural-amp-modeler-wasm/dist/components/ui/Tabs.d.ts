import React from 'react';
import { ReactNode } from 'react';
interface TabProps {
    tabs: (string | ReactNode)[];
    activeTab: number;
    setActiveTab: (index: number) => void;
    trackColor?: string;
    trackSize?: number;
    tightLines?: boolean;
}
declare const Tabs: ({ tabs, activeTab, setActiveTab, trackColor, trackSize, tightLines, }: TabProps) => React.JSX.Element;
export { Tabs };
