import React, { ReactNode } from 'react';
interface Option {
    value: string | number;
    label: string;
}
interface SelectProps {
    options: Option[];
    value?: string | number;
    onChange?: (selected: string | number) => void | Promise<void>;
    label?: string;
    heading?: string;
    disabled?: boolean;
    backgroundColor?: string;
    infoModal?: ReactNode;
}
export declare const Select: ({ options, label, heading, value, onChange, disabled, backgroundColor, infoModal, }: SelectProps) => React.JSX.Element;
export {};
