import React, { ReactNode } from 'react';
import { PREVIEW_MODE } from '../../types';
interface PlayerSkeletonProps {
    previewMode: PREVIEW_MODE;
    infoSlot?: ReactNode;
}
export declare const PlayerSkeleton: React.FC<PlayerSkeletonProps>;
export {};
