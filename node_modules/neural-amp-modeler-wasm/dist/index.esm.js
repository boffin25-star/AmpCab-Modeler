import React, { useState, useCallback, useEffect, createContext, useRef, useMemo, useContext, useId, forwardRef, createElement, memo } from 'react';
import { NamEngine } from './engine/index.js';
export { NamNode, NamNodePool } from './engine/index.js';

var PREVIEW_MODE;
(function (PREVIEW_MODE) {
    PREVIEW_MODE["MODEL"] = "model";
    PREVIEW_MODE["IR"] = "ir";
})(PREVIEW_MODE || (PREVIEW_MODE = {}));
// Default initial value for AudioNodes ref (all null)
const EMPTY_AUDIO_NODES = {
    audioContext: null,
    audioElement: null,
    audioWorkletNode: null,
    inputGainNode: null,
    outputGainNode: null,
    bypassNode: null,
    irNode: null,
    irWetGain: null,
    irDryGain: null,
    irGain: null,
    sourceNode: null,
    liveSourceNode: null,
    liveInputGainNode: null,
    mediaStream: null,
    inputMeterNode: null,
    outputMeterNode: null,
    channelSplitterNode: null,
    channelMergerNode: null,
    channel0LiveMeter: null,
    channel1LiveMeter: null,
    outputWorkaroundDestination: null,
    outputWorkaroundElement: null,
};

const LogoSm = ({ width = 94, height = 32 }) => (React.createElement("svg", { width: width, height: height, viewBox: '0 0 94 32', fill: 'none', xmlns: 'http://www.w3.org/2000/svg' },
    React.createElement("g", null,
        React.createElement("path", { d: 'M33.607 6.68409C33.6555 6.6615 33.7005 6.64215 33.7456 6.61956C36.255 5.53192 39.3255 4.98005 42.8764 4.98005C46.6081 4.98005 49.8174 5.59648 52.4104 6.81318C54.3391 7.71687 55.5454 8.79803 56.0195 10.0406V5.42543H67.0759V0.661817H56.0195V5.27697C55.5454 4.03442 54.3391 2.95326 52.4104 2.04959C49.814 0.832867 46.6049 0.216438 42.8764 0.216438C39.1482 0.216438 36.255 0.768321 33.7456 1.85595C33.6974 1.87531 33.6521 1.8979 33.607 1.92049V0.658591H0.105835V5.4222H33.607V6.68409Z', fill: '#FF0000' }),
        React.createElement("path", { d: 'M79.1289 5.42543H84.7376L93.865 0.661812H79.1289L67.0758 7.15852V11.9221L79.1289 5.42543Z', fill: '#FF0000' }),
        React.createElement("path", { d: 'M56.0197 23.4084C55.3842 23.9441 54.5939 24.4412 53.6457 24.8962C50.7237 26.2936 47.1468 27.0005 43.0215 27.0005C39.1447 27.0005 35.813 26.368 33.1231 25.1222C32.1814 24.6863 31.3846 24.1959 30.7363 23.6569L28.4689 23.8215L28.7078 25.1544C29.0559 27.0972 30.543 28.6916 33.1231 29.8858C35.813 31.1314 39.1447 31.7641 43.0215 31.7641C47.1468 31.7641 50.7237 31.0572 53.6457 29.6599C54.5939 29.208 55.3842 28.711 56.0197 28.1719V31.4446H67.0758V26.681H56.0197V23.4084Z', fill: '#0000FF' }),
        React.createElement("path", { d: 'M22.3571 26.6841H11.3008V31.4478H22.3571V26.6841Z', fill: '#0000FF' }),
        React.createElement("path", { d: 'M11.3008 10.4956H0.105835V15.2592H11.3008V10.4956Z', fill: '#0000FF' }),
        React.createElement("path", { d: 'M44.547 10.1019C44.0698 9.86952 43.6053 9.77592 42.9473 9.77592C42.2443 9.77592 41.7508 9.8792 41.2444 10.1277C40.7866 10.3536 40.1833 10.8377 39.9028 11.8673L39.6414 12.8322L41.6507 12.9097C43.1925 12.8968 43.9278 12.6547 44.3503 12.4708C44.7728 12.2868 45.3342 11.9608 45.3342 11.2282C45.3342 10.4956 44.744 10.1955 44.547 10.1019Z', fill: '#0000FF' }),
        React.createElement("path", { d: 'M54.5811 14.2523C54.3327 14.4492 54.0616 14.6331 53.7682 14.8139C53.8295 14.8397 53.8908 14.8655 53.9521 14.8913C54.7456 15.2367 55.4358 15.6498 56.0194 16.1242V12.4579C55.7582 13.1034 55.281 13.7037 54.5811 14.2555V14.2523Z', fill: '#0000FF' }),
        React.createElement("path", { d: 'M39.4543 19.7388C40.3412 22.2045 42.2346 22.2045 42.9473 22.2045C43.6085 22.2045 44.4988 22.1368 45.5243 21.6139C46.4244 21.1524 46.8629 20.5392 46.8629 19.7324C46.8629 18.9965 46.4854 18.4479 45.7082 18.0509C45.0211 17.7024 44.3212 17.5507 43.3667 17.5507C43.1087 17.5507 42.5023 17.5733 41.3733 17.7346L36.5645 18.4221L37.8127 13.2905L28.7141 12.4127L29.1657 11.0669C29.2335 10.87 29.3108 10.6796 29.3946 10.4892H22.3571V15.2528H28.6431L28.7078 15.6207C28.9302 16.8536 29.6076 17.9476 30.7363 18.8868L38.9383 18.293L39.4577 19.7324L39.4543 19.7388Z', fill: '#0000FF' }),
        React.createElement("path", { d: 'M68.8787 18.464L67.0758 19.4032V24.1668L68.8787 23.2276L79.5674 31.4478H93.2167L86.7147 26.6841H79.5674L68.8787 18.464Z', fill: '#0000FF' }),
        React.createElement("path", { d: 'M84.7377 5.42543H79.1289L67.0759 11.9221V5.42543H56.0195V10.0406C55.5454 8.79804 54.3391 7.71687 52.4104 6.81321C49.814 5.59649 46.6049 4.98005 42.8764 4.98005C39.1482 4.98005 36.255 5.53193 33.7456 6.61956C33.6974 6.63893 33.6521 6.66153 33.607 6.6841V5.42221H0.105835V10.4924H11.3008V26.6778H22.3572V10.4924H29.3947C29.3109 10.6796 29.2302 10.8732 29.1658 11.0701L28.7142 12.4159L37.8128 13.2938L36.5646 18.4253L41.3734 17.7379C42.5024 17.5765 43.1054 17.5539 43.3668 17.5539C44.3213 17.5539 45.0212 17.7024 45.7083 18.0542C46.4855 18.4511 46.863 18.9998 46.863 19.7356C46.863 20.5425 46.4242 21.1589 45.5244 21.6172C44.4989 22.1432 43.6086 22.2078 42.9474 22.2078C42.2347 22.2078 40.3413 22.2078 39.4544 19.7421L38.9352 18.3027L30.7332 18.8965L28.4658 19.0611L28.7045 20.394C28.9271 21.6269 29.6045 22.7209 30.7332 23.6601C31.3815 24.1991 32.1781 24.6897 33.1201 25.1253C35.81 26.3712 39.1416 27.0036 43.0184 27.0036C47.1435 27.0036 50.7203 26.297 53.6426 24.8994C54.5908 24.4444 55.3809 23.9506 56.0163 23.4116V26.6841H67.0727V19.4064L68.8723 18.4672L79.5643 26.6873H93.2137L76.5388 14.4685L93.862 5.42866H84.7343L84.7377 5.42543ZM44.3504 12.4708C43.9279 12.6548 43.1926 12.8968 41.6508 12.9097L39.6414 12.8323L39.9028 11.8673C40.1834 10.8377 40.7864 10.3536 41.2445 10.1277C41.7509 9.87598 42.2444 9.77593 42.9474 9.77593C43.6054 9.77593 44.0667 9.86953 44.5471 10.1019C44.7407 10.1955 45.3342 10.4827 45.3342 11.2283C45.3342 11.9738 44.7729 12.2868 44.3504 12.4708ZM56.0195 16.1242C55.4359 15.6498 54.7456 15.2367 53.9522 14.8913C53.8909 14.8655 53.8296 14.8397 53.7683 14.8139C54.0617 14.6364 54.3328 14.4492 54.5811 14.2523C55.281 13.7004 55.7615 13.1001 56.0195 12.4547V16.121V16.1242Z', fill: '#FFFF00' }))));

const inputs = [
    {
        name: 'Brit - Guitar',
        url: '/inputs/Brit - Guitar.wav',
        default: true,
    },
    {
        name: 'Celestial - Guitar',
        url: '/inputs/Celestial - Guitar.wav',
    },
    {
        name: 'Cream - Guitar',
        url: '/inputs/Cream - Guitar.wav',
    },
    {
        name: 'Decapitated - Guitar',
        url: '/inputs/Decapitated - Guitar.wav',
    },
    {
        name: 'Downtown - Bass',
        url: '/inputs/Downtown - Bass.wav',
    },
    {
        name: "Drivin' - Bass",
        url: "/inputs/Drivin' - Bass.wav",
    },
    {
        name: 'Fast Thrash - Guitar',
        url: '/inputs/Fast Thrash - Guitar.wav',
    },
    {
        name: 'Fear - Guitar',
        url: '/inputs/Fear - Guitar.wav',
    },
    {
        name: 'Frogger - Bass',
        url: '/inputs/Frogger - Bass.wav',
    },
    {
        name: 'Garden - Bass',
        url: '/inputs/Garden - Bass.wav',
    },
    {
        name: 'Groove Thrash - Guitar',
        url: '/inputs/Groove Thrash - Guitar.wav',
    },
    {
        name: 'Hammer Lead - Guitar',
        url: '/inputs/Hammer Lead - Guitar.wav',
    },
    {
        name: 'Harmonics - Guitar',
        url: '/inputs/Harmonics - Guitar.wav',
    },
    {
        name: 'Honky - Guitar',
        url: '/inputs/Honky - Guitar.wav',
    },
    {
        name: 'Hotrod - Guitar',
        url: '/inputs/Hotrod - Guitar.wav',
    },
    {
        name: 'Jazz Hop - Guitar',
        url: '/inputs/Jazz Hop - Guitar.wav',
    },
    {
        name: 'Jazz Trot - Guitar',
        url: '/inputs/Jazz Trot - Guitar.wav',
    },
    {
        name: 'John - Guitar',
        url: '/inputs/John - Guitar.wav',
    },
    {
        name: 'Lunar - Guitar', // Lunar is contributed by Aditya Mhatre
        url: '/inputs/Lunar - Guitar.wav',
    },
    {
        name: 'Mayer - Guitar',
        url: '/inputs/Mayer - Guitar.wav',
    },
    {
        name: 'Metalcore - Guitar', // Metalcore is contributed by Aditya Mhatre
        url: '/inputs/Metalcore - Guitar.wav',
    },
    {
        name: 'Pluck - Guitar',
        url: '/inputs/Pluck - Guitar.wav',
    },
    {
        name: 'Pop Punk - Guitar',
        url: '/inputs/Pop Punk - Guitar.wav',
    },
    {
        name: 'Power - Guitar',
        url: '/inputs/Power - Guitar.wav',
    },
    {
        name: 'Power Thrash - Guitar',
        url: '/inputs/Power Thrash - Guitar.wav',
    },
    {
        name: 'Progression -  Guitar',
        url: '/inputs/Progression -  Guitar.wav',
    },
    {
        name: 'Raid - Guitar',
        url: '/inputs/Raid - Guitar.wav',
    },
    {
        name: "Rollin' - Bass",
        url: "/inputs/Rollin' - Bass.wav",
    },
    {
        name: 'Rotary - Guitar',
        url: '/inputs/Rotary - Guitar.wav',
    },
    {
        name: 'Slide Lead - Guitar',
        url: '/inputs/Slide Lead - Guitar.wav',
    },
    {
        name: "Smokin' - Bass",
        url: "/inputs/Smokin' - Bass.wav",
    },
    {
        name: 'Smooth - Guitar',
        url: '/inputs/Smooth - Guitar.wav',
    },
    {
        name: 'Stroke - Guitar',
        url: '/inputs/Stroke - Guitar.wav',
    },
    {
        name: 'Tomb - Guitar',
        url: '/inputs/Tomb - Guitar.wav',
    },
];
const irs = [
    {
        name: 'None',
        url: '',
        mix: 0,
        gain: 1,
    },
    {
        name: 'Mesa 412 OS',
        url: '/irs/mesa.wav',
        mix: 1,
        gain: 3,
        default: true,
    },
    {
        name: 'Celestion G12 Vintage',
        url: '/irs/celestion.wav',
        mix: 1,
        gain: 1.75,
    },
    {
        name: 'Eminence Governor',
        url: '/irs/eminence.wav',
        mix: 1,
        gain: 3,
    },
    {
        name: 'Ampeg 8x10',
        url: '/irs/ampeg.wav',
        mix: 1,
        gain: 1.25,
    },
    {
        name: 'EMT 140 Plate Reverb',
        url: '/irs/plate.wav',
        mix: 0.3,
        gain: 1,
    },
    {
        name: 'Fender Spring Reverb',
        url: '/irs/spring.wav',
        mix: 0.35,
        gain: 1,
    },
];
const models = [
    {
        name: 'Fender Deluxe Reverb',
        url: '/models/deluxe.nam',
        default: true,
    },
    {
        name: 'Marshall JCM',
        url: '/models/jcm.nam',
    },
    {
        name: 'Vox AC10',
        url: '/models/ac10.nam',
    },
    {
        name: 'Dumble Overdrive Special',
        url: '/models/dumble.nam',
    },
    {
        name: 'Ampeg V-2',
        url: '/models/ampeg.nam',
    },
    {
        name: 'Boosted EVH 5153',
        url: '/models/5153.nam',
    },
    {
        name: 'Roland JC-120',
        url: '/models/jc.nam',
    },
];
const githubBaseUrl = 'https://raw.githubusercontent.com/tone-3000/neural-amp-modeler-wasm/refs/heads/main/ui/public';
const DEFAULT_MODELS = (models.map(model => ({
        ...model,
        url: `${githubBaseUrl}${model.url}`,
    })));
const DEFAULT_IRS = (irs.map(ir => ({
        ...ir,
        url: ir.url ? `${githubBaseUrl}${ir.url}` : '', // Empty string if no url
    })));
const DEFAULT_INPUTS = (inputs.map(input => ({
        ...input,
        url: `${githubBaseUrl}${input.url}`,
    })));
const DEFAULT_AUDIO_SRC = githubBaseUrl + '/inputs/placeholder.wav';
const SOURCE_MODE_OPTIONS = [
    { value: 'demo', label: 'Demo' },
    { value: 'live', label: 'Live' },
];

const Tabs = ({ tabs, activeTab, setActiveTab, trackColor, trackSize = 2, tightLines = false, }) => {
    return (React.createElement("div", { className: `flex width-full ${tightLines ? 'gap-8' : 'gap-0'}` }, tabs.map((tab, index) => (React.createElement("button", { key: index, className: `group flex flex-1 justify-center ${tightLines ? 'px-0' : 'px-4'} py-3 font-semibold transition-colors duration-150 ${index === activeTab
            ? `active text-white ${trackSize === 1 ? 'border-b' : 'border-b-2'} border-white`
            : `text-zinc-400 hover:text-white ${trackSize === 1 ? 'border-b' : 'border-b-2'} ${trackColor ? `border-${trackColor}` : 'border-zinc-700'}`}`, onClick: () => setActiveTab(index) }, tab)))));
};

/**
 * Audio metering utilities for real-time level calculation
 */
/** Threshold for clip detection (slightly below 1.0 to catch near-clips) */
const CLIP_THRESHOLD = 0.99;
/**
 * Calculate peak and RMS levels from an AnalyserNode
 *
 * @param analyser - The AnalyserNode to read from
 * @param buffer - A reusable Float32Array buffer (should be analyser.fftSize length)
 * @returns Peak and RMS levels
 */
function calculateLevels(analyser, buffer) {
    analyser.getFloatTimeDomainData(buffer);
    let peak = 0;
    let sumSquares = 0;
    for (let i = 0; i < buffer.length; i++) {
        const absSample = Math.abs(buffer[i]);
        if (absSample > peak)
            peak = absSample;
        sumSquares += buffer[i] * buffer[i];
    }
    return {
        peak,
        rms: Math.sqrt(sumSquares / buffer.length),
    };
}
/**
 * Convert linear amplitude to decibels
 *
 * @param linear - Linear amplitude (0.0 to 1.0+)
 * @returns Decibel value (-Infinity for silence)
 */
function linearToDb(linear) {
    if (linear <= 0)
        return -Infinity;
    return 20 * Math.log10(linear);
}
/**
 * Convert decibels to linear amplitude
 *
 * @param db - Decibel value
 * @returns Linear amplitude
 */
function dbToLinear(db) {
    return Math.pow(10, db / 20);
}
/**
 * Check if the given levels indicate clipping
 *
 * @param levels - Meter levels to check
 * @returns True if peak exceeds clip threshold
 */
function isClipping(levels) {
    return levels.peak >= CLIP_THRESHOLD;
}
/**
 * Clamp a value between min and max
 */
function clamp$1(value, min, max) {
    return Math.min(Math.max(value, min), max);
}
/**
 * Map a linear level (0-1) to a display value with pseudo-logarithmic scaling
 * This makes the meter more visually responsive across the dynamic range
 *
 * @param level - Linear level (0.0 to 1.0)
 * @returns Scaled display value (0.0 to 1.0)
 */
function scaleForDisplay(level) {
    // Simple power curve - makes quiet signals more visible
    // Adjust exponent for different response curves (0.5 = square root, more responsive to quiet)
    return Math.pow(clamp$1(level, 0, 1), 0.5);
}
/** dB floor for log-scaled meters (-40 dB ≈ 0.01 linear) */
const LOG_METER_DB_MIN = -40;
/**
 * Map a linear level (0-1) to a pseudo-log display value (0-1) for block meters.
 * Uses dB scaling so blocks fill left-to-right in a log-like manner:
 * quiet levels fill fewer blocks, loud levels fill more, clipping = full bar.
 *
 * @param level - Linear level (0.0 to 1.0+)
 * @returns Display value 0.0 to 1.0
 */
function levelToLogDisplay(level) {
    if (level <= 0)
        return 0;
    const db = linearToDb(level);
    const normalized = (db - LOG_METER_DB_MIN) / (0 - LOG_METER_DB_MIN);
    return clamp$1(normalized, 0, 1);
}

const ua = typeof navigator !== 'undefined' ? navigator.userAgent.toLowerCase() : '';
const isFirefox = ua.includes('firefox');
const isSafari = ua.includes('safari') &&
    !ua.includes('chrome') &&
    !ua.includes('firefox') &&
    !ua.includes('edg');
const isEdge = ua.includes('edg');
const isChrome = (ua.includes('chrome') || ua.includes('crios')) && !ua.includes('edg');
const needsMediaStreamWorkaround = isFirefox || isSafari;
function getBrowserName() {
    if (isEdge)
        return 'edge';
    if (isFirefox)
        return 'firefox';
    if (isSafari)
        return 'safari';
    if (isChrome)
        return 'chrome';
    return 'other';
}

/** Clean up all live input nodes (mediaStream, source, gain, splitter, merger, meters) */
function cleanupLiveInputNodes(nodes) {
    if (nodes.mediaStream) {
        nodes.mediaStream.getTracks().forEach(track => track.stop());
        nodes.mediaStream = null;
    }
    if (nodes.liveSourceNode) {
        nodes.liveSourceNode.disconnect();
        nodes.liveSourceNode = null;
    }
    if (nodes.liveInputGainNode) {
        nodes.liveInputGainNode.disconnect();
        nodes.liveInputGainNode = null;
    }
    if (nodes.channelSplitterNode) {
        nodes.channelSplitterNode.disconnect();
        nodes.channelSplitterNode = null;
    }
    if (nodes.channelMergerNode) {
        nodes.channelMergerNode.disconnect();
        nodes.channelMergerNode = null;
    }
    if (nodes.channel0LiveMeter) {
        nodes.channel0LiveMeter.disconnect();
        nodes.channel0LiveMeter = null;
    }
    if (nodes.channel1LiveMeter) {
        nodes.channel1LiveMeter.disconnect();
        nodes.channel1LiveMeter = null;
    }
}
/**
 * Tear down live input audio nodes and restore the demo signal path.
 * Used by stopLiveInput and handleLiveInputUnavailable.
 */
function teardownLiveInput(nodes, options) {
    cleanupLiveInputNodes(nodes);
    // Reconnect file source to restore demo path
    if (nodes.sourceNode && nodes.inputGainNode) {
        try {
            nodes.sourceNode.disconnect(nodes.inputGainNode);
        }
        catch (e) {
            if (!(e instanceof DOMException && e.name === 'InvalidAccessError'))
                throw e;
        }
        nodes.sourceNode.connect(nodes.inputGainNode);
    }
    if (options.muteOutput && nodes.outputGainNode && nodes.audioContext) {
        nodes.outputGainNode.gain.setTargetAtTime(0, nodes.audioContext.currentTime, 0.01);
    }
}
/** Apply output device routing with browser-specific handling */
async function applyOutputDeviceRouting(nodes, deviceId) {
    const { audioContext, outputMeterNode } = nodes;
    if (!audioContext || !outputMeterNode)
        return;
    if (needsMediaStreamWorkaround) {
        // Route through MediaStreamDestination + HTMLAudioElement
        if (nodes.outputWorkaroundElement) {
            nodes.outputWorkaroundElement.pause();
            nodes.outputWorkaroundElement.srcObject = null;
            nodes.outputWorkaroundElement = null;
        }
        if (nodes.outputWorkaroundDestination) {
            nodes.outputWorkaroundDestination.disconnect();
            nodes.outputWorkaroundDestination = null;
        }
        try {
            outputMeterNode.disconnect();
        }
        catch (e) {
            if (!(e instanceof DOMException && e.name === 'InvalidStateError'))
                throw e;
        }
        if (deviceId) {
            const mediaStreamDestination = audioContext.createMediaStreamDestination();
            nodes.outputWorkaroundDestination = mediaStreamDestination;
            outputMeterNode.connect(mediaStreamDestination);
            const outputElement = new Audio();
            outputElement.srcObject = mediaStreamDestination.stream;
            nodes.outputWorkaroundElement = outputElement;
            const elementWithSinkId = outputElement;
            if (typeof elementWithSinkId.setSinkId === 'function') {
                await elementWithSinkId.setSinkId(deviceId);
            }
            await outputElement.play();
        }
        else {
            outputMeterNode.connect(audioContext.destination);
        }
    }
    else {
        const contextWithSinkId = audioContext;
        if (typeof contextWithSinkId.setSinkId === 'function') {
            try {
                await contextWithSinkId.setSinkId(deviceId ?? '');
            }
            catch (e) {
                console.warn('[Audio] Failed to set AudioContext sink:', e);
            }
        }
    }
}

/**
 * Map raw MediaDeviceInfo to simplified { deviceId, label } objects.
 * Provides a fallback label when the browser doesn't expose one.
 */
function mapDevices(allDevices, kind) {
    const prefix = kind === 'audioinput' ? 'Input' : 'Output';
    return allDevices
        .filter(device => device.kind === kind)
        .map(device => ({
        deviceId: device.deviceId,
        label: device.label || `${prefix} ${device.deviceId.slice(0, 8)}`,
    }));
}

/**
 * Manages audio device enumeration, output device selection, microphone permissions,
 * and hot-plug detection. Owns all device and permission state.
 *
 * Audio graph side effects are delegated to the caller via narrow callbacks.
 */
function useAudioDevicesAndPermissions({ applyOutputRouting, teardownLiveInput, onLiveInputLost, liveInputConfig, }) {
    // State
    const [microphonePermission, setMicrophonePermission] = useState({
        status: 'idle',
        error: null,
    });
    const [audioInputDevices, setAudioInputDevices] = useState({
        devices: [],
        isLoading: false,
        error: null,
        preferredDeviceId: null,
    });
    const [audioOutputDevices, setAudioOutputDevices] = useState({
        devices: [],
        selectedDeviceId: null,
    });
    // Permission logic
    const queryBrowserPermission = useCallback(async () => {
        try {
            const result = await navigator.permissions.query({
                name: 'microphone',
            });
            if (result.state === 'granted')
                return 'granted';
            if (result.state === 'denied')
                return 'blocked';
            return 'idle';
        }
        catch {
            // Permissions API not supported for microphone (e.g., Firefox)
            return 'idle';
        }
    }, []);
    // Query permission state on mount and listen for changes
    useEffect(() => {
        if (typeof window === 'undefined')
            return;
        let mounted = true;
        const checkPermission = async () => {
            const status = await queryBrowserPermission();
            if (mounted) {
                setMicrophonePermission(prev => ({
                    ...prev,
                    status,
                    error: status === 'granted' ? null : prev.error,
                }));
            }
        };
        checkPermission();
        const setupPermissionListener = async () => {
            try {
                const result = await navigator.permissions.query({
                    name: 'microphone',
                });
                const handleChange = () => {
                    if (!mounted)
                        return;
                    if (result.state === 'granted') {
                        setMicrophonePermission(prev => ({
                            ...prev,
                            status: 'granted',
                            error: null,
                        }));
                    }
                    else if (result.state === 'denied') {
                        setMicrophonePermission(prev => ({
                            ...prev,
                            status: 'blocked',
                            error: 'Microphone access is blocked. Please enable it in your browser settings.',
                        }));
                    }
                    else {
                        setMicrophonePermission(prev => ({ ...prev, status: 'idle' }));
                    }
                };
                result.addEventListener('change', handleChange);
                return () => result.removeEventListener('change', handleChange);
            }
            catch {
                return undefined;
            }
        };
        let cleanupListener;
        setupPermissionListener().then(cleanup => {
            cleanupListener = cleanup;
        });
        return () => {
            mounted = false;
            cleanupListener?.();
        };
    }, [queryBrowserPermission]);
    // Request microphone permission.
    // Only handles the permission prompt; device enumeration is the caller's responsibility.
    // Returns the device ID the user selected in the browser's permission dialog, or null.
    const requestMicrophonePermission = useCallback(async () => {
        // Capture previous status via functional updater (avoids needing a ref)
        let previousStatus = 'idle';
        setMicrophonePermission(prev => {
            previousStatus = prev.status;
            return { status: 'pending', error: null };
        });
        try {
            const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
            const track = stream.getAudioTracks()[0];
            const selectedDeviceId = track?.getSettings()?.deviceId ?? null;
            stream.getTracks().forEach(t => t.stop());
            setMicrophonePermission({ status: 'granted', error: null });
            setAudioInputDevices(prev => ({
                ...prev,
                preferredDeviceId: selectedDeviceId,
            }));
            return selectedDeviceId;
        }
        catch (error) {
            if (error instanceof DOMException) {
                if (error.name === 'NotAllowedError' ||
                    error.name === 'PermissionDeniedError') {
                    const permissionState = await queryBrowserPermission();
                    // Firefox doesn't support Permissions API for microphone, so queryBrowserPermission
                    // returns 'idle'. If we already tried once (status is 'denied') and get denied again,
                    // Firefox won't re-prompt, treat as blocked.
                    const alreadyDenied = previousStatus === 'denied';
                    if (permissionState === 'blocked' ||
                        (permissionState === 'idle' && alreadyDenied)) {
                        setMicrophonePermission({
                            status: 'blocked',
                            error: 'Microphone access is blocked. Please enable it in your browser settings.',
                        });
                    }
                    else {
                        setMicrophonePermission({
                            status: 'denied',
                            error: 'Microphone access was denied. Please try again.',
                        });
                    }
                }
                else if (error.name === 'NotFoundError') {
                    setMicrophonePermission({
                        status: 'error',
                        error: 'No microphone or audio input device was found.',
                    });
                }
                else if (error.name === 'NotReadableError') {
                    setMicrophonePermission({
                        status: 'error',
                        error: 'Your microphone is being used by another application.',
                    });
                }
                else {
                    setMicrophonePermission({ status: 'error', error: error.message });
                }
            }
            else {
                setMicrophonePermission({
                    status: 'error',
                    error: 'An unexpected error occurred. Please try again.',
                });
            }
            throw error;
        }
    }, [queryBrowserPermission]);
    // Device enumeration
    /**
     * Enumerate audio input devices and update state. Self-healing: if the browser
     * returns devices without labels (Firefox/Safari require a getUserMedia call per
     * session to unlock labels), this function automatically calls getUserMedia and
     * re-enumerates while the stream is still active. This is safe because we only
     * call this function when permission is believed to be granted, so getUserMedia
     * will succeed silently without prompting.
     */
    const refreshAudioInputDevices = useCallback(async () => {
        const permApiStatus = await queryBrowserPermission();
        if (permApiStatus === 'granted' || permApiStatus === 'blocked') {
            setMicrophonePermission(prev => ({
                ...prev,
                status: permApiStatus,
                error: permApiStatus === 'granted' ? null : prev.error,
            }));
            if (permApiStatus === 'blocked') {
                return { inputDevices: [], preferredDeviceId: null };
            }
        }
        setAudioInputDevices(prev => ({ ...prev, isLoading: true, error: null }));
        try {
            let allDevices = await navigator.mediaDevices.enumerateDevices();
            const hasLabels = allDevices.some(d => d.kind === 'audioinput' && d.label);
            // Firefox/Safari workaround: getUserMedia unlocks device labels for the session.
            // Enumerate while the stream is still active to guarantee labels are available.
            if (!hasLabels) {
                try {
                    const stream = await navigator.mediaDevices.getUserMedia({
                        audio: true,
                    });
                    allDevices = await navigator.mediaDevices.enumerateDevices();
                    stream.getTracks().forEach(t => t.stop());
                    setMicrophonePermission({ status: 'granted', error: null });
                }
                catch {
                    // getUserMedia failed, use the unlabeled devices we already have
                }
            }
            const mappedInputDevices = mapDevices(allDevices, 'audioinput');
            setAudioInputDevices(prev => ({
                ...prev,
                devices: mappedInputDevices,
                isLoading: false,
                error: mappedInputDevices.length === 0
                    ? 'No audio input devices found.'
                    : null,
            }));
            return {
                inputDevices: mappedInputDevices,
                preferredDeviceId: audioInputDevices.preferredDeviceId,
            };
        }
        catch {
            setAudioInputDevices(prev => ({
                ...prev,
                isLoading: false,
                error: 'Failed to enumerate audio devices.',
            }));
            return { inputDevices: [], preferredDeviceId: null };
        }
    }, [queryBrowserPermission, audioInputDevices.preferredDeviceId]);
    const refreshAudioOutputDevices = useCallback(async () => {
        try {
            const allDevices = await navigator.mediaDevices.enumerateDevices();
            const mappedOutputDevices = mapDevices(allDevices, 'audiooutput');
            setAudioOutputDevices(prev => ({
                ...prev,
                devices: mappedOutputDevices,
            }));
        }
        catch (error) {
            console.error('Error enumerating output devices:', error);
        }
    }, []);
    // Output device selection
    const setOutputDevice = useCallback(async (deviceId) => {
        // Capture previous for rollback via functional updater
        let previousDeviceId = null;
        setAudioOutputDevices(prev => {
            previousDeviceId = prev.selectedDeviceId;
            return { ...prev, selectedDeviceId: deviceId };
        });
        try {
            await applyOutputRouting(deviceId);
        }
        catch (error) {
            console.warn('[Audio] Failed to set output device, reverting:', error);
            setAudioOutputDevices(prev => ({
                ...prev,
                selectedDeviceId: previousDeviceId,
            }));
        }
    }, [applyOutputRouting]);
    // Live input unavailable handler
    const handleLiveInputUnavailable = useCallback((reason) => {
        teardownLiveInput();
        onLiveInputLost();
        const errorMessages = {
            'device-disconnected': 'Audio input device was disconnected.',
            'permission-revoked': 'Microphone permission was revoked. Please re-enable access in your browser settings.',
        };
        setAudioInputDevices(prev => ({ ...prev, error: errorMessages[reason] }));
    }, [teardownLiveInput, onLiveInputLost]);
    // Hot-plug detection
    useEffect(() => {
        if (typeof window === 'undefined' || !navigator.mediaDevices)
            return;
        const handleDeviceChange = async () => {
            if (microphonePermission.status === 'granted') {
                try {
                    const allDevices = await navigator.mediaDevices.enumerateDevices();
                    const audioInputs = mapDevices(allDevices, 'audioinput');
                    // Check if configured input device was disconnected
                    const configuredDeviceId = liveInputConfig?.deviceId;
                    if (configuredDeviceId) {
                        const configuredDeviceStillExists = audioInputs.some(d => d.deviceId === configuredDeviceId);
                        if (!configuredDeviceStillExists) {
                            console.warn('Configured audio input device was disconnected');
                            handleLiveInputUnavailable('device-disconnected');
                            setAudioInputDevices(prev => ({ ...prev, devices: audioInputs }));
                            return;
                        }
                    }
                    setAudioInputDevices(prev => ({
                        ...prev,
                        devices: audioInputs,
                        error: audioInputs.length === 0
                            ? 'All audio devices have been disconnected.'
                            : null,
                    }));
                    // Check if selected output device was disconnected
                    const audioOutputs = mapDevices(allDevices, 'audiooutput');
                    const selectedStillExists = audioOutputDevices.selectedDeviceId === null ||
                        audioOutputs.some(d => d.deviceId === audioOutputDevices.selectedDeviceId);
                    setAudioOutputDevices(prev => ({
                        ...prev,
                        devices: audioOutputs,
                        selectedDeviceId: selectedStillExists
                            ? prev.selectedDeviceId
                            : null,
                    }));
                    if (!selectedStillExists) {
                        console.warn('Selected audio output device was disconnected, falling back to system default');
                        applyOutputRouting(null).catch(e => console.warn('[Audio] Failed to reset output routing:', e));
                    }
                }
                catch {
                    setAudioInputDevices(prev => ({
                        ...prev,
                        error: 'Failed to refresh device list.',
                    }));
                }
            }
        };
        navigator.mediaDevices.addEventListener('devicechange', handleDeviceChange);
        return () => {
            navigator.mediaDevices.removeEventListener('devicechange', handleDeviceChange);
        };
    }, [
        microphonePermission.status,
        liveInputConfig,
        handleLiveInputUnavailable,
        applyOutputRouting,
        audioOutputDevices.selectedDeviceId,
    ]);
    return {
        microphonePermission,
        audioInputDevices,
        audioOutputDevices,
        requestMicrophonePermission,
        refreshAudioInputDevices,
        refreshAudioOutputDevices,
        setOutputDevice,
        handleLiveInputUnavailable,
    };
}

// Context
const T3kPlayerContext = createContext(null);
// Provider Component
function T3kPlayerContextProvider({ children, engineAssets, }) {
    // Consolidated state
    const [audioState, setAudioState] = useState({
        initState: 'uninitialized',
        isPlaying: false,
        activePlayerId: null,
        isBypassed: false,
        modelUrl: null,
        irUrl: null,
        audioUrl: null,
        inputMode: { type: 'demo' },
        liveInputConfig: null,
    });
    // Global source mode (demo vs live), shared by all players
    const [sourceMode, setSourceMode] = useState('demo');
    // Settings dialog state (global, single instance)
    const [settingsDialog, setSettingsDialog] = useState({
        isOpen: false,
        sourceMode: 'demo',
        onMonitoringChange: () => { },
    });
    // Refs for non-render-triggering data
    const audioNodesRef = useRef({ ...EMPTY_AUDIO_NODES });
    const isInitializingRef = useRef(false);
    const isInitializedRef = useRef(false);
    // The NAM worklet node (also stored in audioNodesRef.audioWorkletNode for
    // graph wiring; this ref keeps the typed NamNode API for model loading).
    const namNodeRef = useRef(null);
    const engineAssetsRef = useRef(engineAssets);
    engineAssetsRef.current = engineAssets;
    // Getter for audio nodes
    const getAudioNodes = useCallback(() => {
        return audioNodesRef.current;
    }, []);
    // Check if audio system is ready for operations
    // Use this for conditional logic or UI state
    const isAudioReady = useCallback(() => {
        if (!isInitializedRef.current)
            return false;
        const nodes = audioNodesRef.current;
        return !!(nodes.audioContext && nodes.audioWorkletNode);
    }, []);
    // Audio device management, permissions, and hot-plug detection
    const { microphonePermission, audioInputDevices, audioOutputDevices, requestMicrophonePermission, refreshAudioInputDevices, refreshAudioOutputDevices, setOutputDevice, handleLiveInputUnavailable, } = useAudioDevicesAndPermissions({
        applyOutputRouting: useCallback(async (deviceId) => {
            const nodes = audioNodesRef.current;
            if (!nodes.audioContext || !nodes.outputMeterNode)
                return;
            await applyOutputDeviceRouting(nodes, deviceId);
        }, []),
        teardownLiveInput: useCallback(() => {
            teardownLiveInput(audioNodesRef.current, { muteOutput: false });
        }, []),
        onLiveInputLost: useCallback(() => {
            setAudioState(prev => ({
                ...prev,
                inputMode: { type: 'demo' },
                liveInputConfig: null,
                isPlaying: false,
                activePlayerId: null,
            }));
        }, []),
        liveInputConfig: audioState.liveInputConfig,
    });
    /**
     * Initialize the audio system fully.
     *
     * This handles the complete initialization sequence:
     * 1. Loads the audio element for demo playback
     * 2. Creates the AudioContext and attaches the NamEngine
     * 3. Creates the NamNode and wires up the audio graph
     * 4. Loads a model into the node
     *
     * After this function resolves, all audio nodes are guaranteed to exist.
     *
     * @param options.audioUrl - URL for demo audio (uses default if not provided)
     * @param options.modelUrl - URL for NAM model (uses default if not provided)
     * @param options.slimSize - Raw NAM-core slimmable size for A2 models
     */
    const init = useCallback(async (options) => {
        if (isInitializedRef.current || isInitializingRef.current)
            return;
        isInitializingRef.current = true;
        setAudioState(prev => ({ ...prev, initState: 'initializing' }));
        try {
            // Create and setup audio element
            const audio = new Audio();
            audio.crossOrigin = 'anonymous';
            audio.src = options?.audioUrl || DEFAULT_AUDIO_SRC;
            getAudioNodes().audioElement = audio;
            document.body.appendChild(audio);
            await new Promise((resolve, reject) => {
                const handleLoad = () => resolve();
                const handleError = () => reject(new Error('Failed to load default audio'));
                audio.addEventListener('loadeddata', handleLoad, { once: true });
                audio.addEventListener('error', handleError, { once: true });
                audio.load();
            });
            // Safety net: reset playback state when audio file ends naturally.
            // Player.tsx also listens for 'ended', but this catches edge cases where
            // the player's listener might not fire (e.g., component unmounted).
            audio.addEventListener('ended', () => {
                const nodes = getAudioNodes();
                if (nodes.outputGainNode && nodes.audioContext) {
                    nodes.outputGainNode.gain.setTargetAtTime(0, nodes.audioContext.currentTime, 0.01);
                }
                setAudioState(prev => ({
                    ...prev,
                    isPlaying: false,
                    activePlayerId: null,
                }));
            });
            // Create the AudioContext and attach the NAM engine (registers the
            // worklet module + fetches the wasm binary; no SharedArrayBuffer).
            const context = new AudioContext({ latencyHint: 'interactive' });
            const engine = await NamEngine.attach(context, engineAssetsRef.current);
            const namNode = await engine.createNode();
            namNodeRef.current = namNode;
            const nodes = getAudioNodes();
            nodes.audioContext = context;
            nodes.audioWorkletNode = namNode;
            // Create gain nodes
            nodes.inputGainNode = new GainNode(context, { gain: 1 });
            nodes.outputGainNode = new GainNode(context, { gain: 1 });
            nodes.bypassNode = new GainNode(context, { gain: 0 });
            // Create metering nodes
            const meterConfig = { fftSize: 2048 };
            nodes.inputMeterNode = new AnalyserNode(context, meterConfig);
            nodes.outputMeterNode = new AnalyserNode(context, meterConfig);
            // Create source from audio element
            nodes.sourceNode = context.createMediaElementSource(nodes.audioElement);
            // Connect audio graph
            nodes.sourceNode.connect(nodes.inputGainNode);
            nodes.inputGainNode.connect(nodes.inputMeterNode);
            nodes.inputMeterNode.connect(nodes.bypassNode);
            nodes.bypassNode.connect(nodes.outputGainNode);
            nodes.inputMeterNode.connect(namNode);
            namNode.connect(nodes.outputGainNode);
            nodes.outputGainNode.connect(nodes.outputMeterNode);
            nodes.outputMeterNode.connect(context.destination);
            context.resume();
            // Load a model into the node (placeholder or the requested one)
            const modelUrl = options?.modelUrl ||
                DEFAULT_MODELS.find(m => m.default)?.url ||
                DEFAULT_MODELS[0].url;
            await loadModelInternal(modelUrl, options?.slimSize);
            // Now fully initialized
            isInitializedRef.current = true;
            setAudioState(prev => ({
                ...prev,
                initState: 'ready',
            }));
        }
        catch (error) {
            setAudioState(prev => ({ ...prev, initState: 'uninitialized' }));
            console.error('Error initializing audio system:', error);
            throw error;
        }
        finally {
            isInitializingRef.current = false;
        }
    }, [getAudioNodes]);
    // Internal model loading function used during init (bypasses isInitializingRef check)
    const loadModelInternal = async (modelUrl, slimSize) => {
        const response = await fetch(modelUrl);
        if (!response.ok) {
            throw new Error(`Failed to fetch model: ${response.statusText}`);
        }
        const jsonStr = await response.text();
        const namNode = namNodeRef.current;
        if (!namNode) {
            throw new Error('NAM node not available');
        }
        // The model is parsed on the audio thread, so rendering through the node
        // pauses for the duration (typically 100-300 ms). I ramp the output down
        // before the load so the cut is click-free; the worklet fades back in
        // after the swap.
        const { audioContext, outputGainNode } = getAudioNodes();
        const previousGain = outputGainNode?.gain.value ?? 1;
        const muted = audioContext !== null &&
            outputGainNode !== null &&
            audioContext.state === 'running' &&
            previousGain > 0.001;
        if (muted) {
            outputGainNode.gain.setTargetAtTime(0, audioContext.currentTime, 0.005);
            await new Promise(r => setTimeout(r, 30));
        }
        try {
            // Non-slimmable models silently ignore slimSize.
            await namNode.loadModel(jsonStr, { slimSize });
        }
        finally {
            if (muted) {
                outputGainNode.gain.setTargetAtTime(previousGain, audioContext.currentTime, 0.01);
            }
        }
        setAudioState(prev => ({ ...prev, modelUrl }));
    };
    // Load model (public API with guards)
    // Uses refs for guards to avoid stale closure issues when called
    // immediately after init() within the same event handler
    const loadModel = useCallback(async (modelUrl, slimSize) => {
        if (isInitializingRef.current) {
            throw new Error('Audio system is initializing');
        }
        if (!isInitializedRef.current) {
            throw new Error('Audio system not initialized. Call init() first.');
        }
        await loadModelInternal(modelUrl, slimSize);
    }, []);
    // Load audio
    const loadAudio = useCallback(async (src) => {
        const audioElement = getAudioNodes().audioElement;
        if (!audioElement) {
            throw new Error('Audio element not initialized');
        }
        try {
            audioElement.src = src;
            await new Promise((resolve, reject) => {
                const handleLoad = () => {
                    audioElement.removeEventListener('loadeddata', handleLoad);
                    audioElement.removeEventListener('error', handleError);
                    resolve();
                };
                const handleError = () => {
                    audioElement.removeEventListener('loadeddata', handleLoad);
                    audioElement.removeEventListener('error', handleError);
                    reject(new Error('Failed to load audio'));
                };
                audioElement.addEventListener('loadeddata', handleLoad, {
                    once: true,
                });
                audioElement.addEventListener('error', handleError, { once: true });
                audioElement.load();
            });
            setAudioState(prev => ({ ...prev, audioUrl: src }));
        }
        catch (error) {
            console.error('Error loading audio:', error);
            throw error;
        }
    }, [getAudioNodes]);
    // Load IR with configuration
    const loadIr = useCallback(async ({ url, mix = 1, gain = 1 }) => {
        const nodes = getAudioNodes();
        const { audioContext, audioWorkletNode, outputGainNode } = nodes;
        if (!audioContext || !audioWorkletNode || !outputGainNode) {
            throw new Error('Audio system not initialized. Call init() first.');
        }
        try {
            // Create or update gain nodes
            if (!nodes.irWetGain) {
                nodes.irWetGain = new GainNode(audioContext, { gain: mix });
            }
            else {
                nodes.irWetGain.gain.setValueAtTime(mix, audioContext.currentTime);
            }
            if (!nodes.irDryGain) {
                nodes.irDryGain = new GainNode(audioContext, { gain: 1 - mix });
            }
            else {
                nodes.irDryGain.gain.setValueAtTime(1 - mix, audioContext.currentTime);
            }
            if (!nodes.irGain) {
                nodes.irGain = new GainNode(audioContext, { gain });
            }
            else {
                nodes.irGain.gain.setValueAtTime(gain, audioContext.currentTime);
            }
            // Fetch and decode IR
            const response = await fetch(url);
            if (!response.ok) {
                throw new Error(`Failed to fetch IR: ${response.statusText}`);
            }
            const arrayBuffer = await response.arrayBuffer();
            const audioBuffer = await audioContext.decodeAudioData(arrayBuffer);
            // Create new convolver
            const newIrNode = new ConvolverNode(audioContext);
            newIrNode.buffer = audioBuffer;
            // Disconnect existing connections
            audioWorkletNode.disconnect();
            nodes.irNode?.disconnect();
            nodes.irWetGain?.disconnect();
            nodes.irDryGain?.disconnect();
            nodes.irGain?.disconnect();
            // Setup parallel wet/dry signal paths
            // Wet path: worklet -> IR -> gain -> wet gain -> output
            audioWorkletNode.connect(newIrNode);
            newIrNode.connect(nodes.irGain);
            nodes.irGain.connect(nodes.irWetGain);
            nodes.irWetGain.connect(outputGainNode);
            // Dry path: worklet -> dry gain -> output
            audioWorkletNode.connect(nodes.irDryGain);
            nodes.irDryGain.connect(outputGainNode);
            nodes.irNode = newIrNode;
            setAudioState(prev => ({ ...prev, irUrl: url }));
        }
        catch (error) {
            console.error('Error loading IR:', error);
            throw error;
        }
    }, [getAudioNodes]);
    // Remove IR
    const removeIr = useCallback(() => {
        const nodes = getAudioNodes();
        const { audioWorkletNode, outputGainNode } = nodes;
        if (!audioWorkletNode || !outputGainNode)
            return;
        // Disconnect all IR nodes
        audioWorkletNode.disconnect();
        nodes.irNode?.disconnect();
        nodes.irWetGain?.disconnect();
        nodes.irDryGain?.disconnect();
        nodes.irGain?.disconnect();
        // Reset IR nodes
        nodes.irNode = null;
        nodes.irWetGain = null;
        nodes.irDryGain = null;
        nodes.irGain = null;
        // Reconnect direct path
        audioWorkletNode.connect(outputGainNode);
        setAudioState(prev => ({ ...prev, irUrl: null }));
    }, [getAudioNodes]);
    // Set bypass to a specific state (idempotent, reads the audio node to determine if action is needed)
    const setBypass = useCallback((bypassed) => {
        if (!isAudioReady()) {
            console.warn('Cannot set bypass: audio not initialized');
            return;
        }
        const nodes = getAudioNodes();
        const { audioWorkletNode, audioContext, bypassNode, outputGainNode, irNode, irWetGain, irDryGain, irGain, } = nodes;
        if (!audioWorkletNode || !audioContext || !bypassNode) {
            console.warn('Cannot set bypass: required nodes not available');
            return;
        }
        const currentlyBypassed = bypassNode.gain.value > 0.5;
        if (currentlyBypassed === bypassed)
            return; // already in desired state
        try {
            if (bypassed) {
                // Enable bypass
                if (irNode && irWetGain && irDryGain && irGain && outputGainNode) {
                    // Disconnect IR paths
                    audioWorkletNode.disconnect(irNode);
                    audioWorkletNode.disconnect(irDryGain);
                    irNode.disconnect(irGain);
                    irGain.disconnect(irWetGain);
                    irWetGain.disconnect(outputGainNode);
                    irDryGain.disconnect(outputGainNode);
                }
                else if (outputGainNode) {
                    // Disconnect direct path
                    audioWorkletNode.disconnect(outputGainNode);
                }
                bypassNode.gain.setTargetAtTime(1, audioContext.currentTime, 0.002);
            }
            else {
                // Disable bypass
                if (irNode && irWetGain && irDryGain && irGain && outputGainNode) {
                    // Reconnect IR paths
                    audioWorkletNode.connect(irNode);
                    irNode.connect(irGain);
                    irGain.connect(irWetGain);
                    irWetGain.connect(outputGainNode);
                    audioWorkletNode.connect(irDryGain);
                    irDryGain.connect(outputGainNode);
                }
                else if (outputGainNode) {
                    // Reconnect direct path
                    audioWorkletNode.connect(outputGainNode);
                }
                bypassNode.gain.setTargetAtTime(0, audioContext.currentTime, 0.002);
            }
            setAudioState(prev => ({ ...prev, isBypassed: bypassed }));
        }
        catch (error) {
            console.error('Error setting bypass:', error);
        }
    }, [isAudioReady, getAudioNodes]);
    // Sync engine settings (model, IR, bypass) to match a player's preferences
    const syncEngineSettings = useCallback(async (config) => {
        const currentModelUrl = audioState.modelUrl;
        const currentIrUrl = audioState.irUrl;
        if (currentModelUrl !== config.modelUrl) {
            await loadModel(config.modelUrl, config.slimSize);
        }
        if (config.ir.url) {
            if (currentIrUrl !== config.ir.url) {
                await loadIr({
                    url: config.ir.url,
                    mix: config.ir.mix,
                    gain: config.ir.gain,
                });
            }
        }
        else if (currentIrUrl) {
            removeIr();
        }
        setBypass(config.bypassed);
    }, [
        audioState.modelUrl,
        audioState.irUrl,
        loadModel,
        loadIr,
        removeIr,
        setBypass,
    ]);
    // Connect visualizer
    const connectVisualizerNode = useCallback((analyserNode) => {
        const { outputGainNode } = getAudioNodes();
        if (!outputGainNode) {
            return () => { };
        }
        outputGainNode.connect(analyserNode);
        return () => {
            try {
                outputGainNode.disconnect(analyserNode);
            }
            catch {
                // Node already disconnected
            }
        };
    }, [getAudioNodes]);
    // Start play audio input from microphone/audio interface
    // Optional initialChannel and initialChannelGains allow restoring a previous configuration
    // without needing to call selectLiveInputChannel separately (which would have stale closure issues)
    const startLiveInput = useCallback(async (deviceId, options) => {
        const requestedChannel = options?.initialChannel ?? 'first';
        const initialChannelGains = options?.initialChannelGains ?? {
            first: 0,
            second: 0,
        };
        if (!isAudioReady()) {
            throw new Error('Audio system not initialized. Call init() first.');
        }
        const nodes = getAudioNodes();
        const { audioContext, inputMeterNode, sourceNode, audioElement } = nodes;
        if (!audioContext || !inputMeterNode) {
            throw new Error('Required audio nodes not available.');
        }
        // Take over audio engine: stop any current playback and reset ownership
        if (audioElement) {
            audioElement.pause();
        }
        if (nodes.outputGainNode && audioContext) {
            nodes.outputGainNode.gain.setTargetAtTime(0, audioContext.currentTime, 0.01);
        }
        setAudioState(prev => ({
            ...prev,
            inputMode: { type: 'connecting' },
            isPlaying: false,
            activePlayerId: null,
        }));
        try {
            // Clean up any existing live input nodes before setting up new ones
            cleanupLiveInputNodes(nodes);
            // Request microphone/audio interface access with settings optimized for instruments
            const baseAudioConstraints = {
                channelCount: { ideal: 2 }, // Request stereo if available
                echoCancellation: false,
                noiseSuppression: false,
                autoGainControl: false,
            };
            let stream;
            try {
                stream = await navigator.mediaDevices.getUserMedia({
                    audio: {
                        ...baseAudioConstraints,
                        deviceId: deviceId ? { exact: deviceId } : undefined,
                    },
                });
            }
            catch (error) {
                if (deviceId &&
                    error instanceof DOMException &&
                    error.name === 'OverconstrainedError') {
                    console.warn(`Device ${deviceId} not available, falling back to default`);
                    stream = await navigator.mediaDevices.getUserMedia({
                        audio: baseAudioConstraints,
                    });
                }
                else {
                    throw error;
                }
            }
            nodes.mediaStream = stream;
            // Get actual channel count and device ID from the track
            const track = stream.getAudioTracks()[0];
            const settings = track.getSettings();
            const channelCount = settings.channelCount ?? 1;
            const actualDeviceId = settings.deviceId ?? deviceId ?? '';
            // Clamp channel selection to what the device actually supports
            const initialChannel = channelCount > 1 ? requestedChannel : 'first';
            const initialChannelIndex = initialChannel === 'first' ? 0 : 1;
            // Listen for track ending (device disconnected, permission revoked, etc.)
            // This is the most reliable signal: Chrome kills the track immediately on permission reset,
            // but may not fire the Permissions API 'change' event.
            track.addEventListener('ended', () => {
                console.warn('Audio track ended unexpectedly');
                handleLiveInputUnavailable('device-disconnected');
            });
            // Create source node from the live stream
            const liveSource = audioContext.createMediaStreamSource(stream);
            nodes.liveSourceNode = liveSource;
            // Disconnect file source from inputGainNode
            sourceNode?.disconnect();
            // Create dedicated gain node for live input (applied before channel splitting)
            nodes.liveInputGainNode = new GainNode(audioContext, { gain: 1 });
            // Create channel splitter for multi-channel input
            nodes.channelSplitterNode = audioContext.createChannelSplitter(2);
            nodes.channelMergerNode = audioContext.createChannelMerger(1);
            // Create per-channel meters (post-gain, since gain is applied before splitting)
            const meterConfig = { fftSize: 2048 };
            nodes.channel0LiveMeter = new AnalyserNode(audioContext, meterConfig);
            nodes.channel1LiveMeter = new AnalyserNode(audioContext, meterConfig);
            // Connect: liveSource → liveInputGainNode → channelSplitter
            // This applies gain BEFORE channel splitting, so meters show post-gain levels
            liveSource.connect(nodes.liveInputGainNode);
            nodes.liveInputGainNode.connect(nodes.channelSplitterNode);
            // Connect each channel to its meter (these are now post-gain)
            nodes.channelSplitterNode.connect(nodes.channel0LiveMeter, 0);
            if (channelCount > 1) {
                nodes.channelSplitterNode.connect(nodes.channel1LiveMeter, 1);
            }
            // Route selected channel to processing chain
            // Connect: channelSplitter → channelMerger → inputMeterNode (bypassing inputGainNode)
            nodes.channelSplitterNode.connect(nodes.channelMergerNode, initialChannelIndex, 0);
            nodes.channelMergerNode.connect(inputMeterNode);
            // Apply initial gain for the selected channel
            const initialGainDb = initialChannelGains[initialChannel] ?? 0;
            const initialLinearGain = dbToLinear(initialGainDb);
            nodes.liveInputGainNode.gain.setValueAtTime(initialLinearGain, audioContext.currentTime);
            setAudioState(prev => ({
                ...prev,
                inputMode: { type: 'live' },
                liveInputConfig: {
                    deviceId: actualDeviceId,
                    channelCount: channelCount,
                    selectedChannel: initialChannel,
                    channelGains: initialChannelGains,
                },
            }));
            // Resume audio context if suspended
            if (audioContext.state === 'suspended') {
                await audioContext.resume();
            }
            // Re-apply output device routing after changing input sources
            const selectedOutputId = audioOutputDevices.selectedDeviceId;
            await applyOutputDeviceRouting(nodes, selectedOutputId);
        }
        catch (error) {
            // Clean up any partially-created live input nodes
            cleanupLiveInputNodes(nodes);
            setAudioState(prev => ({
                ...prev,
                inputMode: { type: 'demo' },
            }));
            console.error('Error starting live input:', error);
            throw error;
        }
    }, [
        isAudioReady,
        getAudioNodes,
        audioOutputDevices.selectedDeviceId,
        handleLiveInputUnavailable,
    ]);
    // Reconnect live input using the saved liveInputConfig.
    // No-op if no config exists or live input is already active.
    const reconnectLiveInput = useCallback(async () => {
        if (audioState.inputMode.type === 'live')
            return;
        if (!audioState.liveInputConfig)
            return;
        await startLiveInput(audioState.liveInputConfig.deviceId, {
            initialChannel: audioState.liveInputConfig.selectedChannel,
            initialChannelGains: audioState.liveInputConfig.channelGains,
        });
    }, [audioState.inputMode.type, audioState.liveInputConfig, startLiveInput]);
    // Stop live input and prepare for demo mode takeover
    // Note: This preserves liveInputConfig so players can still see/use the configured device.
    const stopLiveInput = useCallback(() => {
        teardownLiveInput(getAudioNodes(), { muteOutput: true });
        // Reset ownership - no player owns the audio engine until they press play
        setAudioState(prev => ({
            ...prev,
            inputMode: { type: 'demo' },
            isPlaying: false,
            activePlayerId: null,
        }));
    }, [getAudioNodes]);
    // Select which channel to route from live input to processing
    const selectLiveInputChannel = useCallback((channel) => {
        const nodes = getAudioNodes();
        const { channelSplitterNode, channelMergerNode, liveInputGainNode, audioContext, } = nodes;
        if (!channelSplitterNode || !channelMergerNode) {
            console.warn('Cannot select channel: live input not active');
            return;
        }
        const newChannelIndex = channel === 'first' ? 0 : 1;
        // Disconnect all splitter → merger connections, then connect only the new channel.
        // This avoids relying on React state to track the current connection, which can be
        // stale due to closure capture. Since connect() is additive, a missed disconnect
        // would result in both channels being heard simultaneously.
        channelSplitterNode.disconnect(channelMergerNode);
        channelSplitterNode.connect(channelMergerNode, newChannelIndex, 0);
        // Apply gain for the new channel (side effect outside state updater)
        if (liveInputGainNode && audioContext && audioState.liveInputConfig) {
            const gainDb = audioState.liveInputConfig.channelGains?.[channel] ?? 0;
            liveInputGainNode.gain.setTargetAtTime(dbToLinear(gainDb), audioContext.currentTime, 0.02);
        }
        // Pure state update
        setAudioState(prev => {
            if (!prev.liveInputConfig)
                return prev;
            return {
                ...prev,
                liveInputConfig: {
                    ...prev.liveInputConfig,
                    selectedChannel: channel,
                },
            };
        });
    }, [getAudioNodes, audioState.liveInputConfig]);
    // Set live input gain for the currently selected channel (persists to liveInputConfig)
    const setLiveInputGain = useCallback((gainDb) => {
        const nodes = getAudioNodes();
        const { liveInputGainNode, audioContext } = nodes;
        // Apply gain to the live input gain node (before channel splitting)
        if (liveInputGainNode && audioContext) {
            const linearGain = dbToLinear(gainDb);
            liveInputGainNode.gain.setTargetAtTime(linearGain, audioContext.currentTime, 0.02 // 20ms smoothing
            );
        }
        // Persist to liveInputConfig for the currently selected channel
        setAudioState(prev => {
            if (!prev.liveInputConfig)
                return prev;
            const selectedChannel = prev.liveInputConfig.selectedChannel ?? 'first';
            return {
                ...prev,
                liveInputConfig: {
                    ...prev.liveInputConfig,
                    channelGains: {
                        ...prev.liveInputConfig.channelGains,
                        [selectedChannel]: gainDb,
                    },
                },
            };
        });
    }, [getAudioNodes]);
    // Open the global settings dialog
    const openSettingsDialog = useCallback((config) => {
        setSettingsDialog({
            isOpen: true,
            sourceMode,
            playerId: config.playerId,
            selectedModel: config.selectedModel,
            selectedIr: config.selectedIr,
            onMonitoringChange: config.onMonitoringChange,
        });
    }, [sourceMode]);
    // Set playing state (controls audioElement in demo mode, outputGainNode in live mode)
    // playerId identifies which player is taking control (required when shouldPlay=true)
    const setPlaying = useCallback((shouldPlay, playerId) => {
        // Guard: require initialization when starting playback
        // (stopping is always allowed to handle edge cases)
        if (shouldPlay && !isAudioReady()) {
            console.warn('Cannot start playback: audio not initialized');
            return;
        }
        const nodes = getAudioNodes();
        const { audioElement, outputGainNode, audioContext, mediaStream } = nodes;
        // Check actual node state, not React state (which can be stale due to async updates)
        const isActuallyLiveMode = mediaStream !== null && mediaStream.active;
        // Update audio nodes based on actual input mode
        if (isActuallyLiveMode) {
            // Live mode: control output gain only (no audio element involved)
            if (outputGainNode && audioContext) {
                // Resume context if suspended
                if (shouldPlay && audioContext.state === 'suspended') {
                    audioContext.resume().catch(error => {
                        console.error('Failed to resume audio context:', error);
                        setAudioState(prev => ({
                            ...prev,
                            isPlaying: false,
                            activePlayerId: null,
                        }));
                    });
                }
                outputGainNode.gain.setTargetAtTime(shouldPlay ? 1 : 0, audioContext.currentTime, 0.01 // 10ms for quick response
                );
            }
        }
        else {
            // Demo mode: control audio element playback
            if (audioElement) {
                if (shouldPlay) {
                    audioContext?.resume().catch(error => {
                        console.error('Failed to resume audio context:', error);
                    });
                    audioElement.play().catch(error => {
                        console.error('Playback failed:', error);
                        setAudioState(prev => ({
                            ...prev,
                            isPlaying: false,
                            activePlayerId: null,
                        }));
                    });
                }
                else {
                    audioElement.pause();
                }
            }
            // Also set output gain for demo mode
            if (outputGainNode && audioContext) {
                outputGainNode.gain.setTargetAtTime(shouldPlay ? 1 : 0, audioContext.currentTime, 0.01);
            }
        }
        setAudioState(prev => ({
            ...prev,
            isPlaying: shouldPlay,
            activePlayerId: shouldPlay ? (playerId ?? prev.activePlayerId) : null,
        }));
    }, [isAudioReady, getAudioNodes, audioState.inputMode.type]);
    // Cleanup without destroying audio
    // This should keep the audio context alive and the audio nodes in place,
    // but reset the states and stop playback.
    const cleanup = useCallback(() => {
        setPlaying(false); // stop playback for demo or live mode
        // also reset the audio element current time to the start of the audio
        const { audioElement } = getAudioNodes();
        if (audioElement) {
            audioElement.currentTime = 0;
        }
        // remove the ir from the engine
        removeIr();
        setAudioState(prev => ({
            ...prev,
            // reset the other states
            modelUrl: null,
            audioUrl: null,
            isBypassed: false,
        }));
    }, [getAudioNodes, removeIr, setPlaying]);
    // Close the global settings dialog (keeps whatever the user has set)
    const closeSettingsDialog = useCallback(() => {
        setSettingsDialog(prev => ({ ...prev, isOpen: false }));
    }, []);
    // Full teardown on provider unmount (handles HMR/routing teardown)
    useEffect(() => {
        const nodesRef = audioNodesRef;
        const initRef = isInitializedRef;
        const nodeRef = namNodeRef;
        return () => {
            const nodes = nodesRef.current;
            void nodeRef.current?.dispose();
            nodeRef.current = null;
            if (nodes.audioContext && nodes.audioContext.state !== 'closed') {
                nodes.audioContext.close();
            }
            nodes.audioElement?.remove();
            initRef.current = false;
        };
    }, []);
    // Memoize context value
    const contextValue = useMemo(() => ({
        audioState,
        sourceMode,
        microphonePermission,
        audioInputDevices,
        audioOutputDevices,
        getAudioNodes,
        isAudioReady,
        init,
        loadModel,
        loadAudio,
        loadIr,
        removeIr,
        setBypass,
        syncEngineSettings,
        cleanup,
        connectVisualizerNode,
        startLiveInput,
        reconnectLiveInput,
        stopLiveInput,
        selectLiveInputChannel,
        setLiveInputGain,
        setOutputDevice,
        setPlaying,
        setSourceMode,
        requestMicrophonePermission,
        refreshAudioInputDevices,
        refreshAudioOutputDevices,
        settingsDialog,
        openSettingsDialog,
        closeSettingsDialog,
    }), [
        audioState,
        sourceMode,
        microphonePermission,
        audioInputDevices,
        audioOutputDevices,
        getAudioNodes,
        isAudioReady,
        init,
        loadModel,
        loadAudio,
        loadIr,
        removeIr,
        setBypass,
        syncEngineSettings,
        cleanup,
        connectVisualizerNode,
        startLiveInput,
        reconnectLiveInput,
        stopLiveInput,
        selectLiveInputChannel,
        setLiveInputGain,
        setOutputDevice,
        setPlaying,
        setSourceMode,
        requestMicrophonePermission,
        refreshAudioInputDevices,
        refreshAudioOutputDevices,
        settingsDialog,
        openSettingsDialog,
        closeSettingsDialog,
    ]);
    return (React.createElement(T3kPlayerContext.Provider, { value: contextValue }, children));
}
// SSR-safe defaults (no-op implementations for server-side rendering)
function createNoopContext() {
    return {
        audioState: {
            initState: 'uninitialized',
            isPlaying: false,
            activePlayerId: null,
            isBypassed: false,
            modelUrl: null,
            irUrl: null,
            audioUrl: null,
            inputMode: { type: 'demo' },
            liveInputConfig: null,
        },
        sourceMode: 'demo',
        microphonePermission: { status: 'idle', error: null },
        audioInputDevices: {
            devices: [],
            isLoading: false,
            error: null,
            preferredDeviceId: null,
        },
        audioOutputDevices: { devices: [], selectedDeviceId: null },
        getAudioNodes: () => ({ ...EMPTY_AUDIO_NODES }),
        isAudioReady: () => false,
        init: async () => { },
        loadModel: async () => { },
        loadAudio: async () => { },
        loadIr: async () => { },
        removeIr: () => { },
        setBypass: () => { },
        syncEngineSettings: async () => { },
        cleanup: () => { },
        connectVisualizerNode: () => () => { },
        startLiveInput: async () => { },
        reconnectLiveInput: async () => { },
        stopLiveInput: () => { },
        selectLiveInputChannel: () => { },
        setLiveInputGain: () => { },
        setOutputDevice: async () => { },
        setPlaying: (() => { }),
        setSourceMode: () => { },
        requestMicrophonePermission: async () => null,
        refreshAudioInputDevices: async () => ({
            inputDevices: [],
            preferredDeviceId: null,
        }),
        refreshAudioOutputDevices: async () => { },
        settingsDialog: {
            isOpen: false,
            sourceMode: 'demo',
            onMonitoringChange: () => { },
        },
        openSettingsDialog: () => { },
        closeSettingsDialog: () => { },
    };
}
// Custom hook; useContext is called unconditionally to satisfy Rules of Hooks
function useT3kPlayerContext() {
    const context = useContext(T3kPlayerContext);
    if (typeof window === 'undefined') {
        return createNoopContext();
    }
    if (!context) {
        throw new Error('useT3kPlayerContext must be used within a T3kPlayerContextProvider');
    }
    return context;
}

const FFT_SIZE = 256;
const rafIter$1 = () => {
    let id;
    return {
        async next() {
            const promise = new Promise(resolve => {
                id = requestAnimationFrame(resolve);
            });
            await promise;
            return { value: undefined, done: false };
        },
        async return() {
            cancelAnimationFrame(id);
            return { value: undefined, done: true };
        },
        [Symbol.asyncIterator]() {
            return this;
        },
    };
};
const setupVisualizer = (canvas, audioCtx, bg = '#18181b', useGradient = true) => {
    const canvasCtx = canvas.getContext('2d');
    if (canvasCtx === null) {
        throw new Error('canvasCtx was null');
    }
    const analyzer = audioCtx.createAnalyser();
    analyzer.fftSize = FFT_SIZE;
    const analyzeResultArray = new Uint8Array(analyzer.fftSize);
    let running = true;
    (async () => {
        const { height, width } = canvas;
        const gap = width / analyzeResultArray.length;
        // Create linear gradient
        const gradient = canvasCtx.createLinearGradient(0, 0, width, 0);
        if (useGradient) {
            gradient.addColorStop(0, 'red');
            gradient.addColorStop(0.5, 'yellow');
            gradient.addColorStop(1, 'blue');
        }
        else {
            gradient.addColorStop(0, 'white');
            gradient.addColorStop(1, 'white');
        }
        for await (const _ of rafIter$1()) {
            if (!running)
                break;
            analyzer.getByteTimeDomainData(analyzeResultArray);
            canvasCtx.fillStyle = bg;
            canvasCtx.fillRect(0, 0, width, height);
            canvasCtx.beginPath();
            canvasCtx.lineWidth = 2;
            for (let i = 0; i < analyzeResultArray.length; i++) {
                const data = analyzeResultArray[i];
                const x = gap * i;
                const y = height * (data / 256);
                if (i === 0) {
                    canvasCtx.moveTo(x, y);
                }
                else {
                    canvasCtx.lineTo(x, y);
                }
            }
            canvasCtx.strokeStyle = gradient;
            canvasCtx.lineTo(width, height / 2);
            canvasCtx.stroke();
        }
    })();
    return {
        analyser: analyzer,
        stop: () => {
            running = false;
        },
    };
};
const initVisualizer = ({ canvas, bg = '#18181b', useGradient = true, }) => {
    if (!canvas)
        return;
    // draw a line across the canvas as default state using the gradient
    const canvasCtx = canvas.getContext('2d');
    if (canvasCtx === null) {
        throw new Error('canvasCtx was null');
    }
    const { height, width } = canvas;
    const gradient = canvasCtx.createLinearGradient(0, 0, width, 0);
    if (useGradient) {
        gradient.addColorStop(0, 'red');
        gradient.addColorStop(0.5, 'yellow');
        gradient.addColorStop(1, 'blue');
    }
    else {
        gradient.addColorStop(0, 'white');
        gradient.addColorStop(1, 'white');
    }
    canvasCtx.fillStyle = bg;
    canvasCtx.lineWidth = 2;
    canvasCtx.fillRect(0, 0, width, height);
    canvasCtx.beginPath();
    canvasCtx.strokeStyle = gradient;
    canvasCtx.moveTo(0, height / 2);
    canvasCtx.lineTo(width, height / 2);
    canvasCtx.stroke();
};

function formatTime(time) {
    const minutes = Math.floor(time / 60);
    const seconds = Math.floor(time % 60);
    return `${minutes}:${seconds.toString().padStart(2, '0')}`;
}
function getDefault(items) {
    return items.find(item => item.default) || items[0];
}

function useSourceMode(options = {}) {
    const { playerId, onSourceModeChange } = options;
    const { audioState, audioInputDevices, sourceMode, setSourceMode, startLiveInput, reconnectLiveInput, setPlaying, } = useT3kPlayerContext();
    // Live device options for Select component (shared by multiple consumers)
    const liveDeviceOptions = useMemo(() => audioInputDevices.devices.map(device => ({
        label: device.label,
        value: device.deviceId,
    })), [audioInputDevices.devices]);
    // Handle source mode change (Demo <-> Live)
    const handleSourceModeChange = useCallback(async (newMode) => {
        if (newMode === sourceMode)
            return;
        if (newMode === 'live') {
            // Switching from Demo to Live
            if (audioState.isPlaying) {
                setPlaying(false);
            }
            // If play input is already active (another player started it), just switch mode
            // If not active but we have a config, reconnect using the saved config
            await reconnectLiveInput();
        }
        else {
            // Switching from Live to Demo
            // Stop this player's monitoring if it was active
            if (audioState.activePlayerId === playerId) {
                setPlaying(false);
            }
        }
        setSourceMode(newMode);
        onSourceModeChange?.(newMode);
    }, [
        sourceMode,
        audioState.isPlaying,
        audioState.activePlayerId,
        playerId,
        setPlaying,
        reconnectLiveInput,
        onSourceModeChange,
    ]);
    // Handle device change
    const handleLiveDeviceChange = useCallback(async (deviceId) => {
        if (deviceId !== audioState.liveInputConfig?.deviceId) {
            await startLiveInput(deviceId);
        }
    }, [audioState.liveInputConfig?.deviceId, startLiveInput]);
    return {
        sourceMode,
        liveDeviceOptions,
        handleSourceModeChange,
        handleLiveDeviceChange,
    };
}

// Hook
function usePlayerCore(options) {
    const { id: idProp, disabled = false, slimSize, models, irs, inputs, resolveData, onPlayDemo, onPlayLive, onModelChange, onInputChange, onIrChange, } = options;
    const generatedId = useId();
    const id = idProp ?? generatedId;
    // Context
    const { audioState, audioInputDevices, getAudioNodes, init, loadModel, loadAudio, loadIr, removeIr, setBypass, syncEngineSettings, connectVisualizerNode, cleanup, reconnectLiveInput, stopLiveInput, setPlaying, openSettingsDialog: openDialog, } = useT3kPlayerContext();
    // Source mode hook (per-player)
    const { sourceMode, liveDeviceOptions, handleSourceModeChange, handleLiveDeviceChange, } = useSourceMode({ playerId: id });
    // Derived from context
    const isLiveConfigured = audioState.liveInputConfig !== null;
    const currentDeviceId = audioState.liveInputConfig?.deviceId ?? null;
    const isThisPlayerActive = audioState.activePlayerId === id;
    // Selection state
    const [selectedModel, setSelectedModel] = useState(() => models ? getDefault(models) : null);
    const [selectedInput, setSelectedInput] = useState(() => inputs ? getDefault(inputs) : null);
    const [selectedIr, setSelectedIr] = useState(() => irs ? getDefault(irs) : null);
    // Initialize selections when data arrives (null → array transition for AcordianPlayer)
    useEffect(() => {
        if (models && !selectedModel)
            setSelectedModel(getDefault(models));
    }, [models, selectedModel]);
    useEffect(() => {
        if (inputs && !selectedInput)
            setSelectedInput(getDefault(inputs));
    }, [inputs, selectedInput]);
    useEffect(() => {
        if (irs && !selectedIr)
            setSelectedIr(getDefault(irs));
    }, [irs, selectedIr]);
    const [currentTime, setCurrentTime] = useState(0);
    const [duration, setDuration] = useState(0);
    const [isLoading, setIsLoading] = useState(false);
    const [bypassed, setBypassed] = useState(false);
    // Sync local bypass state from context when this player is active
    useEffect(() => {
        if (isThisPlayerActive) {
            setBypassed(audioState.isBypassed);
        }
    }, [isThisPlayerActive, audioState.isBypassed]);
    // Refs
    const visualizerRef = useRef(null);
    const visualizerNodeRef = useRef(null);
    const canvasWrapperRef = useRef(null);
    // Memoized options
    const modelOptions = useMemo(() => (models ?? []).map(m => ({ label: m.name, value: m.url })), [models]);
    const audioOptions = useMemo(() => (inputs ?? []).map(i => ({ label: i.name, value: i.url })), [inputs]);
    const irOptions = useMemo(() => (irs ?? []).map(ir => ({ label: ir.name, value: ir.url })), [irs]);
    // Helpers
    const ensureSelections = useCallback(async () => {
        if (selectedModel && selectedIr && selectedInput) {
            return { model: selectedModel, ir: selectedIr, input: selectedInput };
        }
        if (resolveData) {
            const data = await resolveData();
            setSelectedModel(data.model);
            setSelectedIr(data.ir);
            setSelectedInput(data.input);
            return data;
        }
        throw new Error('No data available and no resolveData provided');
    }, [selectedModel, selectedIr, selectedInput, resolveData]);
    const syncPlayerToEngine = useCallback(async (model, ir, bypassState) => {
        await syncEngineSettings({
            modelUrl: model.url,
            ir: { url: ir.url, mix: ir.mix, gain: ir.gain },
            bypassed: bypassState,
            slimSize,
        });
    }, [syncEngineSettings, slimSize]);
    // Effects
    // Cleanup on unmount
    useEffect(() => {
        return () => {
            cleanup();
        };
    }, [cleanup]);
    // Resize handler
    const handleResize = useCallback(() => {
        if (canvasWrapperRef.current && visualizerRef.current) {
            visualizerRef.current.width = canvasWrapperRef.current.clientWidth;
            initVisualizer({ canvas: visualizerRef.current });
        }
    }, []);
    useEffect(() => {
        handleResize();
        window.addEventListener('resize', handleResize);
        return () => window.removeEventListener('resize', handleResize);
    }, [handleResize, sourceMode]);
    // Audio element event listeners, only when THIS player is active
    useEffect(() => {
        if (!isThisPlayerActive)
            return;
        const audioElement = getAudioNodes().audioElement;
        if (!audioElement)
            return;
        const handleTimeUpdate = () => setCurrentTime(audioElement.currentTime);
        const handleEnded = () => setPlaying(false);
        const handleLoadedMetadata = () => setDuration(audioElement.duration);
        if (audioElement.duration)
            setDuration(audioElement.duration);
        audioElement.addEventListener('timeupdate', handleTimeUpdate);
        audioElement.addEventListener('ended', handleEnded);
        audioElement.addEventListener('loadedmetadata', handleLoadedMetadata);
        return () => {
            audioElement.removeEventListener('timeupdate', handleTimeUpdate);
            audioElement.removeEventListener('ended', handleEnded);
            audioElement.removeEventListener('loadedmetadata', handleLoadedMetadata);
        };
    }, [getAudioNodes, isThisPlayerActive, setPlaying]);
    // Visualizer setup, only when THIS player is active and in demo mode
    useEffect(() => {
        if (!isThisPlayerActive)
            return;
        if (audioState.initState !== 'ready' || !visualizerRef.current)
            return;
        const audioContext = getAudioNodes().audioContext;
        if (!audioContext)
            return;
        const { analyser, stop } = setupVisualizer(visualizerRef.current, audioContext);
        visualizerNodeRef.current = analyser;
        const disconnect = connectVisualizerNode(analyser);
        return () => {
            stop();
            disconnect();
            visualizerNodeRef.current = null;
        };
    }, [
        audioState.initState,
        getAudioNodes,
        connectVisualizerNode,
        isThisPlayerActive,
        sourceMode,
    ]);
    // Event handlers
    const setLiveMonitoring = useCallback(async (enabled) => {
        if (sourceMode !== 'live')
            return;
        if (enabled) {
            await reconnectLiveInput();
            const { audioContext } = getAudioNodes();
            if (audioContext?.state === 'suspended') {
                await audioContext.resume();
            }
            const { model, ir } = await ensureSelections();
            await syncPlayerToEngine(model, ir, bypassed);
            setPlaying(true, id);
            const device = liveDeviceOptions.find(opt => opt.value === currentDeviceId)?.label ??
                null;
            onPlayLive?.({ model, ir, device });
        }
        else {
            setPlaying(false);
        }
    }, [
        id,
        sourceMode,
        bypassed,
        ensureSelections,
        getAudioNodes,
        reconnectLiveInput,
        setPlaying,
        syncPlayerToEngine,
        onPlayLive,
        liveDeviceOptions,
        currentDeviceId,
    ]);
    const openSettingsDialog = useCallback(async () => {
        const { model, ir } = await ensureSelections();
        openDialog({
            playerId: id,
            selectedModel: model,
            selectedIr: ir,
            onMonitoringChange: setLiveMonitoring,
        });
    }, [ensureSelections, openDialog, id, setLiveMonitoring]);
    const togglePlay = useCallback(async () => {
        const wantsLiveInput = sourceMode === 'live';
        const isActive = audioState.activePlayerId === id;
        // === LIVE MODE ===
        if (wantsLiveInput) {
            const { model, ir } = await ensureSelections();
            const { audioContext } = getAudioNodes();
            await reconnectLiveInput();
            if (audioContext && audioContext.state === 'suspended') {
                await audioContext.resume();
            }
            await syncPlayerToEngine(model, ir, bypassed);
            if (isActive) {
                setPlaying(false);
            }
            else {
                setPlaying(true, id);
                const device = liveDeviceOptions.find(opt => opt.value === currentDeviceId)?.label ??
                    null;
                onPlayLive?.({ model, ir, device });
            }
            return;
        }
        // === DEMO MODE ===
        const { mediaStream } = getAudioNodes();
        if (mediaStream?.active) {
            stopLiveInput();
        }
        setIsLoading(true);
        try {
            const { model, ir, input } = await ensureSelections();
            if (audioState.initState !== 'ready') {
                await init({ audioUrl: input.url, slimSize });
            }
            if (isActive) {
                setPlaying(false);
            }
            else {
                if (audioState.initState === 'ready') {
                    const { audioContext } = getAudioNodes();
                    if (audioContext)
                        await audioContext.resume();
                }
                if (!audioState.audioUrl || audioState.audioUrl !== input.url) {
                    await loadAudio(input.url);
                }
                await syncPlayerToEngine(model, ir, bypassed);
                setPlaying(true, id);
                onPlayDemo?.({ model, ir, input });
            }
        }
        catch (error) {
            console.error('Error in togglePlay:', error);
            setPlaying(false);
        }
        finally {
            setIsLoading(false);
        }
    }, [
        id,
        audioState.activePlayerId,
        audioState.initState,
        audioState.audioUrl,
        sourceMode,
        bypassed,
        slimSize,
        ensureSelections,
        getAudioNodes,
        init,
        loadAudio,
        reconnectLiveInput,
        stopLiveInput,
        setPlaying,
        syncPlayerToEngine,
        onPlayDemo,
        onPlayLive,
        liveDeviceOptions,
        currentDeviceId,
    ]);
    const handleSkipToStart = useCallback(() => {
        const audioElement = getAudioNodes().audioElement;
        if (audioElement) {
            audioElement.currentTime = 0;
            setCurrentTime(0);
        }
    }, [getAudioNodes]);
    const handleBypassToggle = useCallback(() => {
        const newBypassed = !bypassed;
        setBypassed(newBypassed);
        if (isThisPlayerActive) {
            setBypass(newBypassed);
        }
    }, [setBypass, bypassed, isThisPlayerActive]);
    const handleModelChange = useCallback(async (value) => {
        const model = models?.find(m => m.url === String(value));
        if (model) {
            setSelectedModel(model);
            try {
                if (audioState.initState === 'ready' && isThisPlayerActive) {
                    await loadModel(model.url, slimSize);
                }
                onModelChange?.(model);
            }
            catch (error) {
                console.error('Error loading model:', error);
            }
        }
    }, [
        models,
        loadModel,
        onModelChange,
        audioState.initState,
        isThisPlayerActive,
        slimSize,
    ]);
    const handleInputChange = useCallback(async (value) => {
        const wasThisPlayerPlaying = audioState.activePlayerId === id;
        const input = inputs?.find(i => i.url === String(value));
        if (!input)
            return;
        setSelectedInput(input);
        try {
            if (audioState.initState === 'ready' &&
                (!audioState.audioUrl || audioState.audioUrl !== input.url)) {
                await loadAudio(input.url);
            }
            if (wasThisPlayerPlaying) {
                setPlaying(true, id);
            }
            onInputChange?.(input);
        }
        catch (error) {
            console.error('Error loading audio:', error);
        }
    }, [
        id,
        audioState.activePlayerId,
        audioState.audioUrl,
        audioState.initState,
        inputs,
        loadAudio,
        onInputChange,
        setPlaying,
    ]);
    const handleIrChange = useCallback(async (value) => {
        const ir = irs?.find(i => i.url === String(value));
        if (!ir)
            return;
        setSelectedIr(ir);
        try {
            if (isThisPlayerActive) {
                if (audioState.initState === 'ready' && ir.url) {
                    await loadIr({
                        url: ir.url,
                        mix: ir.mix,
                        gain: ir.gain,
                    });
                }
                else {
                    removeIr();
                }
            }
            onIrChange?.(ir);
        }
        catch (error) {
            console.error('Error loading IR:', error);
        }
    }, [
        irs,
        loadIr,
        removeIr,
        onIrChange,
        audioState.initState,
        isThisPlayerActive,
    ]);
    const bypassedStyles = bypassed
        ? 'opacity-50 touch-none cursor-not-allowed grayscale'
        : '';
    return {
        selectedModel,
        selectedInput,
        selectedIr,
        currentTime,
        duration,
        isLoading,
        bypassed,
        isThisPlayerActive,
        sourceMode,
        isLiveConfigured,
        currentDeviceId,
        bypassedStyles,
        modelOptions,
        audioOptions,
        irOptions,
        liveDeviceOptions,
        togglePlay,
        handleSkipToStart,
        handleBypassToggle,
        handleModelChange,
        handleInputChange,
        handleIrChange,
        handleSourceModeChange,
        handleLiveDeviceChange,
        openSettingsDialog,
        visualizerRef,
        canvasWrapperRef,
        audioInputDevices,
        inputModeType: audioState.inputMode.type,
    };
}

/**
 * @license lucide-react v0.513.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */

const toKebabCase = (string) => string.replace(/([a-z0-9])([A-Z])/g, "$1-$2").toLowerCase();
const toCamelCase = (string) => string.replace(
  /^([A-Z])|[\s-_]+(\w)/g,
  (match, p1, p2) => p2 ? p2.toUpperCase() : p1.toLowerCase()
);
const toPascalCase = (string) => {
  const camelCase = toCamelCase(string);
  return camelCase.charAt(0).toUpperCase() + camelCase.slice(1);
};
const mergeClasses = (...classes) => classes.filter((className, index, array) => {
  return Boolean(className) && className.trim() !== "" && array.indexOf(className) === index;
}).join(" ").trim();
const hasA11yProp = (props) => {
  for (const prop in props) {
    if (prop.startsWith("aria-") || prop === "role" || prop === "title") {
      return true;
    }
  }
};

/**
 * @license lucide-react v0.513.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */

var defaultAttributes = {
  xmlns: "http://www.w3.org/2000/svg",
  width: 24,
  height: 24,
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  strokeWidth: 2,
  strokeLinecap: "round",
  strokeLinejoin: "round"
};

/**
 * @license lucide-react v0.513.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */


const Icon = forwardRef(
  ({
    color = "currentColor",
    size = 24,
    strokeWidth = 2,
    absoluteStrokeWidth,
    className = "",
    children,
    iconNode,
    ...rest
  }, ref) => createElement(
    "svg",
    {
      ref,
      ...defaultAttributes,
      width: size,
      height: size,
      stroke: color,
      strokeWidth: absoluteStrokeWidth ? Number(strokeWidth) * 24 / Number(size) : strokeWidth,
      className: mergeClasses("lucide", className),
      ...!children && !hasA11yProp(rest) && { "aria-hidden": "true" },
      ...rest
    },
    [
      ...iconNode.map(([tag, attrs]) => createElement(tag, attrs)),
      ...Array.isArray(children) ? children : [children]
    ]
  )
);

/**
 * @license lucide-react v0.513.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */


const createLucideIcon = (iconName, iconNode) => {
  const Component = forwardRef(
    ({ className, ...props }, ref) => createElement(Icon, {
      ref,
      iconNode,
      className: mergeClasses(
        `lucide-${toKebabCase(toPascalCase(iconName))}`,
        `lucide-${iconName}`,
        className
      ),
      ...props
    })
  );
  Component.displayName = toPascalCase(iconName);
  return Component;
};

/**
 * @license lucide-react v0.513.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */


const __iconNode$9 = [["path", { d: "m6 9 6 6 6-6", key: "qrunsl" }]];
const ChevronDown = createLucideIcon("chevron-down", __iconNode$9);

/**
 * @license lucide-react v0.513.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */


const __iconNode$8 = [["path", { d: "m18 15-6-6-6 6", key: "153udz" }]];
const ChevronUp = createLucideIcon("chevron-up", __iconNode$8);

/**
 * @license lucide-react v0.513.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */


const __iconNode$7 = [
  ["circle", { cx: "12", cy: "12", r: "10", key: "1mglay" }],
  ["line", { x1: "12", x2: "12", y1: "8", y2: "12", key: "1pkeuh" }],
  ["line", { x1: "12", x2: "12.01", y1: "16", y2: "16", key: "4dfq90" }]
];
const CircleAlert = createLucideIcon("circle-alert", __iconNode$7);

/**
 * @license lucide-react v0.513.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */


const __iconNode$6 = [
  ["circle", { cx: "12", cy: "12", r: "10", key: "1mglay" }],
  ["path", { d: "M12 16v-4", key: "1dtifu" }],
  ["path", { d: "M12 8h.01", key: "e9boi3" }]
];
const Info = createLucideIcon("info", __iconNode$6);

/**
 * @license lucide-react v0.513.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */


const __iconNode$5 = [["path", { d: "M21 12a9 9 0 1 1-6.219-8.56", key: "13zald" }]];
const LoaderCircle = createLucideIcon("loader-circle", __iconNode$5);

/**
 * @license lucide-react v0.513.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */


const __iconNode$4 = [
  ["rect", { width: "18", height: "11", x: "3", y: "11", rx: "2", ry: "2", key: "1w4ew1" }],
  ["path", { d: "M7 11V7a5 5 0 0 1 10 0v4", key: "fwvmzm" }]
];
const Lock = createLucideIcon("lock", __iconNode$4);

/**
 * @license lucide-react v0.513.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */


const __iconNode$3 = [
  ["path", { d: "M12 2a3 3 0 0 0-3 3v7a3 3 0 0 0 6 0V5a3 3 0 0 0-3-3Z", key: "131961" }],
  ["path", { d: "M19 10v2a7 7 0 0 1-14 0v-2", key: "1vc78b" }],
  ["line", { x1: "12", x2: "12", y1: "19", y2: "22", key: "x3vr5v" }]
];
const Mic = createLucideIcon("mic", __iconNode$3);

/**
 * @license lucide-react v0.513.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */


const __iconNode$2 = [
  [
    "path",
    {
      d: "M12.22 2h-.44a2 2 0 0 0-2 2v.18a2 2 0 0 1-1 1.73l-.43.25a2 2 0 0 1-2 0l-.15-.08a2 2 0 0 0-2.73.73l-.22.38a2 2 0 0 0 .73 2.73l.15.1a2 2 0 0 1 1 1.72v.51a2 2 0 0 1-1 1.74l-.15.09a2 2 0 0 0-.73 2.73l.22.38a2 2 0 0 0 2.73.73l.15-.08a2 2 0 0 1 2 0l.43.25a2 2 0 0 1 1 1.73V20a2 2 0 0 0 2 2h.44a2 2 0 0 0 2-2v-.18a2 2 0 0 1 1-1.73l.43-.25a2 2 0 0 1 2 0l.15.08a2 2 0 0 0 2.73-.73l.22-.39a2 2 0 0 0-.73-2.73l-.15-.08a2 2 0 0 1-1-1.74v-.5a2 2 0 0 1 1-1.74l.15-.09a2 2 0 0 0 .73-2.73l-.22-.38a2 2 0 0 0-2.73-.73l-.15.08a2 2 0 0 1-2 0l-.43-.25a2 2 0 0 1-1-1.73V4a2 2 0 0 0-2-2z",
      key: "1qme2f"
    }
  ],
  ["circle", { cx: "12", cy: "12", r: "3", key: "1v7zrd" }]
];
const Settings = createLucideIcon("settings", __iconNode$2);

/**
 * @license lucide-react v0.513.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */


const __iconNode$1 = [
  [
    "path",
    {
      d: "m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3",
      key: "wmoenq"
    }
  ],
  ["path", { d: "M12 9v4", key: "juzpu7" }],
  ["path", { d: "M12 17h.01", key: "p32p05" }]
];
const TriangleAlert = createLucideIcon("triangle-alert", __iconNode$1);

/**
 * @license lucide-react v0.513.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */


const __iconNode = [
  ["path", { d: "M18 6 6 18", key: "1bl5f8" }],
  ["path", { d: "m6 6 12 12", key: "d8bk6v" }]
];
const X = createLucideIcon("x", __iconNode);

const Select = ({ options, label, heading, value, onChange, disabled = false, backgroundColor = 'bg-zinc-900', infoModal, }) => {
    const [isOpen, setIsOpen] = useState(false);
    const selectedOption = options.find(opt => opt.value === value) || options[0];
    const containerRef = useRef(null);
    const buttonRef = useRef(null);
    const listRef = useRef(null);
    const [activeIndex, setActiveIndex] = useState(-1);
    // Create a safe ID from the label
    const selectId = label ? label.toLowerCase().replace(/\s+/g, '-') : 'select';
    useEffect(() => {
        const handleOutsideClick = (event) => {
            if (containerRef.current &&
                !containerRef.current.contains(event.target)) {
                setIsOpen(false);
            }
        };
        if (isOpen) {
            document.addEventListener('mousedown', handleOutsideClick);
        }
        return () => {
            document.removeEventListener('mousedown', handleOutsideClick);
        };
    }, [isOpen]);
    const handleSelect = useCallback((option) => {
        setIsOpen(false);
        buttonRef.current?.focus();
        if (onChange) {
            onChange(option.value);
        }
    }, [onChange]);
    const keydownHandlerRef = useRef();
    keydownHandlerRef.current = (e) => {
        if (!isOpen) {
            if (e.key === 'Enter' || e.key === ' ' || e.key === 'ArrowDown') {
                e.preventDefault();
                setIsOpen(true);
                setActiveIndex(options.findIndex(opt => opt.value === selectedOption.value));
            }
            return;
        }
        switch (e.key) {
            case 'Enter':
            case ' ':
                e.preventDefault();
                if (activeIndex >= 0 && activeIndex < options.length) {
                    handleSelect(options[activeIndex]);
                }
                break;
            case 'ArrowDown':
                e.preventDefault();
                if (activeIndex < options.length - 1) {
                    setActiveIndex(prev => prev + 1);
                }
                break;
            case 'ArrowUp':
                e.preventDefault();
                if (activeIndex > 0) {
                    setActiveIndex(prev => prev - 1);
                }
                break;
            case 'Escape':
                setIsOpen(false);
                buttonRef.current?.focus();
                break;
            case 'Tab':
                setIsOpen(false);
                break;
        }
    };
    useEffect(() => {
        const button = buttonRef.current;
        if (!button)
            return;
        const listener = (e) => keydownHandlerRef.current?.(e);
        button.addEventListener('keydown', listener);
        return () => button.removeEventListener('keydown', listener);
    }, []);
    // Add effect for scrolling active item into view
    useEffect(() => {
        if (!isOpen || activeIndex < 0 || !listRef.current)
            return;
        const activeElement = document.getElementById(`${selectId}-option-${activeIndex}`);
        if (!activeElement)
            return;
        const listElement = listRef.current;
        const activeRect = activeElement.getBoundingClientRect();
        const listRect = listElement.getBoundingClientRect();
        if (activeRect.bottom > listRect.bottom) {
            listElement.scrollTop += activeRect.bottom - listRect.bottom;
        }
        else if (activeRect.top < listRect.top) {
            listElement.scrollTop -= listRect.top - activeRect.top;
        }
    }, [activeIndex, isOpen, selectId]);
    return (React.createElement("div", { ref: containerRef, className: 'flex flex-col w-full' },
        heading && (React.createElement("span", { className: 'text-base text-white font-semibold', id: `${selectId}-heading` }, heading)),
        React.createElement("div", { className: `flex justify-between items-end ${heading ? 'pb-2' : 'pb-1'}` },
            label && (React.createElement("span", { className: 'text-sm text-zinc-400', id: `${selectId}-label` }, label)),
            !!infoModal && infoModal),
        React.createElement("div", { className: 'relative' },
            React.createElement("button", { ref: buttonRef, className: `flex items-center justify-between w-full overflow-hidden px-4 py-3 text-md border border-zinc-700 rounded-md bg-transparent focus:outline-none ${disabled ? 'touch-none cursor-not-allowed' : ''}`, onClick: () => setIsOpen(!isOpen), disabled: disabled, "aria-haspopup": 'listbox', "aria-expanded": isOpen, "aria-labelledby": `${selectId}-label`, "aria-controls": `${selectId}-options`, id: `${selectId}-button` },
                React.createElement("span", { className: 'text-ellipsis text-nowrap overflow-hidden min-w-0' }, selectedOption?.label),
                React.createElement(ChevronDown, { className: 'flex-shrink-0', size: 24 })),
            isOpen && (React.createElement("div", { className: `absolute z-10 w-full mt-1 ${backgroundColor} rounded-md shadow-lg` },
                React.createElement("ul", { ref: listRef, id: `${selectId}-options`, role: 'listbox', className: 'py-1 overflow-auto text-base rounded-md max-h-48 focus:outline-none border border-zinc-700', "aria-label": label || 'Options', "aria-labelledby": `${selectId}-label` }, options.map((option, index) => (React.createElement("li", { key: option.value + `${index}`, id: `${selectId}-option-${index}`, role: 'option', "aria-selected": selectedOption.value === option.value, className: `text-ellipsis text-nowrap overflow-hidden cursor-default select-none relative py-2 pl-3 pr-9 ${activeIndex === index ? 'bg-zinc-800' : 'hover:bg-zinc-800'}`, onClick: () => handleSelect(option), onMouseEnter: () => setActiveIndex(index) }, option.label)))))))));
};

const ToggleSimple = ({ isChecked, onChange, disabled = false, label, ariaLabel, }) => {
    const handleToggle = () => {
        const newValue = !isChecked;
        onChange(newValue);
    };
    return (React.createElement("label", { className: `flex flex-col gap-1 ${disabled ? 'touch-none cursor-not-allowed' : 'cursor-pointer'}` },
        label && React.createElement("span", { className: `text-sm text-zinc-400` }, label),
        React.createElement("div", { className: 'relative' },
            React.createElement("input", { type: 'checkbox', className: 'sr-only', checked: isChecked, onChange: handleToggle, disabled: disabled, "aria-label": ariaLabel }),
            React.createElement("div", { style: isChecked ? { backgroundColor: '#00D13B' } : {}, className: `w-10 h-6 bg-zinc-500 rounded-full shadow-inner transition-colors duration-300 ease-in-out` }),
            React.createElement("div", { className: `absolute w-4 h-4 bg-white rounded-full shadow top-1 left-1 transition-transform duration-300 ease-in-out ${isChecked ? 'transform translate-x-full' : ''}` }))));
};

const variantStyles$1 = {
    primary: 'bg-gradient-to-b from-[#00F] to-[#0000D0] text-white border-0 rounded-xl',
    secondary: 'bg-transparent text-white border border-white rounded-full',
    ghost: 'bg-transparent text-zinc-400 border border-transparent rounded-xl',
};
const sizeStyles = {
    sm: 'px-3 py-1.5 text-sm',
    md: 'px-5 py-3.5 text-base',
    lg: 'px-5 py-3.5 text-md',
};
const Button = ({ variant = 'primary', size = 'md', fullWidth = false, children, leftIcon, rightIcon, className = '', disabled, ...props }) => {
    return (React.createElement("button", { className: `
        flex items-center justify-center gap-2 font-medium
        focus:outline-none
        disabled:opacity-50 disabled:cursor-not-allowed
        ${variantStyles$1[variant]}
        ${sizeStyles[size]}
        ${fullWidth ? 'w-full self-stretch' : ''}
        ${className}
      `, disabled: disabled, ...props },
        leftIcon && React.createElement("span", { className: 'flex-shrink-0' }, leftIcon),
        children,
        rightIcon && React.createElement("span", { className: 'flex-shrink-0' }, rightIcon)));
};

const Guitar = () => (React.createElement("svg", { width: '64', height: '64', viewBox: '0 0 64 64', fill: 'none', xmlns: 'http://www.w3.org/2000/svg' },
    React.createElement("path", { d: 'M58.3304 6.02963C58.2244 6.04282 58.0256 6.10872 57.9991 5.95055C57.9859 5.87147 58.0058 5.79239 58.0124 5.71331C58.019 5.63423 58.0058 5.55515 57.9793 5.48265C57.9395 5.3838 57.8733 5.29154 57.7871 5.23223C57.5552 5.06748 57.0782 5.08066 56.588 4.83023C56.0977 4.57322 55.6339 3.99988 55.6339 3.99988L55.402 4.53368C55.137 5.04112 54.819 5.51561 54.4745 5.96373L54.3287 5.81875L54.342 5.80557C54.3817 5.76603 54.3883 5.68695 54.3486 5.64741C54.3353 5.63423 54.3155 5.62105 54.2956 5.61446L54.3221 5.32449L53.8384 4.73138C53.8384 4.73138 53.5204 5.08725 53.3415 5.00817L53.3084 5.04771C53.3879 5.22564 53.0301 5.54197 53.0301 5.54197L53.6264 6.02305L53.9179 5.99668C53.9246 6.01646 53.9312 6.03623 53.9444 6.04941C53.9842 6.08895 54.0571 6.08895 54.1035 6.04941L54.1233 6.02963L54.2956 6.20098C53.9577 6.61616 53.5933 6.99179 53.2289 7.34107L53.0169 7.13019L53.0368 7.11041C53.0765 7.07087 53.0765 6.99838 53.0368 6.95884C53.0235 6.94566 53.0036 6.93248 52.9838 6.92589L53.0103 6.63593L52.5266 6.04282C52.5266 6.04282 52.2086 6.39868 52.0297 6.3196L51.9966 6.35255C52.0761 6.53048 51.7183 6.84681 51.7183 6.84681L52.3146 7.32789L52.6061 7.30153C52.6127 7.3213 52.6194 7.34107 52.6393 7.35425C52.679 7.39379 52.7519 7.40038 52.7983 7.35425L52.8181 7.33448L53.0235 7.53877C52.5796 7.94736 52.1357 8.30323 51.7382 8.59978L51.6388 8.50093L51.6587 8.48116C51.6984 8.44162 51.6984 8.36913 51.6587 8.32959C51.6454 8.31641 51.6256 8.30323 51.6057 8.30323L51.6322 8.01326L51.1485 7.42015C51.1485 7.42015 50.8305 7.77602 50.6516 7.69694L50.6185 7.72989C50.698 7.90782 50.3403 8.22414 50.3403 8.22414L50.9365 8.70522L51.2281 8.67886C51.2347 8.69863 51.2413 8.7184 51.2612 8.73158C51.3009 8.77113 51.3738 8.77772 51.4136 8.73158L51.4334 8.71181L51.4997 8.77772C50.7047 9.34447 50.1349 9.66079 50.1349 9.66079C50.3933 11.4467 49.5055 12.3627 49.5055 12.3627L27.0456 33.9454C27.0456 33.9454 24.3756 36.5419 23.0902 34.2419C22.0302 32.3506 23.9648 30.8678 24.6737 30.4065C24.7665 30.3472 24.74 30.2022 24.6339 30.1824C21.0894 29.3257 17.6111 31.8827 15.9879 34.868C15.3386 36.0542 15.1663 37.3986 14.1195 38.3542C13.8943 38.5585 13.6425 38.743 13.3775 38.8946C12.1121 39.6129 10.5154 39.4547 9.13066 39.7579C7.48095 40.1203 6.16251 41.0891 5.17533 42.4401C4.47305 43.3956 4.06228 44.5291 4.00928 45.7087C3.83039 49.3729 6.26852 52.5691 8.75964 55.0865C8.99153 55.3171 9.21679 55.5478 9.44868 55.7653C10.7009 56.9779 12.0259 58.1377 13.5696 58.9549C16.4715 60.5036 20.1155 60.4904 22.3747 57.828C23.7462 56.2068 24.2629 54.0387 24.5346 51.9825C25.0778 47.8374 28.2183 49.1027 31.0738 46.3085C33.492 43.936 33.2204 41.0891 33.0746 40.2785C33.0548 40.1796 32.9421 40.1401 32.8692 40.206C32.3723 40.6541 30.8088 41.8403 29.4506 40.5882C27.7479 39.112 29.3711 37.2866 29.3711 37.2866C29.3909 37.2668 51.6123 14.5375 51.6123 14.5375C51.6123 14.5375 52.5332 13.6478 54.3287 13.9114C54.3287 13.9114 54.6467 13.3381 55.2231 12.5539L55.3358 12.6659L55.3159 12.6857C55.2761 12.7252 55.2761 12.7977 55.3159 12.8372C55.3291 12.8504 55.349 12.8636 55.3689 12.8702L55.3424 13.1601L55.826 13.7533C55.826 13.7533 56.1441 13.3974 56.3229 13.4765L56.3561 13.4435C56.2766 13.2656 56.6343 12.9493 56.6343 12.9493L56.038 12.4682L55.7465 12.4945C55.7399 12.4748 55.7333 12.455 55.7134 12.4418C55.667 12.3957 55.6008 12.3957 55.5544 12.4418L55.5345 12.4616L55.3888 12.3166C55.6736 11.941 56.0115 11.5258 56.3892 11.1106L56.641 11.361L56.6211 11.3808C56.5747 11.4269 56.5747 11.4928 56.6211 11.539C56.6343 11.5522 56.6542 11.5653 56.6741 11.5653L56.6476 11.8553L57.1312 12.4484C57.1312 12.4484 57.4492 12.0925 57.6281 12.1716L57.6613 12.1387C57.5817 11.9607 57.9395 11.6444 57.9395 11.6444L57.3432 11.1633L57.0517 11.1897C57.0451 11.1699 57.0385 11.1502 57.0186 11.137C56.9788 11.0908 56.906 11.0908 56.8662 11.137L56.8463 11.1567L56.5813 10.8931C56.9457 10.5043 57.3499 10.1221 57.7938 9.75964L58.019 9.98371L57.9991 10.0035C57.9594 10.0496 57.9594 10.1155 57.9991 10.1616C58.0124 10.1748 58.0323 10.188 58.0522 10.188L58.0256 10.478L58.5093 11.0711C58.5093 11.0711 58.8273 10.7152 59.0062 10.7943L59.0393 10.7613C58.9598 10.5834 59.3176 10.2671 59.3176 10.2671L58.7213 9.786L58.4298 9.81236C58.4232 9.79259 58.4165 9.77282 58.4033 9.75964C58.3635 9.7201 58.2907 9.7201 58.2443 9.75964L58.2244 9.77941L58.0256 9.58171C58.4762 9.23902 58.9532 8.91611 59.4633 8.65909L60 8.42844C60 8.42844 59.417 7.96713 59.1652 7.47946C59.1122 7.37402 59.0658 7.26858 59.0261 7.15655C58.9664 6.99179 58.9333 6.81386 58.8936 6.64911C58.8472 6.47117 58.7942 6.28665 58.6418 6.17462C58.5954 6.056 58.4563 6.00986 58.3304 6.02963ZM17.7767 58.2102C17.4455 58.5397 16.9088 58.5397 16.5842 58.2102C16.2529 57.8807 16.2529 57.3469 16.5842 57.024C16.9154 56.6945 17.4521 56.6945 17.7767 57.024C18.108 57.3535 18.108 57.8807 17.7767 58.2102ZM15.9879 56.4309C15.6566 56.7604 15.12 56.7604 14.7953 56.4309C14.4707 56.1014 14.4641 55.5676 14.7953 55.2447C15.1266 54.9151 15.6632 54.9151 15.9879 55.2447C16.3125 55.5742 16.3125 56.1014 15.9879 56.4309ZM20.6124 56.8065C20.2811 57.136 19.7445 57.136 19.4198 56.8065C19.0952 56.477 19.0885 55.9432 19.4198 55.6203C19.7511 55.2908 20.2877 55.2908 20.6124 55.6203C20.937 55.9498 20.9436 56.477 20.6124 56.8065ZM18.7043 54.9086C18.373 55.2381 17.8364 55.2381 17.5117 54.9086C17.1871 54.5791 17.1804 54.0453 17.5117 53.7223C17.843 53.3928 18.3796 53.3928 18.7043 53.7223C19.0289 54.0518 19.0355 54.5791 18.7043 54.9086ZM27.887 38.5585C27.887 38.5585 26.9926 40.4696 28.6025 41.781C29.656 42.6378 30.6365 42.7432 31.1533 42.4796C31.6701 42.216 30.4709 45.2211 27.9996 46.6182C27.0721 47.1454 25.906 47.4288 25.0845 48.1207C24.1635 48.8918 23.6402 50.3021 23.4613 51.4553C22.7987 51.3763 22.229 50.9149 21.8314 50.3811C21.4339 49.8473 21.1689 49.2213 20.8509 48.6414C20.2148 47.4815 19.3867 46.4468 18.2339 45.7615C17.5316 45.3463 16.7829 45.03 16.0608 44.6477C15.862 44.5423 13.9672 43.1254 14.4972 42.8289C14.4972 42.8289 20.5991 39.3559 21.2153 34.9603C21.2153 34.9603 21.7784 35.6193 22.1759 35.8433C22.5735 36.0608 23.1499 36.3244 23.819 36.1069C24.4882 35.8961 25.1706 35.8697 25.1706 35.8697L27.887 38.5585Z', fill: '#8D8D93' }),
    React.createElement("path", { d: 'M27.1184 39.7316C27.231 39.8437 27.2045 39.9557 27.1184 40.048L25.6542 41.5044C25.5681 41.59 25.4289 41.59 25.3362 41.5044L22.1825 38.3675C22.0964 38.2818 22.0964 38.1434 22.1825 38.0512L23.6467 36.5947C23.7328 36.5091 23.872 36.5091 23.9316 36.5684L27.1184 39.7316Z', fill: '#8D8D93' }),
    React.createElement("path", { d: 'M17.6178 42.8421C17.5317 42.7565 17.5317 42.6181 17.6178 42.5258L19.082 41.0694C19.1681 40.9837 19.3073 40.9837 19.4 41.0694L22.5802 44.2326C22.6663 44.3183 22.6663 44.4567 22.5802 44.549L21.116 46.0054C21.0299 46.0911 20.8907 46.0911 20.798 46.0054L17.6178 42.8421Z', fill: '#8D8D93' })));

// Height of placeholder is 254px to match the "demo" mode height so there is not a jump when switching modes
const LiveTakeover = ({ onConnect }) => {
    return (React.createElement("div", { className: 'flex flex-col items-center justify-center gap-6 bg-zinc-900 rounded-lg min-h-[254px]' },
        React.createElement("div", { className: 'flex flex-col items-center gap-6' },
            React.createElement("div", { className: 'flex flex-col items-center gap-2 text-center' },
                React.createElement(Guitar, null),
                React.createElement("h3", { className: 'font-semibold text-white' }, "Play Live"),
                React.createElement("p", { className: 'text-base text-zinc-400 max-w-[360px]' }, "Play tones directly on the TONE3000 website by connecting your instrument to an audio interface.")),
            React.createElement(Button, { variant: 'secondary', onClick: onConnect }, "Connect"))));
};

function clamp(v, min, max) {
  return Math.max(min, Math.min(v, max));
}
const V = {
  toVector(v, fallback) {
    if (v === undefined) v = fallback;
    return Array.isArray(v) ? v : [v, v];
  },
  add(v1, v2) {
    return [v1[0] + v2[0], v1[1] + v2[1]];
  },
  sub(v1, v2) {
    return [v1[0] - v2[0], v1[1] - v2[1]];
  },
  addTo(v1, v2) {
    v1[0] += v2[0];
    v1[1] += v2[1];
  },
  subTo(v1, v2) {
    v1[0] -= v2[0];
    v1[1] -= v2[1];
  }
};
function rubberband(distance, dimension, constant) {
  if (dimension === 0 || Math.abs(dimension) === Infinity) return Math.pow(distance, constant * 5);
  return distance * dimension * constant / (dimension + constant * distance);
}
function rubberbandIfOutOfBounds(position, min, max, constant = 0.15) {
  if (constant === 0) return clamp(position, min, max);
  if (position < min) return -rubberband(min - position, max - min, constant) + min;
  if (position > max) return +rubberband(position - max, max - min, constant) + max;
  return position;
}
function computeRubberband(bounds, [Vx, Vy], [Rx, Ry]) {
  const [[X0, X1], [Y0, Y1]] = bounds;
  return [rubberbandIfOutOfBounds(Vx, X0, X1, Rx), rubberbandIfOutOfBounds(Vy, Y0, Y1, Ry)];
}

function _toPrimitive(input, hint) {
  if (typeof input !== "object" || input === null) return input;
  var prim = input[Symbol.toPrimitive];
  if (prim !== undefined) {
    var res = prim.call(input, hint || "default");
    if (typeof res !== "object") return res;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (hint === "string" ? String : Number)(input);
}

function _toPropertyKey(arg) {
  var key = _toPrimitive(arg, "string");
  return typeof key === "symbol" ? key : String(key);
}

function _defineProperty(obj, key, value) {
  key = _toPropertyKey(key);
  if (key in obj) {
    Object.defineProperty(obj, key, {
      value: value,
      enumerable: true,
      configurable: true,
      writable: true
    });
  } else {
    obj[key] = value;
  }
  return obj;
}

function ownKeys(e, r) {
  var t = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var o = Object.getOwnPropertySymbols(e);
    r && (o = o.filter(function (r) {
      return Object.getOwnPropertyDescriptor(e, r).enumerable;
    })), t.push.apply(t, o);
  }
  return t;
}
function _objectSpread2(e) {
  for (var r = 1; r < arguments.length; r++) {
    var t = null != arguments[r] ? arguments[r] : {};
    r % 2 ? ownKeys(Object(t), !0).forEach(function (r) {
      _defineProperty(e, r, t[r]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) {
      Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
    });
  }
  return e;
}

const EVENT_TYPE_MAP = {
  pointer: {
    start: 'down',
    change: 'move',
    end: 'up'
  },
  mouse: {
    start: 'down',
    change: 'move',
    end: 'up'
  },
  touch: {
    start: 'start',
    change: 'move',
    end: 'end'
  },
  gesture: {
    start: 'start',
    change: 'change',
    end: 'end'
  }
};
function capitalize(string) {
  if (!string) return '';
  return string[0].toUpperCase() + string.slice(1);
}
const actionsWithoutCaptureSupported = ['enter', 'leave'];
function hasCapture(capture = false, actionKey) {
  return capture && !actionsWithoutCaptureSupported.includes(actionKey);
}
function toHandlerProp(device, action = '', capture = false) {
  const deviceProps = EVENT_TYPE_MAP[device];
  const actionKey = deviceProps ? deviceProps[action] || action : action;
  return 'on' + capitalize(device) + capitalize(actionKey) + (hasCapture(capture, actionKey) ? 'Capture' : '');
}
const pointerCaptureEvents = ['gotpointercapture', 'lostpointercapture'];
function parseProp(prop) {
  let eventKey = prop.substring(2).toLowerCase();
  const passive = !!~eventKey.indexOf('passive');
  if (passive) eventKey = eventKey.replace('passive', '');
  const captureKey = pointerCaptureEvents.includes(eventKey) ? 'capturecapture' : 'capture';
  const capture = !!~eventKey.indexOf(captureKey);
  if (capture) eventKey = eventKey.replace('capture', '');
  return {
    device: eventKey,
    capture,
    passive
  };
}
function toDomEventType(device, action = '') {
  const deviceProps = EVENT_TYPE_MAP[device];
  const actionKey = deviceProps ? deviceProps[action] || action : action;
  return device + actionKey;
}
function isTouch(event) {
  return 'touches' in event;
}
function getPointerType(event) {
  if (isTouch(event)) return 'touch';
  if ('pointerType' in event) return event.pointerType;
  return 'mouse';
}
function getCurrentTargetTouchList(event) {
  return Array.from(event.touches).filter(e => {
    var _event$currentTarget, _event$currentTarget$;
    return e.target === event.currentTarget || ((_event$currentTarget = event.currentTarget) === null || _event$currentTarget === void 0 || (_event$currentTarget$ = _event$currentTarget.contains) === null || _event$currentTarget$ === void 0 ? void 0 : _event$currentTarget$.call(_event$currentTarget, e.target));
  });
}
function getTouchList(event) {
  return event.type === 'touchend' || event.type === 'touchcancel' ? event.changedTouches : event.targetTouches;
}
function getValueEvent(event) {
  return isTouch(event) ? getTouchList(event)[0] : event;
}
function touchIds(event) {
  return getCurrentTargetTouchList(event).map(touch => touch.identifier);
}
function pointerId(event) {
  const valueEvent = getValueEvent(event);
  return isTouch(event) ? valueEvent.identifier : valueEvent.pointerId;
}
function pointerValues(event) {
  const valueEvent = getValueEvent(event);
  return [valueEvent.clientX, valueEvent.clientY];
}
function getEventDetails(event) {
  const payload = {};
  if ('buttons' in event) payload.buttons = event.buttons;
  if ('shiftKey' in event) {
    const {
      shiftKey,
      altKey,
      metaKey,
      ctrlKey
    } = event;
    Object.assign(payload, {
      shiftKey,
      altKey,
      metaKey,
      ctrlKey
    });
  }
  return payload;
}

function call(v, ...args) {
  if (typeof v === 'function') {
    return v(...args);
  } else {
    return v;
  }
}
function noop() {}
function chain(...fns) {
  if (fns.length === 0) return noop;
  if (fns.length === 1) return fns[0];
  return function () {
    let result;
    for (const fn of fns) {
      result = fn.apply(this, arguments) || result;
    }
    return result;
  };
}
function assignDefault(value, fallback) {
  return Object.assign({}, fallback, value || {});
}

const BEFORE_LAST_KINEMATICS_DELAY = 32;
class Engine {
  constructor(ctrl, args, key) {
    this.ctrl = ctrl;
    this.args = args;
    this.key = key;
    if (!this.state) {
      this.state = {};
      this.computeValues([0, 0]);
      this.computeInitial();
      if (this.init) this.init();
      this.reset();
    }
  }
  get state() {
    return this.ctrl.state[this.key];
  }
  set state(state) {
    this.ctrl.state[this.key] = state;
  }
  get shared() {
    return this.ctrl.state.shared;
  }
  get eventStore() {
    return this.ctrl.gestureEventStores[this.key];
  }
  get timeoutStore() {
    return this.ctrl.gestureTimeoutStores[this.key];
  }
  get config() {
    return this.ctrl.config[this.key];
  }
  get sharedConfig() {
    return this.ctrl.config.shared;
  }
  get handler() {
    return this.ctrl.handlers[this.key];
  }
  reset() {
    const {
      state,
      shared,
      ingKey,
      args
    } = this;
    shared[ingKey] = state._active = state.active = state._blocked = state._force = false;
    state._step = [false, false];
    state.intentional = false;
    state._movement = [0, 0];
    state._distance = [0, 0];
    state._direction = [0, 0];
    state._delta = [0, 0];
    state._bounds = [[-Infinity, Infinity], [-Infinity, Infinity]];
    state.args = args;
    state.axis = undefined;
    state.memo = undefined;
    state.elapsedTime = state.timeDelta = 0;
    state.direction = [0, 0];
    state.distance = [0, 0];
    state.overflow = [0, 0];
    state._movementBound = [false, false];
    state.velocity = [0, 0];
    state.movement = [0, 0];
    state.delta = [0, 0];
    state.timeStamp = 0;
  }
  start(event) {
    const state = this.state;
    const config = this.config;
    if (!state._active) {
      this.reset();
      this.computeInitial();
      state._active = true;
      state.target = event.target;
      state.currentTarget = event.currentTarget;
      state.lastOffset = config.from ? call(config.from, state) : state.offset;
      state.offset = state.lastOffset;
      state.startTime = state.timeStamp = event.timeStamp;
    }
  }
  computeValues(values) {
    const state = this.state;
    state._values = values;
    state.values = this.config.transform(values);
  }
  computeInitial() {
    const state = this.state;
    state._initial = state._values;
    state.initial = state.values;
  }
  compute(event) {
    const {
      state,
      config,
      shared
    } = this;
    state.args = this.args;
    let dt = 0;
    if (event) {
      state.event = event;
      if (config.preventDefault && event.cancelable) state.event.preventDefault();
      state.type = event.type;
      shared.touches = this.ctrl.pointerIds.size || this.ctrl.touchIds.size;
      shared.locked = !!document.pointerLockElement;
      Object.assign(shared, getEventDetails(event));
      shared.down = shared.pressed = shared.buttons % 2 === 1 || shared.touches > 0;
      dt = event.timeStamp - state.timeStamp;
      state.timeStamp = event.timeStamp;
      state.elapsedTime = state.timeStamp - state.startTime;
    }
    if (state._active) {
      const _absoluteDelta = state._delta.map(Math.abs);
      V.addTo(state._distance, _absoluteDelta);
    }
    if (this.axisIntent) this.axisIntent(event);
    const [_m0, _m1] = state._movement;
    const [t0, t1] = config.threshold;
    const {
      _step,
      values
    } = state;
    if (config.hasCustomTransform) {
      if (_step[0] === false) _step[0] = Math.abs(_m0) >= t0 && values[0];
      if (_step[1] === false) _step[1] = Math.abs(_m1) >= t1 && values[1];
    } else {
      if (_step[0] === false) _step[0] = Math.abs(_m0) >= t0 && Math.sign(_m0) * t0;
      if (_step[1] === false) _step[1] = Math.abs(_m1) >= t1 && Math.sign(_m1) * t1;
    }
    state.intentional = _step[0] !== false || _step[1] !== false;
    if (!state.intentional) return;
    const movement = [0, 0];
    if (config.hasCustomTransform) {
      const [v0, v1] = values;
      movement[0] = _step[0] !== false ? v0 - _step[0] : 0;
      movement[1] = _step[1] !== false ? v1 - _step[1] : 0;
    } else {
      movement[0] = _step[0] !== false ? _m0 - _step[0] : 0;
      movement[1] = _step[1] !== false ? _m1 - _step[1] : 0;
    }
    if (this.restrictToAxis && !state._blocked) this.restrictToAxis(movement);
    const previousOffset = state.offset;
    const gestureIsActive = state._active && !state._blocked || state.active;
    if (gestureIsActive) {
      state.first = state._active && !state.active;
      state.last = !state._active && state.active;
      state.active = shared[this.ingKey] = state._active;
      if (event) {
        if (state.first) {
          if ('bounds' in config) state._bounds = call(config.bounds, state);
          if (this.setup) this.setup();
        }
        state.movement = movement;
        this.computeOffset();
      }
    }
    const [ox, oy] = state.offset;
    const [[x0, x1], [y0, y1]] = state._bounds;
    state.overflow = [ox < x0 ? -1 : ox > x1 ? 1 : 0, oy < y0 ? -1 : oy > y1 ? 1 : 0];
    state._movementBound[0] = state.overflow[0] ? state._movementBound[0] === false ? state._movement[0] : state._movementBound[0] : false;
    state._movementBound[1] = state.overflow[1] ? state._movementBound[1] === false ? state._movement[1] : state._movementBound[1] : false;
    const rubberband = state._active ? config.rubberband || [0, 0] : [0, 0];
    state.offset = computeRubberband(state._bounds, state.offset, rubberband);
    state.delta = V.sub(state.offset, previousOffset);
    this.computeMovement();
    if (gestureIsActive && (!state.last || dt > BEFORE_LAST_KINEMATICS_DELAY)) {
      state.delta = V.sub(state.offset, previousOffset);
      const absoluteDelta = state.delta.map(Math.abs);
      V.addTo(state.distance, absoluteDelta);
      state.direction = state.delta.map(Math.sign);
      state._direction = state._delta.map(Math.sign);
      if (!state.first && dt > 0) {
        state.velocity = [absoluteDelta[0] / dt, absoluteDelta[1] / dt];
        state.timeDelta = dt;
      }
    }
  }
  emit() {
    const state = this.state;
    const shared = this.shared;
    const config = this.config;
    if (!state._active) this.clean();
    if ((state._blocked || !state.intentional) && !state._force && !config.triggerAllEvents) return;
    const memo = this.handler(_objectSpread2(_objectSpread2(_objectSpread2({}, shared), state), {}, {
      [this.aliasKey]: state.values
    }));
    if (memo !== undefined) state.memo = memo;
  }
  clean() {
    this.eventStore.clean();
    this.timeoutStore.clean();
  }
}

function selectAxis([dx, dy], threshold) {
  const absDx = Math.abs(dx);
  const absDy = Math.abs(dy);
  if (absDx > absDy && absDx > threshold) {
    return 'x';
  }
  if (absDy > absDx && absDy > threshold) {
    return 'y';
  }
  return undefined;
}
class CoordinatesEngine extends Engine {
  constructor(...args) {
    super(...args);
    _defineProperty(this, "aliasKey", 'xy');
  }
  reset() {
    super.reset();
    this.state.axis = undefined;
  }
  init() {
    this.state.offset = [0, 0];
    this.state.lastOffset = [0, 0];
  }
  computeOffset() {
    this.state.offset = V.add(this.state.lastOffset, this.state.movement);
  }
  computeMovement() {
    this.state.movement = V.sub(this.state.offset, this.state.lastOffset);
  }
  axisIntent(event) {
    const state = this.state;
    const config = this.config;
    if (!state.axis && event) {
      const threshold = typeof config.axisThreshold === 'object' ? config.axisThreshold[getPointerType(event)] : config.axisThreshold;
      state.axis = selectAxis(state._movement, threshold);
    }
    state._blocked = (config.lockDirection || !!config.axis) && !state.axis || !!config.axis && config.axis !== state.axis;
  }
  restrictToAxis(v) {
    if (this.config.axis || this.config.lockDirection) {
      switch (this.state.axis) {
        case 'x':
          v[1] = 0;
          break;
        case 'y':
          v[0] = 0;
          break;
      }
    }
  }
}

const identity = v => v;
const DEFAULT_RUBBERBAND = 0.15;
const commonConfigResolver = {
  enabled(value = true) {
    return value;
  },
  eventOptions(value, _k, config) {
    return _objectSpread2(_objectSpread2({}, config.shared.eventOptions), value);
  },
  preventDefault(value = false) {
    return value;
  },
  triggerAllEvents(value = false) {
    return value;
  },
  rubberband(value = 0) {
    switch (value) {
      case true:
        return [DEFAULT_RUBBERBAND, DEFAULT_RUBBERBAND];
      case false:
        return [0, 0];
      default:
        return V.toVector(value);
    }
  },
  from(value) {
    if (typeof value === 'function') return value;
    if (value != null) return V.toVector(value);
  },
  transform(value, _k, config) {
    const transform = value || config.shared.transform;
    this.hasCustomTransform = !!transform;
    return transform || identity;
  },
  threshold(value) {
    return V.toVector(value, 0);
  }
};

const DEFAULT_AXIS_THRESHOLD = 0;
const coordinatesConfigResolver = _objectSpread2(_objectSpread2({}, commonConfigResolver), {}, {
  axis(_v, _k, {
    axis
  }) {
    this.lockDirection = axis === 'lock';
    if (!this.lockDirection) return axis;
  },
  axisThreshold(value = DEFAULT_AXIS_THRESHOLD) {
    return value;
  },
  bounds(value = {}) {
    if (typeof value === 'function') {
      return state => coordinatesConfigResolver.bounds(value(state));
    }
    if ('current' in value) {
      return () => value.current;
    }
    if (typeof HTMLElement === 'function' && value instanceof HTMLElement) {
      return value;
    }
    const {
      left = -Infinity,
      right = Infinity,
      top = -Infinity,
      bottom = Infinity
    } = value;
    return [[left, right], [top, bottom]];
  }
});

const KEYS_DELTA_MAP = {
  ArrowRight: (displacement, factor = 1) => [displacement * factor, 0],
  ArrowLeft: (displacement, factor = 1) => [-1 * displacement * factor, 0],
  ArrowUp: (displacement, factor = 1) => [0, -1 * displacement * factor],
  ArrowDown: (displacement, factor = 1) => [0, displacement * factor]
};
class DragEngine extends CoordinatesEngine {
  constructor(...args) {
    super(...args);
    _defineProperty(this, "ingKey", 'dragging');
  }
  reset() {
    super.reset();
    const state = this.state;
    state._pointerId = undefined;
    state._pointerActive = false;
    state._keyboardActive = false;
    state._preventScroll = false;
    state._delayed = false;
    state.swipe = [0, 0];
    state.tap = false;
    state.canceled = false;
    state.cancel = this.cancel.bind(this);
  }
  setup() {
    const state = this.state;
    if (state._bounds instanceof HTMLElement) {
      const boundRect = state._bounds.getBoundingClientRect();
      const targetRect = state.currentTarget.getBoundingClientRect();
      const _bounds = {
        left: boundRect.left - targetRect.left + state.offset[0],
        right: boundRect.right - targetRect.right + state.offset[0],
        top: boundRect.top - targetRect.top + state.offset[1],
        bottom: boundRect.bottom - targetRect.bottom + state.offset[1]
      };
      state._bounds = coordinatesConfigResolver.bounds(_bounds);
    }
  }
  cancel() {
    const state = this.state;
    if (state.canceled) return;
    state.canceled = true;
    state._active = false;
    setTimeout(() => {
      this.compute();
      this.emit();
    }, 0);
  }
  setActive() {
    this.state._active = this.state._pointerActive || this.state._keyboardActive;
  }
  clean() {
    this.pointerClean();
    this.state._pointerActive = false;
    this.state._keyboardActive = false;
    super.clean();
  }
  pointerDown(event) {
    const config = this.config;
    const state = this.state;
    if (event.buttons != null && (Array.isArray(config.pointerButtons) ? !config.pointerButtons.includes(event.buttons) : config.pointerButtons !== -1 && config.pointerButtons !== event.buttons)) return;
    const ctrlIds = this.ctrl.setEventIds(event);
    if (config.pointerCapture) {
      event.target.setPointerCapture(event.pointerId);
    }
    if (ctrlIds && ctrlIds.size > 1 && state._pointerActive) return;
    this.start(event);
    this.setupPointer(event);
    state._pointerId = pointerId(event);
    state._pointerActive = true;
    this.computeValues(pointerValues(event));
    this.computeInitial();
    if (config.preventScrollAxis && getPointerType(event) !== 'mouse') {
      state._active = false;
      this.setupScrollPrevention(event);
    } else if (config.delay > 0) {
      this.setupDelayTrigger(event);
      if (config.triggerAllEvents) {
        this.compute(event);
        this.emit();
      }
    } else {
      this.startPointerDrag(event);
    }
  }
  startPointerDrag(event) {
    const state = this.state;
    state._active = true;
    state._preventScroll = true;
    state._delayed = false;
    this.compute(event);
    this.emit();
  }
  pointerMove(event) {
    const state = this.state;
    const config = this.config;
    if (!state._pointerActive) return;
    const id = pointerId(event);
    if (state._pointerId !== undefined && id !== state._pointerId) return;
    const _values = pointerValues(event);
    if (document.pointerLockElement === event.target) {
      state._delta = [event.movementX, event.movementY];
    } else {
      state._delta = V.sub(_values, state._values);
      this.computeValues(_values);
    }
    V.addTo(state._movement, state._delta);
    this.compute(event);
    if (state._delayed && state.intentional) {
      this.timeoutStore.remove('dragDelay');
      state.active = false;
      this.startPointerDrag(event);
      return;
    }
    if (config.preventScrollAxis && !state._preventScroll) {
      if (state.axis) {
        if (state.axis === config.preventScrollAxis || config.preventScrollAxis === 'xy') {
          state._active = false;
          this.clean();
          return;
        } else {
          this.timeoutStore.remove('startPointerDrag');
          this.startPointerDrag(event);
          return;
        }
      } else {
        return;
      }
    }
    this.emit();
  }
  pointerUp(event) {
    this.ctrl.setEventIds(event);
    try {
      if (this.config.pointerCapture && event.target.hasPointerCapture(event.pointerId)) {
        ;
        event.target.releasePointerCapture(event.pointerId);
      }
    } catch (_unused) {
    }
    const state = this.state;
    const config = this.config;
    if (!state._active || !state._pointerActive) return;
    const id = pointerId(event);
    if (state._pointerId !== undefined && id !== state._pointerId) return;
    this.state._pointerActive = false;
    this.setActive();
    this.compute(event);
    const [dx, dy] = state._distance;
    state.tap = dx <= config.tapsThreshold && dy <= config.tapsThreshold;
    if (state.tap && config.filterTaps) {
      state._force = true;
    } else {
      const [_dx, _dy] = state._delta;
      const [_mx, _my] = state._movement;
      const [svx, svy] = config.swipe.velocity;
      const [sx, sy] = config.swipe.distance;
      const sdt = config.swipe.duration;
      if (state.elapsedTime < sdt) {
        const _vx = Math.abs(_dx / state.timeDelta);
        const _vy = Math.abs(_dy / state.timeDelta);
        if (_vx > svx && Math.abs(_mx) > sx) state.swipe[0] = Math.sign(_dx);
        if (_vy > svy && Math.abs(_my) > sy) state.swipe[1] = Math.sign(_dy);
      }
    }
    this.emit();
  }
  pointerClick(event) {
    if (!this.state.tap && event.detail > 0) {
      event.preventDefault();
      event.stopPropagation();
    }
  }
  setupPointer(event) {
    const config = this.config;
    const device = config.device;
    if (config.pointerLock) {
      event.currentTarget.requestPointerLock();
    }
    if (!config.pointerCapture) {
      this.eventStore.add(this.sharedConfig.window, device, 'change', this.pointerMove.bind(this));
      this.eventStore.add(this.sharedConfig.window, device, 'end', this.pointerUp.bind(this));
      this.eventStore.add(this.sharedConfig.window, device, 'cancel', this.pointerUp.bind(this));
    }
  }
  pointerClean() {
    if (this.config.pointerLock && document.pointerLockElement === this.state.currentTarget) {
      document.exitPointerLock();
    }
  }
  preventScroll(event) {
    if (this.state._preventScroll && event.cancelable) {
      event.preventDefault();
    }
  }
  setupScrollPrevention(event) {
    this.state._preventScroll = false;
    persistEvent(event);
    const remove = this.eventStore.add(this.sharedConfig.window, 'touch', 'change', this.preventScroll.bind(this), {
      passive: false
    });
    this.eventStore.add(this.sharedConfig.window, 'touch', 'end', remove);
    this.eventStore.add(this.sharedConfig.window, 'touch', 'cancel', remove);
    this.timeoutStore.add('startPointerDrag', this.startPointerDrag.bind(this), this.config.preventScrollDelay, event);
  }
  setupDelayTrigger(event) {
    this.state._delayed = true;
    this.timeoutStore.add('dragDelay', () => {
      this.state._step = [0, 0];
      this.startPointerDrag(event);
    }, this.config.delay);
  }
  keyDown(event) {
    const deltaFn = KEYS_DELTA_MAP[event.key];
    if (deltaFn) {
      const state = this.state;
      const factor = event.shiftKey ? 10 : event.altKey ? 0.1 : 1;
      this.start(event);
      state._delta = deltaFn(this.config.keyboardDisplacement, factor);
      state._keyboardActive = true;
      V.addTo(state._movement, state._delta);
      this.compute(event);
      this.emit();
    }
  }
  keyUp(event) {
    if (!(event.key in KEYS_DELTA_MAP)) return;
    this.state._keyboardActive = false;
    this.setActive();
    this.compute(event);
    this.emit();
  }
  bind(bindFunction) {
    const device = this.config.device;
    bindFunction(device, 'start', this.pointerDown.bind(this));
    if (this.config.pointerCapture) {
      bindFunction(device, 'change', this.pointerMove.bind(this));
      bindFunction(device, 'end', this.pointerUp.bind(this));
      bindFunction(device, 'cancel', this.pointerUp.bind(this));
      bindFunction('lostPointerCapture', '', this.pointerUp.bind(this));
    }
    if (this.config.keys) {
      bindFunction('key', 'down', this.keyDown.bind(this));
      bindFunction('key', 'up', this.keyUp.bind(this));
    }
    if (this.config.filterTaps) {
      bindFunction('click', '', this.pointerClick.bind(this), {
        capture: true,
        passive: false
      });
    }
  }
}
function persistEvent(event) {
  'persist' in event && typeof event.persist === 'function' && event.persist();
}

const isBrowser = typeof window !== 'undefined' && window.document && window.document.createElement;
function supportsTouchEvents() {
  return isBrowser && 'ontouchstart' in window;
}
function isTouchScreen() {
  return supportsTouchEvents() || isBrowser && window.navigator.maxTouchPoints > 1;
}
function supportsPointerEvents() {
  return isBrowser && 'onpointerdown' in window;
}
function supportsPointerLock() {
  return isBrowser && 'exitPointerLock' in window.document;
}
function supportsGestureEvents() {
  try {
    return 'constructor' in GestureEvent;
  } catch (e) {
    return false;
  }
}
const SUPPORT = {
  isBrowser,
  gesture: supportsGestureEvents(),
  touch: supportsTouchEvents(),
  touchscreen: isTouchScreen(),
  pointer: supportsPointerEvents(),
  pointerLock: supportsPointerLock()
};

const DEFAULT_PREVENT_SCROLL_DELAY = 250;
const DEFAULT_DRAG_DELAY = 180;
const DEFAULT_SWIPE_VELOCITY = 0.5;
const DEFAULT_SWIPE_DISTANCE = 50;
const DEFAULT_SWIPE_DURATION = 250;
const DEFAULT_KEYBOARD_DISPLACEMENT = 10;
const DEFAULT_DRAG_AXIS_THRESHOLD = {
  mouse: 0,
  touch: 0,
  pen: 8
};
const dragConfigResolver = _objectSpread2(_objectSpread2({}, coordinatesConfigResolver), {}, {
  device(_v, _k, {
    pointer: {
      touch = false,
      lock = false,
      mouse = false
    } = {}
  }) {
    this.pointerLock = lock && SUPPORT.pointerLock;
    if (SUPPORT.touch && touch) return 'touch';
    if (this.pointerLock) return 'mouse';
    if (SUPPORT.pointer && !mouse) return 'pointer';
    if (SUPPORT.touch) return 'touch';
    return 'mouse';
  },
  preventScrollAxis(value, _k, {
    preventScroll
  }) {
    this.preventScrollDelay = typeof preventScroll === 'number' ? preventScroll : preventScroll || preventScroll === undefined && value ? DEFAULT_PREVENT_SCROLL_DELAY : undefined;
    if (!SUPPORT.touchscreen || preventScroll === false) return undefined;
    return value ? value : preventScroll !== undefined ? 'y' : undefined;
  },
  pointerCapture(_v, _k, {
    pointer: {
      capture = true,
      buttons = 1,
      keys = true
    } = {}
  }) {
    this.pointerButtons = buttons;
    this.keys = keys;
    return !this.pointerLock && this.device === 'pointer' && capture;
  },
  threshold(value, _k, {
    filterTaps = false,
    tapsThreshold = 3,
    axis = undefined
  }) {
    const threshold = V.toVector(value, filterTaps ? tapsThreshold : axis ? 1 : 0);
    this.filterTaps = filterTaps;
    this.tapsThreshold = tapsThreshold;
    return threshold;
  },
  swipe({
    velocity = DEFAULT_SWIPE_VELOCITY,
    distance = DEFAULT_SWIPE_DISTANCE,
    duration = DEFAULT_SWIPE_DURATION
  } = {}) {
    return {
      velocity: this.transform(V.toVector(velocity)),
      distance: this.transform(V.toVector(distance)),
      duration
    };
  },
  delay(value = 0) {
    switch (value) {
      case true:
        return DEFAULT_DRAG_DELAY;
      case false:
        return 0;
      default:
        return value;
    }
  },
  axisThreshold(value) {
    if (!value) return DEFAULT_DRAG_AXIS_THRESHOLD;
    return _objectSpread2(_objectSpread2({}, DEFAULT_DRAG_AXIS_THRESHOLD), value);
  },
  keyboardDisplacement(value = DEFAULT_KEYBOARD_DISPLACEMENT) {
    return value;
  }
});

_objectSpread2(_objectSpread2({}, commonConfigResolver), {}, {
  device(_v, _k, {
    shared,
    pointer: {
      touch = false
    } = {}
  }) {
    const sharedConfig = shared;
    if (sharedConfig.target && !SUPPORT.touch && SUPPORT.gesture) return 'gesture';
    if (SUPPORT.touch && touch) return 'touch';
    if (SUPPORT.touchscreen) {
      if (SUPPORT.pointer) return 'pointer';
      if (SUPPORT.touch) return 'touch';
    }
  },
  bounds(_v, _k, {
    scaleBounds = {},
    angleBounds = {}
  }) {
    const _scaleBounds = state => {
      const D = assignDefault(call(scaleBounds, state), {
        min: -Infinity,
        max: Infinity
      });
      return [D.min, D.max];
    };
    const _angleBounds = state => {
      const A = assignDefault(call(angleBounds, state), {
        min: -Infinity,
        max: Infinity
      });
      return [A.min, A.max];
    };
    if (typeof scaleBounds !== 'function' && typeof angleBounds !== 'function') return [_scaleBounds(), _angleBounds()];
    return state => [_scaleBounds(state), _angleBounds(state)];
  },
  threshold(value, _k, config) {
    this.lockDirection = config.axis === 'lock';
    const threshold = V.toVector(value, this.lockDirection ? [0.1, 3] : 0);
    return threshold;
  },
  modifierKey(value) {
    if (value === undefined) return 'ctrlKey';
    return value;
  },
  pinchOnWheel(value = true) {
    return value;
  }
});

_objectSpread2(_objectSpread2({}, coordinatesConfigResolver), {}, {
  mouseOnly: (value = true) => value
});

_objectSpread2(_objectSpread2({}, coordinatesConfigResolver), {}, {
  mouseOnly: (value = true) => value
});

const EngineMap = new Map();
const ConfigResolverMap = new Map();
function registerAction(action) {
  EngineMap.set(action.key, action.engine);
  ConfigResolverMap.set(action.key, action.resolver);
}
const dragAction = {
  key: 'drag',
  engine: DragEngine,
  resolver: dragConfigResolver
};

function _objectWithoutPropertiesLoose(source, excluded) {
  if (source == null) return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i;
  for (i = 0; i < sourceKeys.length; i++) {
    key = sourceKeys[i];
    if (excluded.indexOf(key) >= 0) continue;
    target[key] = source[key];
  }
  return target;
}

function _objectWithoutProperties(source, excluded) {
  if (source == null) return {};
  var target = _objectWithoutPropertiesLoose(source, excluded);
  var key, i;
  if (Object.getOwnPropertySymbols) {
    var sourceSymbolKeys = Object.getOwnPropertySymbols(source);
    for (i = 0; i < sourceSymbolKeys.length; i++) {
      key = sourceSymbolKeys[i];
      if (excluded.indexOf(key) >= 0) continue;
      if (!Object.prototype.propertyIsEnumerable.call(source, key)) continue;
      target[key] = source[key];
    }
  }
  return target;
}

const sharedConfigResolver = {
  target(value) {
    if (value) {
      return () => 'current' in value ? value.current : value;
    }
    return undefined;
  },
  enabled(value = true) {
    return value;
  },
  window(value = SUPPORT.isBrowser ? window : undefined) {
    return value;
  },
  eventOptions({
    passive = true,
    capture = false
  } = {}) {
    return {
      passive,
      capture
    };
  },
  transform(value) {
    return value;
  }
};

const _excluded = ["target", "eventOptions", "window", "enabled", "transform"];
function resolveWith(config = {}, resolvers) {
  const result = {};
  for (const [key, resolver] of Object.entries(resolvers)) {
    switch (typeof resolver) {
      case 'function':
        {
          result[key] = resolver.call(result, config[key], key, config);
        }
        break;
      case 'object':
        result[key] = resolveWith(config[key], resolver);
        break;
      case 'boolean':
        if (resolver) result[key] = config[key];
        break;
    }
  }
  return result;
}
function parse(newConfig, gestureKey, _config = {}) {
  const _ref = newConfig,
    {
      target,
      eventOptions,
      window,
      enabled,
      transform
    } = _ref,
    rest = _objectWithoutProperties(_ref, _excluded);
  _config.shared = resolveWith({
    target,
    eventOptions,
    window,
    enabled,
    transform
  }, sharedConfigResolver);
  if (gestureKey) {
    const resolver = ConfigResolverMap.get(gestureKey);
    _config[gestureKey] = resolveWith(_objectSpread2({
      shared: _config.shared
    }, rest), resolver);
  } else {
    for (const key in rest) {
      const resolver = ConfigResolverMap.get(key);
      if (resolver) {
        _config[key] = resolveWith(_objectSpread2({
          shared: _config.shared
        }, rest[key]), resolver);
      }
    }
  }
  return _config;
}

class EventStore {
  constructor(ctrl, gestureKey) {
    _defineProperty(this, "_listeners", new Set());
    this._ctrl = ctrl;
    this._gestureKey = gestureKey;
  }
  add(element, device, action, handler, options) {
    const listeners = this._listeners;
    const type = toDomEventType(device, action);
    const _options = this._gestureKey ? this._ctrl.config[this._gestureKey].eventOptions : {};
    const eventOptions = _objectSpread2(_objectSpread2({}, _options), options);
    element.addEventListener(type, handler, eventOptions);
    const remove = () => {
      element.removeEventListener(type, handler, eventOptions);
      listeners.delete(remove);
    };
    listeners.add(remove);
    return remove;
  }
  clean() {
    this._listeners.forEach(remove => remove());
    this._listeners.clear();
  }
}

class TimeoutStore {
  constructor() {
    _defineProperty(this, "_timeouts", new Map());
  }
  add(key, callback, ms = 140, ...args) {
    this.remove(key);
    this._timeouts.set(key, window.setTimeout(callback, ms, ...args));
  }
  remove(key) {
    const timeout = this._timeouts.get(key);
    if (timeout) window.clearTimeout(timeout);
  }
  clean() {
    this._timeouts.forEach(timeout => void window.clearTimeout(timeout));
    this._timeouts.clear();
  }
}

class Controller {
  constructor(handlers) {
    _defineProperty(this, "gestures", new Set());
    _defineProperty(this, "_targetEventStore", new EventStore(this));
    _defineProperty(this, "gestureEventStores", {});
    _defineProperty(this, "gestureTimeoutStores", {});
    _defineProperty(this, "handlers", {});
    _defineProperty(this, "config", {});
    _defineProperty(this, "pointerIds", new Set());
    _defineProperty(this, "touchIds", new Set());
    _defineProperty(this, "state", {
      shared: {
        shiftKey: false,
        metaKey: false,
        ctrlKey: false,
        altKey: false
      }
    });
    resolveGestures(this, handlers);
  }
  setEventIds(event) {
    if (isTouch(event)) {
      this.touchIds = new Set(touchIds(event));
      return this.touchIds;
    } else if ('pointerId' in event) {
      if (event.type === 'pointerup' || event.type === 'pointercancel') this.pointerIds.delete(event.pointerId);else if (event.type === 'pointerdown') this.pointerIds.add(event.pointerId);
      return this.pointerIds;
    }
  }
  applyHandlers(handlers, nativeHandlers) {
    this.handlers = handlers;
    this.nativeHandlers = nativeHandlers;
  }
  applyConfig(config, gestureKey) {
    this.config = parse(config, gestureKey, this.config);
  }
  clean() {
    this._targetEventStore.clean();
    for (const key of this.gestures) {
      this.gestureEventStores[key].clean();
      this.gestureTimeoutStores[key].clean();
    }
  }
  effect() {
    if (this.config.shared.target) this.bind();
    return () => this._targetEventStore.clean();
  }
  bind(...args) {
    const sharedConfig = this.config.shared;
    const props = {};
    let target;
    if (sharedConfig.target) {
      target = sharedConfig.target();
      if (!target) return;
    }
    if (sharedConfig.enabled) {
      for (const gestureKey of this.gestures) {
        const gestureConfig = this.config[gestureKey];
        const bindFunction = bindToProps(props, gestureConfig.eventOptions, !!target);
        if (gestureConfig.enabled) {
          const Engine = EngineMap.get(gestureKey);
          new Engine(this, args, gestureKey).bind(bindFunction);
        }
      }
      const nativeBindFunction = bindToProps(props, sharedConfig.eventOptions, !!target);
      for (const eventKey in this.nativeHandlers) {
        nativeBindFunction(eventKey, '', event => this.nativeHandlers[eventKey](_objectSpread2(_objectSpread2({}, this.state.shared), {}, {
          event,
          args
        })), undefined, true);
      }
    }
    for (const handlerProp in props) {
      props[handlerProp] = chain(...props[handlerProp]);
    }
    if (!target) return props;
    for (const handlerProp in props) {
      const {
        device,
        capture,
        passive
      } = parseProp(handlerProp);
      this._targetEventStore.add(target, device, '', props[handlerProp], {
        capture,
        passive
      });
    }
  }
}
function setupGesture(ctrl, gestureKey) {
  ctrl.gestures.add(gestureKey);
  ctrl.gestureEventStores[gestureKey] = new EventStore(ctrl, gestureKey);
  ctrl.gestureTimeoutStores[gestureKey] = new TimeoutStore();
}
function resolveGestures(ctrl, internalHandlers) {
  if (internalHandlers.drag) setupGesture(ctrl, 'drag');
  if (internalHandlers.wheel) setupGesture(ctrl, 'wheel');
  if (internalHandlers.scroll) setupGesture(ctrl, 'scroll');
  if (internalHandlers.move) setupGesture(ctrl, 'move');
  if (internalHandlers.pinch) setupGesture(ctrl, 'pinch');
  if (internalHandlers.hover) setupGesture(ctrl, 'hover');
}
const bindToProps = (props, eventOptions, withPassiveOption) => (device, action, handler, options = {}, isNative = false) => {
  var _options$capture, _options$passive;
  const capture = (_options$capture = options.capture) !== null && _options$capture !== void 0 ? _options$capture : eventOptions.capture;
  const passive = (_options$passive = options.passive) !== null && _options$passive !== void 0 ? _options$passive : eventOptions.passive;
  let handlerProp = isNative ? device : toHandlerProp(device, action, capture);
  if (withPassiveOption && passive) handlerProp += 'Passive';
  props[handlerProp] = props[handlerProp] || [];
  props[handlerProp].push(handler);
};

function useRecognizers(handlers, config = {}, gestureKey, nativeHandlers) {
  const ctrl = React.useMemo(() => new Controller(handlers), []);
  ctrl.applyHandlers(handlers, nativeHandlers);
  ctrl.applyConfig(config, gestureKey);
  React.useEffect(ctrl.effect.bind(ctrl));
  React.useEffect(() => {
    return ctrl.clean.bind(ctrl);
  }, []);
  if (config.target === undefined) {
    return ctrl.bind.bind(ctrl);
  }
  return undefined;
}

function useDrag(handler, config) {
  registerAction(dragAction);
  return useRecognizers({
    drag: handler
  }, config || {}, 'drag');
}

var commonjsGlobal = typeof globalThis !== 'undefined' ? globalThis : typeof window !== 'undefined' ? window : typeof global !== 'undefined' ? global : typeof self !== 'undefined' ? self : {};

function getDefaultExportFromCjs (x) {
	return x && x.__esModule && Object.prototype.hasOwnProperty.call(x, 'default') ? x['default'] : x;
}

var __assign = (commonjsGlobal && commonjsGlobal.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
function pushProp(target, key, value) {
    if (key === 'className') {
        target.className = [target.className, value].join(' ').trim();
    }
    else if (key === 'style') {
        target.style = __assign(__assign({}, target.style), value);
    }
    else if (typeof value === 'function') {
        var oldFn_1 = target[key];
        target[key] = oldFn_1 ? function () {
            var args = [];
            for (var _i = 0; _i < arguments.length; _i++) {
                args[_i] = arguments[_i];
            }
            oldFn_1.apply(void 0, args);
            value.apply(void 0, args);
        } : value;
    }
    else if (
    // skip merging undefined values
    value === undefined ||
        // skip if both value are the same primitive value
        (typeof value !== 'object' && value === target[key])) {
        return;
    }
    else if (!(key in target)) {
        target[key] = value;
    }
    else {
        throw new Error("Didn\u2019t know how to merge prop '".concat(key, "'. ") +
            "Only 'className', 'style', and event handlers are supported");
    }
}
var dist = function mergeProps() {
    var props = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        props[_i] = arguments[_i];
    }
    if (props.length === 1) {
        return props[0];
    }
    return props.reduce(function (merged, ps) {
        for (var key in ps) {
            pushProp(merged, key, ps[key]);
        }
        return merged;
    }, {});
};


var q$1 = /*@__PURE__*/getDefaultExportFromCjs(dist);

var n$1=(r,m,e)=>Math.max(m,Math.min(e,r)),o=r=>n$1(r,0,1);var a=(r,m,e)=>(e-m)*r+m,b=(r,m,e)=>(r-m)/(e-m);

var jsxRuntime = {exports: {}};

var reactJsxRuntime_production_min = {};

/**
 * @license React
 * react-jsx-runtime.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var f$1=React,k=Symbol.for("react.element"),l=Symbol.for("react.fragment"),m=Object.prototype.hasOwnProperty,n=f$1.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentOwner,p$1={key:!0,ref:!0,__self:!0,__source:!0};
function q(c,a,g){var b,d={},e=null,h=null;void 0!==g&&(e=""+g);void 0!==a.key&&(e=""+a.key);void 0!==a.ref&&(h=a.ref);for(b in a)m.call(a,b)&&!p$1.hasOwnProperty(b)&&(d[b]=a[b]);if(c&&c.defaultProps)for(b in a=c.defaultProps,a)void 0===d[b]&&(d[b]=a[b]);return {$$typeof:k,type:c,key:e,ref:h,props:d,_owner:n.current}}reactJsxRuntime_production_min.Fragment=l;reactJsxRuntime_production_min.jsx=q;reactJsxRuntime_production_min.jsxs=q;

{
  jsxRuntime.exports = reactJsxRuntime_production_min;
}

var jsxRuntimeExports = jsxRuntime.exports;

var C=Object.defineProperty;var p=Object.getOwnPropertySymbols;var P=Object.prototype.hasOwnProperty,H=Object.prototype.propertyIsEnumerable;var x=(e,r,o)=>r in e?C(e,r,{enumerable:!0,configurable:!0,writable:!0,value:o}):e[r]=o,s=(e,r)=>{for(var o in r||(r={}))P.call(r,o)&&x(e,o,r[o]);if(p)for(var o of p(r))H.call(r,o)&&x(e,o,r[o]);return e};var v=(e,r)=>{var o={};for(var n in e)P.call(e,n)&&r.indexOf(n)<0&&(o[n]=e[n]);if(e!=null&&p)for(var n of p(e))r.indexOf(n)<0&&H.call(e,n)&&(o[n]=e[n]);return o};var w="y",L=!1,D=b,T=a,f=forwardRef((M,A)=>{var K=M,{valueRaw:e,valueMin:r,valueMax:o$1,dragSensitivity:n,valueRawRoundFn:i,valueRawDisplayFn:t,onValueRawChange:l,orientation:u,axis:a=w,includeIntoTabOrder:y=L,mapTo01:N=D,mapFrom01:R=T}=K,g=v(K,["valueRaw","valueMin","valueMax","dragSensitivity","valueRawRoundFn","valueRawDisplayFn","onValueRawChange","orientation","axis","includeIntoTabOrder","mapTo01","mapFrom01"]);let k=i(e),F=useDrag(({delta:b})=>{let c=0;c+=b[0]*n,c+=b[1]*-n;let h=N(e,r,o$1),z=o(h+c),I=n$1(R(z,r,o$1),r,o$1);l(I);},{pointer:{keys:!1},axis:S(u,a)});return jsxRuntimeExports.jsx("div",s({ref:A,role:"slider","aria-valuenow":k,"aria-valuemin":r,"aria-valuemax":o$1,"aria-orientation":W(u,a),"aria-valuetext":t(e),tabIndex:y?0:-1},q$1(F(),{style:{touchAction:"none"},onPointerDown(b){b.currentTarget.focus();}},g)))});f.displayName="KnobHeadless";f.defaultProps={axis:w,includeIntoTabOrder:L,mapTo01:D,mapFrom01:T};var S=(e,r)=>e?e==="horizontal"?"x":"y":r==="xy"?void 0:r,W=(e,r)=>{if(e)return e;if(r!=="xy")return r==="x"?"horizontal":"vertical"};var O=forwardRef((e,r)=>jsxRuntimeExports.jsx("label",s({ref:r},e)));O.displayName="KnobHeadlessLabel";var E=forwardRef((e,r)=>jsxRuntimeExports.jsx("output",s({ref:r},e)));E.displayName="KnobHeadlessOutput";

const KnobInner = ({ value, size }) => {
    // Angle calculation: value 0 = -135°, value 0.5 = 0° (top), value 1 = +135°
    const angleDeg = value * 270 - 135;
    // Convert to radians, offset by -90 so 0° is at top
    const angleRad = (angleDeg - 90) * (Math.PI / 180);
    // Sweep angle from start (0) to current value position (rounded to reduce jitter)
    const sweepDeg = Math.round(value * 270 * 10) / 10;
    const outerRadius = size / 2;
    const innerRadius = outerRadius * 0.5; // Thinner arc, larger inner circle
    const arcCenterRadius = (outerRadius + innerRadius) / 2;
    // Handle dimensions - positioned in the middle of the arc ring
    const handleWidth = Math.round(size * 0.12);
    const handleHeight = Math.round((outerRadius - innerRadius) * 1.2);
    // Calculate handle offset from center (use transform for smoother animation)
    const handleOffsetX = arcCenterRadius * Math.cos(angleRad);
    const handleOffsetY = arcCenterRadius * Math.sin(angleRad);
    // Build dynamic gradient that only extends to the handle position
    const buildGradient = () => {
        if (sweepDeg <= 0)
            return 'transparent';
        // Create gradient stops proportionally within the sweep range
        const stops = [
            { pos: 0, color: 'rgba(30, 30, 30, 0.3)' }, // was 0.5
            { pos: 0.2, color: 'rgba(50, 50, 50, 0.6)' },
            { pos: 0.4, color: 'rgba(100, 100, 100, 0.75)' },
            { pos: 0.6, color: 'rgba(160, 160, 160, 0.85)' },
            { pos: 0.8, color: 'rgba(220, 220, 220, 0.9)' },
            { pos: 1, color: 'rgba(255, 255, 255, 1)' },
        ];
        const gradientStops = stops
            .map(s => `${s.color} ${s.pos * sweepDeg}deg`)
            .join(', ');
        return `conic-gradient(from 225deg, ${gradientStops}, transparent ${sweepDeg}deg)`;
    };
    return (React.createElement("div", { style: {
            width: size,
            height: size,
            position: 'relative',
            borderRadius: '50%',
            pointerEvents: 'none',
        } },
        React.createElement("div", { style: {
                position: 'absolute',
                width: '100%',
                height: '100%',
                borderRadius: '50%',
                background: `conic-gradient(from 225deg, rgba(50, 50, 50, 0.8) 0deg, rgba(50, 50, 50, 0.1) 270deg, transparent 270deg)`,
            } }),
        React.createElement("div", { style: {
                position: 'absolute',
                width: '100%',
                height: '100%',
                borderRadius: '50%',
                background: buildGradient(),
                willChange: 'background',
                backfaceVisibility: 'hidden',
            } }),
        React.createElement("div", { style: {
                position: 'absolute',
                top: '50%',
                left: '50%',
                transform: 'translate(-50%, -50%)',
                width: innerRadius * 2,
                height: innerRadius * 2,
                borderRadius: '50%',
                backgroundColor: '#1C1C1E',
            } }),
        React.createElement("div", { style: {
                position: 'absolute',
                left: '50%',
                top: '50%',
                width: handleWidth,
                height: handleHeight,
                backgroundColor: '#ffffff',
                borderLeft: '2px solid #000000',
                transform: `translate(-50%, -50%) translate(${handleOffsetX}px, ${handleOffsetY}px) rotate(${angleDeg}deg)`,
                transformOrigin: 'center center',
                willChange: 'transform',
                backfaceVisibility: 'hidden',
            } })));
};

const KnobControl = ({ label, value, onChange, size = 64, labelSize = 14, labelBottom = true, isDisabled = false, }) => {
    // const angleDeg = value * 270 - 135; // -135..+135
    const knobRef = useRef(null);
    useEffect(() => {
        const knobElement = knobRef.current;
        if (!knobElement)
            return;
        const preventSelection = (e) => {
            e.preventDefault();
            return false;
        };
        const handleMouseDown = () => {
            // Prevent text selection during drag
            const bodyStyle = document.body.style;
            bodyStyle.userSelect = 'none';
            bodyStyle.webkitUserSelect = 'none';
            bodyStyle.mozUserSelect = 'none';
            bodyStyle.msUserSelect = 'none';
            // Add class for CSS targeting
            document.body.classList.add('dragging');
        };
        const handleMouseUp = () => {
            // Restore text selection
            const bodyStyle = document.body.style;
            bodyStyle.userSelect = '';
            bodyStyle.webkitUserSelect = '';
            bodyStyle.mozUserSelect = '';
            bodyStyle.msUserSelect = '';
            // Remove class
            document.body.classList.remove('dragging');
        };
        // Add event listeners
        knobElement.addEventListener('selectstart', preventSelection);
        knobElement.addEventListener('dragstart', preventSelection);
        knobElement.addEventListener('mousedown', handleMouseDown);
        document.addEventListener('mouseup', handleMouseUp);
        return () => {
            // Cleanup
            knobElement.removeEventListener('selectstart', preventSelection);
            knobElement.removeEventListener('dragstart', preventSelection);
            knobElement.removeEventListener('mousedown', handleMouseDown);
            document.removeEventListener('mouseup', handleMouseUp);
            // Ensure body styles are reset
            const bodyStyle = document.body.style;
            bodyStyle.userSelect = '';
            bodyStyle.webkitUserSelect = '';
            bodyStyle.mozUserSelect = '';
            bodyStyle.msUserSelect = '';
            document.body.classList.remove('dragging');
        };
    }, []);
    return (React.createElement("div", { style: {
            display: 'flex',
            flexDirection: labelBottom ? 'column' : 'column-reverse',
            justifyContent: 'center',
            alignItems: 'center',
            gap: labelSize === 14 ? '14px' : '10px',
        } },
        React.createElement(f, { ref: knobRef, "aria-label": label, valueRaw: value, valueMin: 0, valueMax: 1, dragSensitivity: 0.006, valueRawRoundFn: x => Math.round(x * 100) / 100, valueRawDisplayFn: x => `${x.toFixed(2)}`, onValueRawChange: onChange, className: 'knob', style: {
                width: size,
                height: size,
                position: 'relative',
                userSelect: 'none',
                outline: 'none',
                boxShadow: 'none',
                WebkitTapHighlightColor: 'transparent',
                cursor: isDisabled ? 'not-allowed' : 'pointer',
                pointerEvents: isDisabled ? 'none' : 'auto',
            } },
            React.createElement(KnobInner, { value: value, size: size })),
        React.createElement("span", { style: {
                fontSize: labelSize,
                fontWeight: 400,
                textAlign: 'center',
                color: '#ffffff',
            } }, label)));
};

const BLOCK_SIZE = 6;
const BLOCK_GAP = 3;
const INACTIVE_COLOR = '#3f3f46'; /* zinc-700 */
// Responsive block count by container width
function getBlockCountForWidth(width) {
    if (width >= 360)
        return 40;
    if (width >= 240)
        return 20;
    if (width >= 120)
        return 12;
    return 8;
}
// Interpolate color along gradient: blue (left) → yellow (middle) → red (right)
const getGradientColor = (position) => {
    let r, g, b;
    if (position <= 0.5) {
        const t = position * 2;
        r = Math.round(255 * t);
        g = Math.round(255 * t);
        b = Math.round(255 * (1 - t));
    }
    else {
        const t = (position - 0.5) * 2;
        r = 255;
        g = Math.round(255 * (1 - t));
        b = 0;
    }
    return `rgb(${r}, ${g}, ${b})`;
};
// Grayscale gradient for inactive state: dark (left) → light (right)
const getGrayscaleColor = (position) => {
    const v = Math.round(90 + position * 100); // ~90 to ~190
    return `rgb(${v}, ${v}, ${v})`;
};
/**
 * A horizontal block-style audio level meter.
 *
 * Like DbMeter but horizontal: each block is active or inactive based on level.
 * Level 1 = full bar (all blocks active), level 0 = empty (all blocks inactive).
 *
 * Usage:
 * ```tsx
 * <InputMeter analyser={nodes.inputMeterNode} inactive={!isReady} />
 * ```
 */
const InputMeter = ({ analyser, blockCount: blockCountProp, className = '', label = 'Input level', inactive = false, }) => {
    const [level, setLevel] = useState(0);
    const [blockCount, setBlockCount] = useState(40);
    const containerRef = useRef(null);
    const animationFrameRef = useRef(undefined);
    const isRunningRef = useRef(false);
    const bufferRef = useRef(new Float32Array(2048));
    // Responsive block count from container width
    useEffect(() => {
        const el = containerRef.current;
        if (!el || blockCountProp !== undefined)
            return;
        const updateBlockCount = (width) => setBlockCount(getBlockCountForWidth(width));
        updateBlockCount(el.getBoundingClientRect().width);
        const ro = new ResizeObserver(entries => {
            const { width } = entries[0]?.contentRect ?? { width: 0 };
            updateBlockCount(width);
        });
        ro.observe(el);
        return () => ro.disconnect();
    }, [blockCountProp]);
    const effectiveBlockCount = blockCountProp ?? blockCount;
    // Convert level (0-1) to last active block index; index <= activeBlockIndex means active
    // Use pseudo-log display so blocks fill left-to-right in a log-like manner
    const activeBlockIndex = useMemo(() => {
        const display = levelToLogDisplay(level);
        if (display <= 0)
            return -1;
        const idx = Math.floor(display * effectiveBlockCount) - 1;
        return Math.max(-1, Math.min(effectiveBlockCount - 1, idx));
    }, [level, effectiveBlockCount]);
    useEffect(() => {
        if (!analyser) {
            setLevel(0);
            return;
        }
        if (isRunningRef.current)
            return;
        isRunningRef.current = true;
        const updateMeter = () => {
            if (!isRunningRef.current)
                return;
            try {
                const levels = calculateLevels(analyser, bufferRef.current);
                // Use peak so block fill matches clip indicator (both use peak; RMS would show ~half when clipping)
                setLevel(clamp$1(levels.peak, 0, 1));
            }
            catch {
                // Silently handle errors
            }
            if (isRunningRef.current) {
                animationFrameRef.current = requestAnimationFrame(updateMeter);
            }
        };
        updateMeter();
        return () => {
            isRunningRef.current = false;
            if (animationFrameRef.current !== undefined) {
                cancelAnimationFrame(animationFrameRef.current);
                animationFrameRef.current = undefined;
            }
        };
    }, [analyser]);
    const blocks = useMemo(() => {
        return Array.from({ length: effectiveBlockCount }, (_, i) => i);
    }, [effectiveBlockCount]);
    return (React.createElement("div", { ref: containerRef, className: `flex items-center flex-1 min-w-0 ${className}`, role: 'meter', "aria-label": label, "aria-valuemin": 0, "aria-valuemax": 100, "aria-valuenow": Math.round(level * 100) },
        React.createElement("div", { className: 'flex flex-1 min-w-0', style: { gap: BLOCK_GAP } }, blocks.map(index => {
            const isActive = index <= activeBlockIndex;
            const position = effectiveBlockCount > 1 ? index / (effectiveBlockCount - 1) : 0;
            const color = isActive
                ? inactive
                    ? getGrayscaleColor(position)
                    : getGradientColor(position)
                : INACTIVE_COLOR;
            return (React.createElement("div", { key: index, style: {
                    width: BLOCK_SIZE,
                    height: BLOCK_SIZE,
                    minWidth: BLOCK_SIZE,
                    flex: 1,
                    backgroundColor: color,
                    flexShrink: 0,
                } }));
        }))));
};
InputMeter.displayName = 'InputMeter';

/**
 * A clip indicator that latches red when clipping is detected.
 *
 * The indicator turns red when the 'clipped' class is added (by useMeterAnimation).
 * Clicking it resets the latch.
 *
 * Usage:
 * ```tsx
 * const clipRef = useRef<HTMLButtonElement>(null);
 * const { resetClipLatch } = useMeterAnimation(
 *   { analyser, meterRef, clipRef },
 *   null,
 *   true
 * );
 * <ClipIndicator ref={clipRef} onClick={() => resetClipLatch('input')} />
 * ```
 */
const ClipIndicator = forwardRef(({ onClick, size = 24, className = '', label = 'Clip indicator - click to reset', inactive = false, }, ref) => {
    return (React.createElement("button", { ref: ref, type: 'button', onClick: onClick, className: `
          rounded-sm
          bg-zinc-700
          focus:outline-none
          [&.clipped]:bg-[#f00]
          ${className}
          ${inactive ? 'grayscale cursor-not-allowed' : 'cursor-pointer'}
        `, style: {
            width: size,
            height: size,
            minWidth: size,
            minHeight: size,
        }, "aria-label": label, "aria-pressed": false, title: 'Click to reset clip indicator' }));
});
ClipIndicator.displayName = 'ClipIndicator';

/**
 * Async iterator for requestAnimationFrame loop
 * Allows clean cancellation via break or return
 */
const rafIter = () => {
    let id;
    let cancelled = false;
    return {
        async next() {
            if (cancelled) {
                return { value: undefined, done: true };
            }
            const promise = new Promise(resolve => {
                id = requestAnimationFrame(resolve);
            });
            await promise;
            return { value: undefined, done: false };
        },
        async return() {
            cancelled = true;
            cancelAnimationFrame(id);
            return { value: undefined, done: true };
        },
        [Symbol.asyncIterator]() {
            return this;
        },
    };
};
/**
 * Hook for animating audio level meters at 60fps
 *
 * Uses direct DOM manipulation via CSS custom properties for performance.
 * Does NOT trigger React re-renders.
 *
 * @param inputConfig - Configuration for input meter
 * @param outputConfig - Configuration for output meter
 * @param enabled - Whether animation is running (default: true)
 * @returns Object with resetClipLatch function and current levels ref
 */
function useMeterAnimation(inputConfig, outputConfig, enabled = true) {
    // Reusable buffer for reading analyser data (must match fftSize of AnalyserNodes)
    const bufferRef = useRef(new Float32Array(2048));
    // Track current levels (for external access if needed)
    const levelsRef = useRef({
        input: { peak: 0, rms: 0 },
        output: { peak: 0, rms: 0 },
    });
    // Track clip latch state
    const clipStateRef = useRef({
        inputClipLatched: false,
        outputClipLatched: false,
    });
    // Animation loop cleanup ref
    const cleanupRef = useRef(null);
    // Reset clip latch indicators
    const resetClipLatch = useCallback((which = 'all') => {
        if (which === 'input' || which === 'all') {
            clipStateRef.current.inputClipLatched = false;
            inputConfig?.clipRef?.current?.classList.remove('clipped');
        }
        if (which === 'output' || which === 'all') {
            clipStateRef.current.outputClipLatched = false;
            outputConfig?.clipRef?.current?.classList.remove('clipped');
        }
    }, [inputConfig?.clipRef, outputConfig?.clipRef]);
    useEffect(() => {
        // Clean up any existing animation loop
        cleanupRef.current?.();
        cleanupRef.current = null;
        // Don't start if disabled or no analysers
        if (!enabled)
            return;
        if (!inputConfig?.analyser && !outputConfig?.analyser)
            return;
        const buffer = bufferRef.current;
        let running = true;
        // Start animation loop
        (async () => {
            for await (const _ of rafIter()) {
                if (!running)
                    break;
                // Update input meter
                if (inputConfig?.analyser && inputConfig.meterRef.current) {
                    const levels = calculateLevels(inputConfig.analyser, buffer);
                    levelsRef.current.input = levels;
                    // Update meter display (using scaled value for visual response)
                    const displayLevel = scaleForDisplay(levels.rms);
                    inputConfig.meterRef.current.style.setProperty('--level', String(displayLevel));
                    // Check for clipping
                    if (isClipping(levels)) {
                        clipStateRef.current.inputClipLatched = true;
                        inputConfig.clipRef?.current?.classList.add('clipped');
                    }
                }
                // Update output meter
                if (outputConfig?.analyser && outputConfig.meterRef.current) {
                    const levels = calculateLevels(outputConfig.analyser, buffer);
                    levelsRef.current.output = levels;
                    // Update meter display
                    const displayLevel = scaleForDisplay(levels.rms);
                    outputConfig.meterRef.current.style.setProperty('--level', String(displayLevel));
                    // Check for clipping
                    if (isClipping(levels)) {
                        clipStateRef.current.outputClipLatched = true;
                        outputConfig.clipRef?.current?.classList.add('clipped');
                    }
                }
            }
        })();
        // Cleanup function
        cleanupRef.current = () => {
            running = false;
        };
        return () => {
            running = false;
            cleanupRef.current = null;
        };
    }, [
        enabled,
        inputConfig?.analyser,
        inputConfig?.meterRef,
        inputConfig?.clipRef,
        outputConfig?.analyser,
        outputConfig?.meterRef,
        outputConfig?.clipRef,
    ]);
    return {
        /** Reset clip latch indicator(s) */
        resetClipLatch,
        /** Current meter levels (updated every frame, not reactive) */
        levelsRef,
        /** Current clip state (updated every frame, not reactive) */
        clipStateRef,
    };
}

const DB_MIN = -12;
const DB_MAX = 12;
/** Map dB (-12..+12) to knob value (0..1) */
function dbToKnob(db) {
    return Math.max(0, Math.min(1, (db - DB_MIN) / (DB_MAX - DB_MIN)));
}
/** Map knob value (0..1) to dB (-12..+12) */
function knobToDb(knob) {
    return knob * (DB_MAX - DB_MIN) + DB_MIN;
}
const InputControls = ({ isMonitoring, isConnecting, }) => {
    const { getAudioNodes, audioState, setLiveInputGain } = useT3kPlayerContext();
    const nodes = getAudioNodes();
    const isReady = audioState.initState === 'ready';
    const liveInputConfig = audioState.liveInputConfig;
    const currentChannel = liveInputConfig?.selectedChannel ?? 'first';
    const inputGainDb = liveInputConfig?.channelGains?.[currentChannel] ?? 0;
    const inputMeterRef = useRef(null);
    const inputClipRef = useRef(null);
    const isDisabledOrConnecting = !isReady || !isMonitoring || isConnecting;
    const { resetClipLatch } = useMeterAnimation(isReady && nodes.inputMeterNode
        ? {
            analyser: nodes.inputMeterNode,
            meterRef: inputMeterRef,
            clipRef: inputClipRef,
        }
        : null, null, isReady);
    useEffect(() => {
        resetClipLatch('input');
        inputClipRef.current?.classList.remove('clipped');
    }, [liveInputConfig?.selectedChannel, resetClipLatch]);
    const handleKnobChange = useCallback((knobValue) => setLiveInputGain(knobToDb(knobValue)), [setLiveInputGain]);
    return (React.createElement("div", { className: 'flex flex-row items-center gap-4 border py-3 px-4 border-zinc-700 rounded-md w-full' },
        React.createElement("div", { className: `flex flex-col gap-4 items-center ${isDisabledOrConnecting ? 'opacity-50 cursor-not-allowed' : ''}` },
            React.createElement("span", { className: 'text-xs text-zinc-400 text-center tabular-nums' },
                inputGainDb >= 0 ? '+' : '',
                inputGainDb.toFixed(1),
                " dB"),
            React.createElement(KnobControl, { label: 'Input Gain', value: dbToKnob(inputGainDb), onChange: handleKnobChange, size: 36, isDisabled: isDisabledOrConnecting })),
        React.createElement("div", { ref: inputMeterRef, className: 'flex items-center gap-4 flex-1 min-w-0' },
            React.createElement(InputMeter, { analyser: nodes.inputMeterNode, label: 'Input level', inactive: isDisabledOrConnecting }),
            React.createElement(ClipIndicator, { ref: inputClipRef, onClick: () => resetClipLatch('input'), size: 16, inactive: isDisabledOrConnecting }))));
};

const variantStyles = {
    error: {
        container: '',
        icon: 'text-[#F00]',
        title: 'text-[#F00]',
        description: 'text-[#F00]',
    },
    warning: {
        container: '',
        icon: 'text-[#F00]',
        title: 'text-[#F00]',
        description: 'text-[#F00]',
    },
    info: {
        container: '',
        icon: 'text-zinc-400',
        title: 'text-zinc-400',
        description: 'text-zinc-400',
    },
};
const AlertIcon = ({ variant, className, }) => {
    if (variant === 'warning') {
        return React.createElement(CircleAlert, { size: 20, className: className });
    }
    return React.createElement(TriangleAlert, { size: 20, className: className });
};
const Alert = ({ variant, children }) => {
    const styles = variantStyles[variant];
    return (React.createElement("div", { className: `flex items-start gap-3 ${styles.container}` },
        React.createElement(AlertIcon, { variant: variant, className: `${styles.icon} flex-shrink-0` }),
        React.createElement("div", { className: `flex flex-col gap-1 text-sm ${styles.title}` }, children)));
};

const PlayerSettings = ({ previewMode, disabled, bypassed, bypassedStyles, onBypassToggle, sourceMode, onOpenSettings, modelOptions, irOptions, audioOptions, selectedModelUrl, selectedIrUrl, selectedInputUrl, onModelChange, onIrChange, onInputChange, isLiveConfigured, currentDeviceId, liveDeviceOptions, inputModeType, audioInputError, isThisPlayerActive, }) => {
    const isMonitoring = isThisPlayerActive && sourceMode === 'live';
    const isConnecting = inputModeType === 'connecting';
    const renderModelSelect = () => (React.createElement(Select, { options: modelOptions, label: 'Model', onChange: onModelChange, value: selectedModelUrl, disabled: bypassed }));
    const renderIrSelect = () => (React.createElement(Select, { options: irOptions, label: 'IR', onChange: onIrChange, value: selectedIrUrl, disabled: bypassed }));
    if (sourceMode === 'live' && !isLiveConfigured && !isConnecting) {
        return React.createElement(LiveTakeover, { onConnect: onOpenSettings });
    }
    return (React.createElement("div", { className: 'flex flex-col gap-4' },
        React.createElement("div", { className: 'flex flex-row items-center gap-4 flex-wrap' },
            React.createElement("div", { className: 'flex-1 min-w-[0px]' },
                React.createElement("div", { className: `flex w-full flex-1 min-w-[0px] ${bypassedStyles}` }, previewMode === PREVIEW_MODE.MODEL
                    ? renderModelSelect()
                    : renderIrSelect())),
            React.createElement("div", { className: 'flex items-center pt-[24px] flex-shrink-0' },
                React.createElement(ToggleSimple, { label: '', onChange: onBypassToggle, isChecked: !bypassed, ariaLabel: 'Bypass', disabled: disabled }))),
        React.createElement("div", { className: `w-full sm:flex-1 min-w-0 ${bypassedStyles}` }, previewMode === PREVIEW_MODE.MODEL
            ? renderIrSelect()
            : renderModelSelect()),
        React.createElement("div", { className: 'w-full sm:flex-1 min-w-0' },
            sourceMode === 'demo' && (React.createElement(Select, { options: audioOptions, label: 'Input', onChange: onInputChange, value: selectedInputUrl })),
            sourceMode === 'live' && isLiveConfigured && (React.createElement("div", { className: 'w-full' },
                React.createElement("div", { className: 'flex flex-col gap-1 w-full' },
                    React.createElement("div", { className: 'flex items-center justify-between w-full' },
                        React.createElement("span", { className: 'text-sm text-zinc-400' }, isConnecting
                            ? 'Connecting...'
                            : (liveDeviceOptions?.find(option => option.value === currentDeviceId)?.label ?? 'No device selected')),
                        React.createElement("button", { type: 'button', className: 'text-white', onClick: onOpenSettings },
                            React.createElement(Settings, { size: 18 }))),
                    React.createElement(InputControls, { isMonitoring: isMonitoring, isConnecting: isConnecting }),
                    audioInputError && (React.createElement(Alert, { variant: 'error' }, audioInputError))))))));
};

const Demo = ({ size = 20 }) => (React.createElement("svg", { width: size, height: size, viewBox: '0 0 20 20', fill: 'none', xmlns: 'http://www.w3.org/2000/svg' },
    React.createElement("path", { d: 'M4.16663 4.16667C4.16654 3.87342 4.24383 3.58533 4.3907 3.3315C4.53756 3.07767 4.7488 2.86708 5.00309 2.721C5.25737 2.57492 5.5457 2.49853 5.83895 2.49952C6.13221 2.50052 6.42001 2.57887 6.67329 2.72667L16.6708 8.55834C16.9231 8.70474 17.1326 8.91479 17.2782 9.16751C17.4239 9.42023 17.5007 9.70675 17.501 9.99845C17.5012 10.2902 17.4249 10.5768 17.2797 10.8298C17.1344 11.0828 16.9253 11.2932 16.6733 11.44L6.67329 17.2733C6.42001 17.4211 6.13221 17.4995 5.83895 17.5005C5.5457 17.5015 5.25737 17.4251 5.00309 17.279C4.7488 17.1329 4.53756 16.9223 4.3907 16.6685C4.24383 16.4147 4.16654 16.1266 4.16663 15.8333V4.16667Z', stroke: 'currentColor', strokeWidth: '1.66667', strokeLinecap: 'round', strokeLinejoin: 'round' })));

const PlayIcon = ({ size = 20 }) => (React.createElement("svg", { width: size, height: size, viewBox: '0 0 20 20', fill: 'none', xmlns: 'http://www.w3.org/2000/svg' },
    React.createElement("circle", { cx: '10', cy: '10', r: '8.5', stroke: 'currentColor' }),
    React.createElement("circle", { cx: '10', cy: '10', r: '6', fill: 'currentColor' })));

const Play = ({ size = 48 }) => (React.createElement("svg", { width: size, height: size, viewBox: '0 0 48 48', fill: 'none', xmlns: 'http://www.w3.org/2000/svg' },
    React.createElement("path", { d: 'M24 48C20.7216 48 17.6314 47.3726 14.7294 46.1176C11.8431 44.8627 9.29412 43.1294 7.08235 40.9176C4.87059 38.7059 3.13725 36.1569 1.88235 33.2706C0.627451 30.3686 0 27.2784 0 24C0 20.7216 0.627451 17.6392 1.88235 14.7529C3.13725 11.851 4.86275 9.29412 7.05882 7.08235C9.27059 4.87059 11.8196 3.13725 14.7059 1.88235C17.6078 0.627451 20.698 0 23.9765 0C27.2549 0 30.3451 0.627451 33.2471 1.88235C36.149 3.13725 38.7059 4.87059 40.9176 7.08235C43.1294 9.29412 44.8627 11.851 46.1176 14.7529C47.3726 17.6392 48 20.7216 48 24C48 27.2784 47.3726 30.3686 46.1176 33.2706C44.8627 36.1569 43.1294 38.7059 40.9176 40.9176C38.7059 43.1294 36.149 44.8627 33.2471 46.1176C30.3608 47.3726 27.2784 48 24 48ZM19.5294 33.4824L33.3647 25.3412C33.6941 25.1373 33.9137 24.8706 34.0235 24.5412C34.1333 24.1961 34.1333 23.8588 34.0235 23.5294C33.9137 23.1843 33.6941 22.9176 33.3647 22.7294L19.5294 14.5412C19.1843 14.3373 18.8235 14.251 18.4471 14.2824C18.0706 14.3137 17.749 14.4392 17.4824 14.6588C17.2314 14.8784 17.1059 15.1843 17.1059 15.5765V32.4471C17.1059 32.8392 17.2314 33.1529 17.4824 33.3882C17.7333 33.6235 18.0392 33.7569 18.4 33.7882C18.7765 33.8196 19.1529 33.7176 19.5294 33.4824Z', fill: 'white' })));

const Skip = ({ opacity = 0.6, size = 32 }) => (React.createElement("svg", { width: size, height: size, viewBox: '0 0 32 32', fill: 'none', xmlns: 'http://www.w3.org/2000/svg' },
    React.createElement("path", { d: 'M16 32C13.8144 32 11.7542 31.5817 9.81961 30.7451C7.89542 29.9085 6.19608 28.7529 4.72157 27.2784C3.24706 25.8039 2.0915 24.1046 1.2549 22.1804C0.418301 20.2458 0 18.1856 0 16C0 13.8144 0.418301 11.7595 1.2549 9.83529C2.0915 7.90065 3.24183 6.19608 4.70588 4.72157C6.18039 3.24706 7.87974 2.0915 9.80392 1.2549C11.7386 0.418301 13.7987 0 15.9843 0C18.1699 0 20.2301 0.418301 22.1647 1.2549C24.0993 2.0915 25.8039 3.24706 27.2784 4.72157C28.7529 6.19608 29.9085 7.90065 30.7451 9.83529C31.5817 11.7595 32 13.8144 32 16C32 18.1856 31.5817 20.2458 30.7451 22.1804C29.9085 24.1046 28.7529 25.8039 27.2784 27.2784C25.8039 28.7529 24.0993 29.9085 22.1647 30.7451C20.2405 31.5817 18.1856 32 16 32ZM8.95686 21.1608C8.95686 21.8301 9.2915 22.1647 9.96078 22.1647H11.702C12.3712 22.1647 12.7059 21.8301 12.7059 21.1608V16.4863C12.8209 16.7477 13.0353 16.9673 13.349 17.1451L21.0667 21.6784C21.2131 21.7725 21.3542 21.8405 21.4902 21.8824C21.6366 21.9242 21.783 21.9451 21.9294 21.9451C22.2327 21.9451 22.4889 21.8405 22.698 21.6314C22.9072 21.4222 23.0118 21.1033 23.0118 20.6745V11.3725C23.0118 10.9542 22.9072 10.6405 22.698 10.4314C22.4889 10.2118 22.2327 10.102 21.9294 10.102C21.783 10.102 21.6366 10.1229 21.4902 10.1647C21.3542 10.2065 21.2131 10.2745 21.0667 10.3686L13.349 14.902C13.0353 15.0797 12.8209 15.2993 12.7059 15.5608V10.8392C12.7059 10.1699 12.3712 9.83529 11.702 9.83529H9.96078C9.2915 9.83529 8.95686 10.1699 8.95686 10.8392V21.1608Z', fill: 'white', fillOpacity: opacity })));

const Pause = ({ size = 48 }) => (React.createElement("svg", { width: size, height: size, viewBox: '0 0 48 48', fill: 'none', xmlns: 'http://www.w3.org/2000/svg' },
    React.createElement("path", { d: 'M24 48C20.7216 48 17.6314 47.3726 14.7294 46.1176C11.8431 44.8627 9.29412 43.1294 7.08235 40.9176C4.87059 38.7059 3.13725 36.1569 1.88235 33.2706C0.627451 30.3686 0 27.2784 0 24C0 20.7216 0.627451 17.6392 1.88235 14.7529C3.13725 11.851 4.86275 9.29412 7.05882 7.08235C9.27059 4.87059 11.8196 3.13725 14.7059 1.88235C17.6078 0.627451 20.698 0 23.9765 0C27.2549 0 30.3451 0.627451 33.2471 1.88235C36.149 3.13725 38.7059 4.87059 40.9176 7.08235C43.1294 9.29412 44.8627 11.851 46.1176 14.7529C47.3726 17.6392 48 20.7216 48 24C48 27.2784 47.3726 30.3686 46.1176 33.2706C44.8627 36.1569 43.1294 38.7059 40.9176 40.9176C38.7059 43.1294 36.149 44.8627 33.2471 46.1176C30.3608 47.3726 27.2784 48 24 48ZM17.1529 33.4353H19.5529C20.149 33.4353 20.5882 33.2863 20.8706 32.9882C21.1529 32.6902 21.2941 32.3137 21.2941 31.8588V16.0941C21.2941 15.6235 21.1529 15.2392 20.8706 14.9412C20.5882 14.6431 20.149 14.4941 19.5529 14.4941H17.1529C16.5569 14.4941 16.1176 14.6431 15.8353 14.9412C15.5529 15.2392 15.4118 15.6235 15.4118 16.0941V31.8588C15.4118 32.3137 15.5529 32.6902 15.8353 32.9882C16.1176 33.2863 16.5569 33.4353 17.1529 33.4353ZM28.5176 33.4353H30.8941C31.4902 33.4353 31.9294 33.2863 32.2118 32.9882C32.4941 32.6902 32.6353 32.3137 32.6353 31.8588V16.0941C32.6353 15.6235 32.4941 15.2392 32.2118 14.9412C31.9294 14.6431 31.4902 14.4941 30.8941 14.4941H28.5176C27.9216 14.4941 27.4745 14.6431 27.1765 14.9412C26.8941 15.2392 26.7529 15.6235 26.7529 16.0941V31.8588C26.7529 32.3137 26.8941 32.6902 27.1765 32.9882C27.4745 33.2863 27.9216 33.4353 28.5176 33.4353Z', fill: 'white' })));

const CircularLoader = ({ size = 40, color = '#000', }) => {
    const circleSize = size - 20;
    const strokeWidth = 3;
    const radius = (circleSize - strokeWidth) / 2;
    const circumference = 2 * Math.PI * radius;
    return (React.createElement("div", { className: 'flex items-center justify-center animate-spin bg-white rounded-full', style: { width: size, height: size } },
        React.createElement("svg", { width: circleSize, height: circleSize, viewBox: `0 0 ${circleSize} ${circleSize}`, style: { transform: 'rotate(-90deg)' } },
            React.createElement("circle", { cx: circleSize / 2, cy: circleSize / 2, r: radius, fill: 'none', stroke: color, strokeWidth: strokeWidth, strokeLinecap: 'round', strokeDasharray: circumference, strokeDashoffset: circumference * 0.75 }))));
};

const CANVAS_STYLE$2 = {
    marginBottom: -20,
    marginTop: -20,
    width: '100%',
    height: '130px',
};
const DemoPlaybar = ({ togglePlay, isThisPlayerActive, isLoading, handleSkipToStart, currentTime, duration, canvasWrapperRef, visualizerRef, infoSlot, }) => (React.createElement("div", { className: 'flex items-center gap-4 overflow-hidden w-full' },
    React.createElement("button", { onClick: togglePlay, className: 'p-0 focus:outline-none', "aria-label": isThisPlayerActive ? 'Pause' : 'Play' }, isThisPlayerActive ? (React.createElement(Pause, null)) : isLoading ? (React.createElement(CircularLoader, { size: 48 })) : (React.createElement(Play, null))),
    React.createElement("button", { onClick: handleSkipToStart, className: 'p-0 focus:outline-none', "aria-label": 'Skip to start' },
        React.createElement(Skip, { opacity: currentTime > 0 ? 1 : 0.6 })),
    React.createElement("div", { className: 'hidden sm:flex text-sm font-mono gap-2 text-zinc-400' },
        React.createElement("span", null, formatTime(currentTime)),
        React.createElement("span", null, " / "),
        React.createElement("span", null, formatTime(duration))),
    React.createElement("div", { ref: canvasWrapperRef, className: 'flex-1' },
        React.createElement("canvas", { ref: visualizerRef, height: 130, style: CANVAS_STYLE$2 })),
    infoSlot && infoSlot));

const Mute = ({ size = 24 }) => (React.createElement("svg", { width: size, height: size, viewBox: '0 0 24 24', fill: 'none', xmlns: 'http://www.w3.org/2000/svg' },
    React.createElement("path", { d: 'M22.7334 2.7334L2.7334 22.7334L1.2666 21.2666L21.2666 1.2666L22.7334 2.7334Z', fill: 'currentColor' }),
    React.createElement("path", { d: 'M16.6401 11.9741V22.3022L10.0688 17.5336L11.2876 15.8549L14.5659 18.2338V11.9741H16.6401Z', fill: 'currentColor' }),
    React.createElement("path", { d: 'M3.21289 14.4705V9.42654C3.21289 8.32652 4.07804 7.25662 5.32324 7.25662H8.81738L16.6396 1.59744V8.34549H14.5664V5.65701L9.49023 9.32986H5.33301C5.33064 9.33149 5.32637 9.33418 5.32129 9.33963C5.30056 9.36195 5.28711 9.39571 5.28711 9.42654V14.4705C5.2872 14.4978 5.29735 14.5193 5.30957 14.532L5.32031 14.5398H8.63086V16.614H5.32324C4.10813 16.614 3.21308 15.6021 3.21289 14.4705Z', fill: 'currentColor' })));
const Unmute = ({ size = 24 }) => (React.createElement("svg", { width: size, height: size, viewBox: '0 0 24 24', fill: 'none', xmlns: 'http://www.w3.org/2000/svg' },
    React.createElement("path", { d: 'M16.6401 11.974V22.3021L10.0688 17.5336L11.2876 15.8549L14.5659 18.2338V11.974H16.6401Z', fill: 'currentColor' }),
    React.createElement("path", { d: 'M3.21289 14.4705V9.42651C3.21289 8.32649 4.07804 7.25659 5.32324 7.25659H8.81738L16.6396 1.59741V8.34546H14.5664V5.65698L9.49023 9.32983H5.33301C5.33064 9.33146 5.32637 9.33415 5.32129 9.3396C5.30056 9.36192 5.28711 9.39568 5.28711 9.42651V14.4705C5.2872 14.4978 5.29735 14.5192 5.30957 14.532L5.32031 14.5398H8.63086V16.614H5.32324C4.10813 16.614 3.21308 15.6021 3.21289 14.4705Z', fill: 'currentColor' }),
    React.createElement("path", { d: 'M16.64 22.3L8.81583 16.6111H5.32113C4.10561 16.6111 3.20996 15.599 3.20996 14.4676V9.42567C3.20996 8.32598 4.07564 7.25638 5.32113 7.25638H8.81679L16.64 1.59998V22.3ZM5.28479 14.4676C5.28479 14.4951 5.29547 14.5169 5.30774 14.5297C5.31258 14.5347 5.31609 14.5368 5.3173 14.5374C5.31768 14.5374 5.31989 14.5383 5.32113 14.5383H9.48992L14.5661 18.228V5.6576L9.488 9.32915H5.32973C5.32728 9.33089 5.32302 9.33358 5.31826 9.3387C5.29753 9.36102 5.28479 9.39484 5.28479 9.42567V14.4676Z', fill: 'currentColor' })));

const LED_SIZE = 36;
const INNER_CIRCLE_SIZE = 16;
const RING_GAP = 2;
const RING_STROKE = 2;
const MonitorButton = forwardRef(({ isMonitoring, onClick, analyserNode, size = LED_SIZE, disabled = false }, ref) => {
    const internalRef = useRef(null);
    const buttonRef = ref || internalRef;
    const bufferRef = useRef(new Float32Array(2048));
    const center = size / 2;
    const innerRadius = INNER_CIRCLE_SIZE / 2;
    const ringMinRadius = innerRadius + RING_GAP + RING_STROKE / 2;
    const ringMaxRadius = size / 2 - RING_STROKE / 2 - 1;
    const [ringRadius, setRingRadius] = useState(ringMinRadius);
    useEffect(() => {
        if (!isMonitoring || !analyserNode) {
            setRingRadius(ringMinRadius);
            return;
        }
        let running = true;
        const buffer = bufferRef.current;
        const animate = () => {
            if (!running)
                return;
            const levels = calculateLevels(analyserNode, buffer);
            const glow = scaleForDisplay(levels.rms);
            const radius = ringMinRadius + glow * (ringMaxRadius - ringMinRadius);
            setRingRadius(radius);
            requestAnimationFrame(animate);
        };
        requestAnimationFrame(animate);
        return () => {
            running = false;
        };
    }, [isMonitoring, analyserNode, ringMinRadius, ringMaxRadius]);
    const innerColor = isMonitoring ? '#f00' : '#a1a1aa';
    const ringColor = isMonitoring ? '#f00' : '#a1a1aa';
    return (React.createElement("button", { type: 'button', onClick: onClick, "aria-label": isMonitoring ? 'Stop monitoring' : 'Start monitoring', "aria-pressed": isMonitoring, className: 'flex items-center justify-center gap-1', disabled: disabled, style: {
            cursor: disabled ? 'not-allowed' : 'pointer',
        } },
        React.createElement("span", { ref: buttonRef, className: `
            monitor-button-reset-focus rounded-full flex-shrink-0 transition-all duration-150 ease-in
            focus:outline-none focus:ring-0 focus:ring-offset-0
          `, style: {
                width: size,
                height: size,
                minWidth: size,
                minHeight: size,
                background: 'transparent',
                border: 'none',
                padding: 0,
                position: 'relative',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                WebkitTapHighlightColor: 'transparent',
            } },
            React.createElement("svg", { width: size, height: size, viewBox: `0 0 ${size} ${size}`, style: { position: 'absolute', inset: 0, overflow: 'visible' } },
                React.createElement("circle", { cx: center, cy: center, r: innerRadius, fill: innerColor }),
                React.createElement("circle", { cx: center, cy: center, r: ringRadius, fill: 'none', stroke: ringColor, strokeWidth: RING_STROKE }))),
        React.createElement("span", { className: 'flex items-center gap-4' },
            React.createElement("span", { className: `hidden md:block text-sm font-mono ${isMonitoring ? 'text-white' : 'text-zinc-400'}` }, "LIVE"),
            React.createElement("span", { className: 'w-[1px] h-[40px] bg-zinc-700' }),
            React.createElement("span", { className: disabled ? 'text-zinc-400' : 'text-white' }, isMonitoring ? React.createElement(Unmute, null) : React.createElement(Mute, null)))));
});
MonitorButton.displayName = 'MonitorButton';

const CANVAS_STYLE$1 = {
    marginBottom: -20,
    marginTop: -20,
    width: '100%',
    height: '130px',
};
const LivePlaybar = ({ togglePlay, isThisPlayerActive, sourceMode, isLiveConfigured, canvasWrapperRef, visualizerRef, infoSlot, onOpenSettings, }) => {
    const isMonitoring = isThisPlayerActive && sourceMode === 'live';
    const { getAudioNodes, audioState } = useT3kPlayerContext();
    const nodes = getAudioNodes();
    const isReady = audioState.initState === 'ready';
    return (React.createElement("div", { className: 'flex items-center gap-4 overflow-hidden w-full' },
        React.createElement(MonitorButton, { isMonitoring: isMonitoring, onClick: togglePlay, disabled: !isLiveConfigured, analyserNode: isMonitoring && isReady ? nodes.inputMeterNode : null, size: 48 }),
        React.createElement("div", { ref: canvasWrapperRef, className: 'flex-1' },
            React.createElement("canvas", { ref: visualizerRef, height: 130, style: CANVAS_STYLE$1 })),
        infoSlot && infoSlot));
};

const PlayerFC$1 = ({ models = DEFAULT_MODELS, irs = DEFAULT_IRS, inputs = DEFAULT_INPUTS, previewMode, slimSize, demoOnly, onPlayDemo, onPlayLive, onModelChange, onInputChange, onIrChange, id, infoSlot, }) => {
    const core = usePlayerCore({
        id,
        previewMode,
        slimSize,
        models,
        irs,
        inputs,
        onPlayDemo,
        onPlayLive,
        onModelChange,
        onInputChange,
        onIrChange,
    });
    return (React.createElement("div", { className: 'bg-zinc-900 border border-zinc-700 text-white p-4 lg:p-8 pt-0 lg:pt-2 rounded-xl w-full flex flex-col gap-6' },
        React.createElement("div", { className: 'flex flex-col' },
            React.createElement("div", { className: 'flex items-center min-h-[80px]' }, demoOnly || core.sourceMode === 'demo' ? (React.createElement(DemoPlaybar, { togglePlay: core.togglePlay, isThisPlayerActive: core.isThisPlayerActive, isLoading: core.isLoading, handleSkipToStart: core.handleSkipToStart, currentTime: core.currentTime, duration: core.duration, canvasWrapperRef: core.canvasWrapperRef, visualizerRef: core.visualizerRef, infoSlot: infoSlot })) : (React.createElement(LivePlaybar, { togglePlay: core.togglePlay, isThisPlayerActive: core.isThisPlayerActive, sourceMode: core.sourceMode, isLiveConfigured: core.isLiveConfigured, canvasWrapperRef: core.canvasWrapperRef, visualizerRef: core.visualizerRef, infoSlot: infoSlot, onOpenSettings: core.openSettingsDialog }))),
            React.createElement("div", { className: 'flex flex-col gap-6' },
                !demoOnly && (React.createElement(Tabs, { tabs: SOURCE_MODE_OPTIONS.map(o => (React.createElement("div", { className: 'flex gap-2 items-center' },
                        o.value === 'demo' ? (React.createElement(Demo, { size: 20 })) : (React.createElement(PlayIcon, { size: 20 })),
                        React.createElement("span", null, o.label === 'Live' ? 'Play' : o.label)))), activeTab: SOURCE_MODE_OPTIONS.findIndex(o => o.value === core.sourceMode), setActiveTab: index => core.handleSourceModeChange(SOURCE_MODE_OPTIONS[index].value) })),
                React.createElement(PlayerSettings, { previewMode: previewMode, bypassed: core.bypassed, bypassedStyles: core.bypassedStyles, onBypassToggle: core.handleBypassToggle, sourceMode: core.sourceMode, onOpenSettings: core.openSettingsDialog, modelOptions: core.modelOptions, irOptions: core.irOptions, audioOptions: core.audioOptions, selectedModelUrl: core.selectedModel?.url ?? '', selectedIrUrl: core.selectedIr?.url ?? '', selectedInputUrl: core.selectedInput?.url ?? '', onModelChange: core.handleModelChange, onIrChange: core.handleIrChange, onInputChange: core.handleInputChange, isLiveConfigured: core.isLiveConfigured, currentDeviceId: core.currentDeviceId, liveDeviceOptions: core.liveDeviceOptions, inputModeType: core.inputModeType, audioInputError: core.audioInputDevices.error, isThisPlayerActive: core.isThisPlayerActive }))),
        React.createElement("a", { href: 'https://www.tone3000.com', target: '_blank', className: 'flex flex-row gap-2 items-center self-end' },
            React.createElement("p", { className: 'text-zinc-400 text-xs' }, "Powered by"),
            React.createElement(LogoSm, { width: 42, height: 14 }))));
};
const Player = memo((props) => {
    return React.createElement(PlayerFC$1, { ...props });
}, (prevProps, nextProps) => {
    return (JSON.stringify(prevProps.models) === JSON.stringify(nextProps.models) &&
        JSON.stringify(prevProps.irs) === JSON.stringify(nextProps.irs) &&
        JSON.stringify(prevProps.inputs) === JSON.stringify(nextProps.inputs));
});
Player.displayName = 'Player';

const CANVAS_STYLE = {
    marginBottom: -20,
    marginTop: -20,
    width: '100%',
    height: '130px',
};
const PlayerSkeleton = ({ previewMode, infoSlot, }) => {
    const visualizerRef = useRef(null);
    const canvasWrapperRef = useRef(null);
    const renderModelSelect = () => (React.createElement(Select, { options: [], label: 'Model', onChange: () => { }, value: '', disabled: true }));
    const renderIrSelect = () => (React.createElement(Select, { options: [], label: 'IR', onChange: () => { }, value: '', disabled: true }));
    return (React.createElement("div", { className: 'bg-zinc-900 border border-zinc-700 text-white p-4 lg:p-8 pt-0 lg:pt-2 rounded-xl w-full flex flex-col gap-6 opacity-50 cursor-not-allowed' },
        React.createElement("div", { className: 'flex flex-col' },
            React.createElement("div", { className: 'flex items-center min-h-[80px]' },
                React.createElement("div", { className: 'flex items-center gap-4 overflow-hidden w-full' },
                    React.createElement("button", { className: 'p-0 focus:outline-none', "aria-label": 'Play', disabled: true },
                        React.createElement(Play, null)),
                    React.createElement("button", { className: 'p-0 focus:outline-none', "aria-label": 'Skip to start', disabled: true },
                        React.createElement(Skip, { opacity: 0.6 })),
                    React.createElement("div", { className: 'hidden sm:flex text-sm font-mono gap-2 text-zinc-400' },
                        React.createElement("span", null, "00:00"),
                        React.createElement("span", null, " / "),
                        React.createElement("span", null, "00:00")),
                    React.createElement("div", { ref: canvasWrapperRef, className: 'flex-1' },
                        React.createElement("canvas", { ref: visualizerRef, height: 130, style: CANVAS_STYLE })),
                    infoSlot && infoSlot)),
            React.createElement("div", { className: 'flex flex-col gap-6' },
                React.createElement(Tabs, { tabs: SOURCE_MODE_OPTIONS.map(o => (React.createElement("div", { className: 'flex gap-2 items-center' },
                        o.value === 'demo' ? (React.createElement(Demo, { size: 20 })) : (React.createElement(PlayIcon, { size: 20 })),
                        React.createElement("span", null, o.label)))), activeTab: 0, setActiveTab: () => { } }),
                React.createElement("div", { className: 'flex flex-col gap-4' },
                    React.createElement("div", { className: 'flex flex-row items-center gap-4 flex-wrap' },
                        React.createElement("div", { className: 'flex-1 min-w-[0px]' },
                            React.createElement("div", { className: 'flex w-full flex-1 min-w-[0px]' }, previewMode === PREVIEW_MODE.MODEL
                                ? renderModelSelect()
                                : renderIrSelect())),
                        React.createElement("div", { className: 'flex items-center pt-[24px] flex-shrink-0' },
                            React.createElement(ToggleSimple, { label: '', onChange: () => { }, isChecked: true, ariaLabel: 'Bypass', disabled: true }))),
                    React.createElement("div", { className: 'w-full sm:flex-1 min-w-0' }, previewMode === PREVIEW_MODE.MODEL
                        ? renderIrSelect()
                        : renderModelSelect()),
                    React.createElement("div", { className: 'w-full sm:flex-1 min-w-0' },
                        React.createElement(Select, { options: [], label: 'Input', onChange: () => { }, value: '', disabled: true }))))),
        React.createElement("a", { href: 'https://www.tone3000.com', target: '_blank', className: 'flex flex-row gap-2 items-center self-end' },
            React.createElement("p", { className: 'text-zinc-400 text-xs' }, "Powered by"),
            React.createElement(LogoSm, { width: 42, height: 14 }))));
};

const DisabledPlaybar = ({ infoSlot, }) => (React.createElement("div", { className: 'flex items-center gap-4 overflow-hidden w-full' },
    React.createElement("div", { className: 'p-0 cursor-not-allowed opacity-50', "aria-label": 'Play (disabled)', "aria-disabled": 'true' },
        React.createElement(Play, null)),
    React.createElement("div", { className: 'p-0 cursor-not-allowed opacity-50', "aria-label": 'Skip to start (disabled)', "aria-disabled": 'true' },
        React.createElement(Skip, { opacity: 0.6 })),
    React.createElement("div", { className: 'hidden sm:flex text-sm font-mono gap-2 text-zinc-400 opacity-50' },
        React.createElement("span", null, "0:00"),
        React.createElement("span", null, " / "),
        React.createElement("span", null, "0:00")),
    React.createElement("div", { className: 'flex-1 h-px bg-zinc-700' }),
    infoSlot));

const T3kDisabledPlayer = ({ infoSlot, }) => (React.createElement("div", { className: 'neural-amp-modeler' },
    React.createElement("div", { className: 'bg-zinc-900 border border-zinc-700 text-white px-4 lg:px-8 py-4 rounded-xl w-full flex items-center min-h-[80px]' },
        React.createElement(DisabledPlaybar, { infoSlot: infoSlot }))));

const T3kPlayer = ({ isLoading, previewMode = PREVIEW_MODE.MODEL, disabled, models, infoSlot, ...props }) => {
    if (isLoading) {
        return (React.createElement("div", { className: 'neural-amp-modeler' },
            React.createElement(PlayerSkeleton, { previewMode: previewMode })));
    }
    if (disabled) {
        return React.createElement(T3kDisabledPlayer, { infoSlot: infoSlot });
    }
    return (React.createElement("div", { className: 'neural-amp-modeler' },
        React.createElement(Player, { ...props, models: models, previewMode: previewMode, infoSlot: infoSlot })));
};

const maxWidthStyles = {
    sm: 'max-w-sm',
    md: 'max-w-md',
    lg: 'max-w-lg',
    xl: 'max-w-xl',
};
const Dialog = ({ isOpen, onClose, onOpen, header, footer, children, closeOnBackdropClick = true, maxWidth = 'md', isLoading = false, }) => {
    // Call onOpen exactly once per open transition.
    // The ref persists across StrictMode's simulated unmount/remount,
    // preventing the double-fire while still calling on each real mount.
    const didOpenRef = useRef(false);
    useEffect(() => {
        if (isOpen && !didOpenRef.current) {
            didOpenRef.current = true;
            onOpen?.();
        }
        if (!isOpen) {
            didOpenRef.current = false;
        }
    }, [isOpen, onOpen]);
    // Handle escape key
    useEffect(() => {
        const handleEscape = (e) => {
            if (e.key === 'Escape' && isOpen) {
                onClose();
            }
        };
        document.addEventListener('keydown', handleEscape);
        return () => document.removeEventListener('keydown', handleEscape);
    }, [isOpen, onClose]);
    if (!isOpen)
        return null;
    return (React.createElement("div", { className: 'fixed inset-0 z-50 flex items-center justify-center', role: 'dialog', "aria-modal": 'true' },
        React.createElement("div", { className: 'absolute inset-0 bg-black/70', onClick: closeOnBackdropClick ? onClose : undefined, "aria-hidden": 'true' }),
        isLoading && (React.createElement(LoaderCircle, { size: 42, className: 'text-zinc-700 flex-shrink-0 animate-spin' })),
        !isLoading && (React.createElement("div", { className: `relative bg-zinc-900 text-white border border-zinc-700 rounded-xl w-full ${maxWidthStyles[maxWidth]} mx-4 shadow-xl max-h-[90vh] overflow-y-auto` },
            header && React.createElement("div", null, header),
            React.createElement("div", { className: 'px-4 pb-6' }, children),
            footer && React.createElement("div", null, footer)))));
};

function BlockedInstructions({ browser }) {
    switch (browser) {
        case 'safari':
            return (React.createElement(React.Fragment, null,
                "To fix this on Safari:",
                React.createElement("ol", { className: 'list-decimal list-inside mt-2 space-y-1' },
                    React.createElement("li", null, "In the top menu bar, click Safari \u2192 Settings for This Website\u2026"),
                    React.createElement("li", null, "Find Microphone"),
                    React.createElement("li", null, "Set to Allow"),
                    React.createElement("li", null, "Refresh the page")),
                React.createElement("br", null),
                "Alternative: Safari \u2192 Settings \u2192 Websites \u2192 Microphone \u2192 set this site to Allow"));
        case 'firefox':
            return (React.createElement(React.Fragment, null,
                "To fix this on Firefox:",
                React.createElement("ol", { className: 'list-decimal list-inside mt-2 space-y-1' },
                    React.createElement("li", null, "Click the icon to the left of the website address"),
                    React.createElement("li", null, "Open Connection secure / Permissions"),
                    React.createElement("li", null, "Set Use the Microphone to Allow"),
                    React.createElement("li", null, "Refresh the page")),
                React.createElement("br", null),
                "If blocked permanently: Type about:preferences#privacy \u2192 Permissions \u2192 Microphone \u2192 Settings\u2026 \u2192 remove this site from blocked list"));
        case 'edge':
            return (React.createElement(React.Fragment, null,
                "To fix this on Edge:",
                React.createElement("ol", { className: 'list-decimal list-inside mt-2 space-y-1' },
                    React.createElement("li", null, "Click the icon to the left of the website address"),
                    React.createElement("li", null, "Open Permissions for this site"),
                    React.createElement("li", null, "Set Microphone to Allow"),
                    React.createElement("li", null, "Refresh the page")),
                React.createElement("br", null),
                "If blocked permanently: Type about:preferences#privacy \u2192 Permissions \u2192 Microphone \u2192 Settings\u2026 \u2192 remove this site from blocked list"));
        case 'chrome':
            return (React.createElement(React.Fragment, null,
                "To fix this on Chrome:",
                React.createElement("ol", { className: 'list-decimal list-inside mt-2 space-y-1' },
                    React.createElement("li", null, "Click the icon to the left of the website address (sliders / site controls icon)"),
                    React.createElement("li", null, "Find Microphone"),
                    React.createElement("li", null, "Set to Allow"),
                    React.createElement("li", null, "Refresh the page")),
                React.createElement("br", null),
                "If you don't see Microphone: Chrome menu \u2192 Settings \u2192 Privacy & security \u2192 Site settings \u2192 Microphone \u2192 remove this site from \"Not allowed\""));
        default:
            return (React.createElement(React.Fragment, null, "To fix this, open your browser settings and allow microphone access for this site. Look for the site controls icon (lock or info) next to the address bar, then set Microphone to Allow and refresh the page."));
    }
}
const PermissionContent = ({ status, errorMessage, onRequestPermission, }) => {
    const isPending = status === 'pending';
    const isBlocked = status === 'blocked';
    const hasError = status === 'denied' || status === 'error';
    const isGranted = status === 'granted';
    return (React.createElement("div", { className: 'flex flex-col gap-4' },
        React.createElement("div", { className: 'flex flex-col gap-2' },
            React.createElement("div", { className: 'text-base text-zinc-400' },
                "Allow microphone access to connect your interface:",
                React.createElement("ol", { className: 'list-decimal list-inside mt-2 space-y-1' },
                    React.createElement("li", null, "\"Allow Microphone\""),
                    React.createElement("li", null, "\"Allow while visiting the site\"")))),
        isBlocked && (React.createElement("div", { className: 'flex flex-col gap-3' },
            React.createElement(Alert, { variant: 'error' },
                React.createElement(React.Fragment, null,
                    errorMessage ?? 'Microphone access was previously denied.',
                    React.createElement("br", null),
                    React.createElement("br", null),
                    React.createElement(BlockedInstructions, { browser: getBrowserName() }))))),
        hasError && (React.createElement(Alert, { variant: 'error' }, (errorMessage ?? status === 'denied')
            ? 'Microphone access was previously denied. Try again and select "Allow."'
            : 'Unable to access microphone. Try again and select "Allow."')),
        React.createElement("div", { className: 'flex h-2' }),
        React.createElement(Button, { variant: 'primary', size: 'lg', fullWidth: true, leftIcon: isPending || isGranted ? (React.createElement(LoaderCircle, { size: 18, className: 'animate-spin' })) : (React.createElement(Mic, { size: 18 })), onClick: onRequestPermission, disabled: isPending || isBlocked || isGranted }, isPending
            ? 'Asking for permission...'
            : isGranted
                ? 'Configuring settings...'
                : 'Allow Microphone'),
        React.createElement("div", { className: 'flex gap-2 text-zinc-400 justify-center' },
            React.createElement(Lock, { size: 18 }),
            React.createElement("span", { className: 'text-sm' }, "Audio is processed locally and never leaves your device."))));
};

/**
 * A radio button matching the design: selected = white inner dot with dark grey
 * ring; unselected = thin light grey outline, transparent interior.
 */
const Radio = ({ checked, onChange, value, name, label, className = '', disabled, ...props }) => {
    const content = (React.createElement(React.Fragment, null,
        React.createElement("span", { className: `
          flex items-center justify-center w-5 h-5 rounded-full flex-shrink-0
          transition-colors duration-150
          ${checked
                ? 'border border-white bg-transparent'
                : 'border border-zinc-500 bg-transparent'}
          ${disabled ? 'opacity-50' : ''}
        `, "aria-hidden": true }, checked && React.createElement("span", { className: 'w-3 h-3 rounded-full bg-white' })),
        React.createElement("input", { type: 'radio', name: name, value: value, checked: checked, onChange: onChange, disabled: disabled, className: 'sr-only', ...props }),
        label != null && React.createElement("span", { className: 'text-sm text-zinc-300' }, label)));
    if (label != null) {
        return (React.createElement("label", { className: `flex items-center gap-3 cursor-pointer ${disabled ? 'cursor-not-allowed' : ''} ${className}` }, content));
    }
    return React.createElement("div", { className: `flex items-center ${className}` }, content);
};
Radio.displayName = 'Radio';

const InterfaceIcon = ({ size = 20 }) => (React.createElement("svg", { width: size, height: size, viewBox: '0 0 20 20', fill: 'none', xmlns: 'http://www.w3.org/2000/svg' },
    React.createElement("path", { d: 'M8.3335 13.3333H8.34183', stroke: 'currentColor', strokeWidth: '1.66667', strokeLinecap: 'round', strokeLinejoin: 'round' }),
    React.createElement("path", { d: 'M1.84341 9.64749C1.72726 9.87926 1.66677 10.1349 1.66675 10.3942V15C1.66675 15.442 1.84234 15.8659 2.1549 16.1785C2.46746 16.4911 2.89139 16.6667 3.33341 16.6667H16.6667C17.1088 16.6667 17.5327 16.4911 17.8453 16.1785C18.1578 15.8659 18.3334 15.442 18.3334 15V10.3942C18.3334 10.1349 18.2729 9.87926 18.1567 9.64749L15.4584 4.25833C15.3204 3.98065 15.1077 3.74697 14.8442 3.58356C14.5807 3.42015 14.2768 3.33349 13.9667 3.33333H6.03341C5.72334 3.33349 5.41947 3.42015 5.15595 3.58356C4.89244 3.74697 4.67973 3.98065 4.54175 4.25833L1.84341 9.64749Z', stroke: 'currentColor', strokeWidth: '1.66667', strokeLinecap: 'round', strokeLinejoin: 'round' }),
    React.createElement("path", { d: 'M18.2886 10.0108H1.71191', stroke: 'currentColor', strokeWidth: '1.66667', strokeLinecap: 'round', strokeLinejoin: 'round' }),
    React.createElement("path", { d: 'M5 13.3333H5.00833', stroke: 'currentColor', strokeWidth: '1.66667', strokeLinecap: 'round', strokeLinejoin: 'round' })));

const DeviceContent = ({ devices, selectedDeviceId, selectedChannel, channelCount, isMonitoring, onDeviceChange, onChannelChange, onMonitoringChange, isConnecting, error, connectionError, onRefresh, channel0Meter, channel1Meter, outputDevices, selectedOutputDeviceId, onOutputDeviceChange, }) => {
    const hasDevices = devices.length > 0;
    const isStereo = channelCount >= 2;
    // Show error state if failed to load devices
    if (error && !hasDevices) {
        return (React.createElement("div", { className: 'flex flex-col gap-4' },
            React.createElement(Alert, { variant: 'error' },
                error,
                " Failed to load audio input devices. Please check your connections and try again."),
            React.createElement(Button, { variant: 'secondary', onClick: onRefresh }, "Refresh Devices")));
    }
    // Show no devices state
    if (!hasDevices) {
        return (React.createElement("div", { className: 'flex flex-col gap-4' },
            React.createElement(Alert, { variant: 'error' }, "No audio input devices found. Please connect an audio interface and try again."),
            React.createElement(Button, { variant: 'secondary', onClick: onRefresh }, "Refresh Devices")));
    }
    return (React.createElement("div", { className: 'flex flex-col gap-5' },
        React.createElement("div", { className: 'flex flex-col gap-4 w-full border border-zinc-700 rounded-lg p-4' },
            React.createElement("div", { className: 'flex gap-4 items-center' },
                React.createElement(InterfaceIcon, { size: 20 }),
                React.createElement("h3", { className: 'text-base font-mono text-white font-semibold' }, "AUDIO INTERFACE")),
            isConnecting ? (React.createElement("div", { className: 'flex flex-col gap-2' },
                React.createElement("div", { className: 'flex flex-col' },
                    React.createElement("span", { className: 'text-base text-white font-semibold' }, "Select Interface"),
                    React.createElement("span", { className: 'text-sm text-zinc-400' }, "Plug your instrument into Input 1 or 2 of your interface.")),
                React.createElement("div", { className: 'flex items-center justify-between w-full overflow-hidden px-4 py-3 text-md border border-zinc-700 rounded-md bg-transparent opacity-50 cursor-wait' },
                    React.createElement("span", { className: 'text-ellipsis text-nowrap overflow-hidden min-w-0' }, "Initializing audio..."),
                    React.createElement(LoaderCircle, { size: 20, className: 'text-zinc-400 flex-shrink-0 animate-spin' })))) : (React.createElement(Select, { options: devices.map(d => ({
                    label: d.label,
                    value: d.deviceId,
                })), value: selectedDeviceId, label: 'Plug your instrument into Input 1 or 2 of your interface.', heading: 'Select Interface', onChange: async (value) => {
                    await onDeviceChange(String(value));
                    onMonitoringChange(true); // Enable monitoring by default when selecting a device
                } })),
            !isStereo && isSafari && (React.createElement(Alert, { variant: 'warning' }, "Safari supports Input 1 only. Chrome and Firefox support Input 1 or 2.")),
            error && hasDevices && (React.createElement(Alert, { variant: 'warning' }, "A device was disconnected. Please select another device.")),
            isStereo && (React.createElement("div", { className: 'flex flex-col gap-2' },
                React.createElement("div", { className: 'flex flex-col gap-1' },
                    React.createElement("span", { className: 'text-base text-white font-semibold' }, "Input Channel"),
                    React.createElement("span", { className: 'text-sm text-zinc-400' }, "Play your instrument and select the channel with signal.")),
                React.createElement("div", { className: 'flex flex-col gap-2 mt-1' },
                    React.createElement("label", { className: 'flex items-center gap-3 cursor-pointer' },
                        React.createElement(Radio, { name: 'input-channel', value: 'first', checked: selectedChannel === 'first', onChange: () => onChannelChange('first') }),
                        React.createElement("span", { className: 'text-sm text-zinc-300 w-4' }, "1"),
                        React.createElement(InputMeter, { analyser: channel0Meter, label: 'Channel 1 level', inactive: selectedChannel !== 'first', className: 'flex-1 min-w-0' })),
                    React.createElement("label", { className: 'flex items-center gap-3 cursor-pointer' },
                        React.createElement(Radio, { name: 'input-channel', value: 'second', checked: selectedChannel === 'second', onChange: () => onChannelChange('second') }),
                        React.createElement("span", { className: 'text-sm text-zinc-300 w-4' }, "2"),
                        React.createElement(InputMeter, { analyser: channel1Meter, label: 'Channel 2 level', inactive: selectedChannel !== 'second', className: 'flex-1 min-w-0' })))))),
        connectionError && (React.createElement(Alert, { variant: 'error' }, connectionError ||
            'Failed to connect to audio input device. Please check your connections and try again.')),
        isConnecting ? (React.createElement("div", { className: 'flex flex-col gap-2' },
            React.createElement("div", { className: 'flex flex-col' },
                React.createElement("span", { className: 'text-base text-white font-semibold' }, "Output Device"),
                React.createElement("span", { className: 'text-sm text-zinc-400' }, "Select where you want to hear the sound.")),
            React.createElement("div", { className: 'flex items-center justify-between w-full overflow-hidden px-4 py-3 text-md border border-zinc-700 rounded-md bg-transparent opacity-50 cursor-wait' },
                React.createElement("span", { className: 'text-ellipsis text-nowrap overflow-hidden min-w-0' }, "Initializing audio..."),
                React.createElement(LoaderCircle, { size: 20, className: 'text-zinc-400 flex-shrink-0 animate-spin' })))) : (outputDevices.length > 0 && (React.createElement("div", null,
            React.createElement(Select, { options: [
                    ...(needsMediaStreamWorkaround
                        ? [{ label: 'System Default', value: '' }]
                        : []),
                    ...outputDevices.map(d => ({
                        label: d.label,
                        value: d.deviceId,
                    })),
                ], value: selectedOutputDeviceId ?? '', heading: 'Output Device', label: 'Select where you want to hear the sound.', onChange: value => onOutputDeviceChange(value === '' ? null : String(value)) })))),
        React.createElement("div", { className: 'flex flex-col' },
            React.createElement("h3", { className: 'text-base text-white font-semibold' }, "Input Monitoring"),
            React.createElement("p", { className: 'text-sm text-zinc-400 pb-2' }, "Hear your instrument while playing."),
            React.createElement("div", { className: 'flex items-center justify-between w-full px-4 py-3 border border-zinc-700 rounded-lg bg-transparent' },
                React.createElement("span", { className: 'text-base text-white' }, "Hear Yourself"),
                React.createElement(ToggleSimple, { label: '', isChecked: isMonitoring, onChange: onMonitoringChange, ariaLabel: 'Hear yourself', disabled: isConnecting || !selectedDeviceId })))));
};

const SettingsDialog = ({ isOpen, onClose, sourceMode, selectedModel, selectedIr, playerId, onMonitoringChange, }) => {
    const { microphonePermission, audioInputDevices, audioOutputDevices, audioState, init, requestMicrophonePermission, refreshAudioInputDevices, refreshAudioOutputDevices, startLiveInput, selectLiveInputChannel, setOutputDevice, getAudioNodes, } = useT3kPlayerContext();
    // Local state: only UI flow
    const [step, setStep] = useState('permission');
    const [isInitializing, setIsInitializing] = useState(true);
    const [connectionError, setConnectionError] = useState(null);
    // Derived from context (single source of truth)
    const liveInputConfig = audioState.liveInputConfig;
    const currentDeviceId = liveInputConfig?.deviceId ?? null;
    const currentChannel = liveInputConfig?.selectedChannel ?? 'first';
    const channelCount = liveInputConfig?.channelCount ?? 1;
    const isDemoMode = sourceMode === 'demo';
    // Derive effective step: if permission isn't granted while on device-select, show permission.
    // Applies in both modes; demo mode needs permission for output device labels on Firefox/Safari.
    const effectiveStep = !isInitializing &&
        microphonePermission.status !== 'granted' &&
        step === 'device-select'
        ? 'permission'
        : step;
    // Helper to ensure audio system is initialized before starting live input
    const initAndStartLiveInput = useCallback(async (deviceId) => {
        setConnectionError(null);
        try {
            if (audioState.initState !== 'ready') {
                await init();
            }
            await startLiveInput(deviceId);
        }
        catch (error) {
            const message = error instanceof DOMException
                ? error.message
                : 'Failed to connect to audio device. Please try again.';
            setConnectionError(message);
        }
    }, [audioState.initState, init, startLiveInput]);
    // Imperative open handler, called once by Dialog's onOpen (ref-guarded against StrictMode)
    const handleOpen = useCallback(() => {
        setIsInitializing(true);
        if (sourceMode === 'demo') {
            if (microphonePermission.status === 'granted') {
                // Permission granted: enumerate input devices first to trigger getUserMedia
                // self-healing (Firefox/Safari require per-session getUserMedia to unlock device labels),
                // then enumerate output devices so they have labels.
                refreshAudioInputDevices()
                    .then(() => refreshAudioOutputDevices())
                    .then(() => {
                    setIsInitializing(false);
                    setStep('device-select');
                });
            }
            else {
                // No permission: enumerate what we can. effectiveStep will redirect to
                // the permission step so the user can unlock device labels.
                refreshAudioOutputDevices().then(() => {
                    setIsInitializing(false);
                    setStep('device-select');
                });
            }
        }
        else if (microphonePermission.status === 'granted') {
            // Live mode with permission: enumerate both input and output devices
            Promise.all([
                refreshAudioInputDevices(),
                refreshAudioOutputDevices(),
            ]).then(([{ inputDevices, preferredDeviceId }]) => {
                setIsInitializing(false);
                setStep('device-select');
                // Auto-connect on first open in live mode (no device configured yet)
                if (!audioState.liveInputConfig && inputDevices.length > 0) {
                    const deviceToConnect = preferredDeviceId ?? inputDevices[0].deviceId;
                    initAndStartLiveInput(deviceToConnect);
                }
            });
        }
        else {
            // Play-live mode without permission: show permission step
            setIsInitializing(false);
            setStep('permission');
        }
    }, [
        sourceMode,
        microphonePermission.status,
        refreshAudioInputDevices,
        refreshAudioOutputDevices,
        audioState.liveInputConfig,
        initAndStartLiveInput,
    ]);
    // Event handlers
    const handleDeviceChange = useCallback((deviceId) => initAndStartLiveInput(deviceId), [initAndStartLiveInput]);
    const handleRequestPermission = useCallback(async () => {
        try {
            const preferredDeviceId = await requestMicrophonePermission();
            // requestMicrophonePermission only handles the permission prompt.
            // Enumerate devices separately: refreshAudioInputDevices will get labels
            // because getUserMedia just ran (self-healing workaround handles Firefox/Safari).
            await Promise.all([
                refreshAudioInputDevices(),
                refreshAudioOutputDevices(),
            ]);
            setStep('device-select');
            if (preferredDeviceId) {
                initAndStartLiveInput(preferredDeviceId);
            }
        }
        catch {
            // Error handling is done in context
        }
    }, [
        requestMicrophonePermission,
        refreshAudioInputDevices,
        refreshAudioOutputDevices,
        initAndStartLiveInput,
    ]);
    const handleRefreshDevices = useCallback(async () => {
        await Promise.all([
            refreshAudioInputDevices(),
            refreshAudioOutputDevices(),
        ]);
    }, [refreshAudioInputDevices, refreshAudioOutputDevices]);
    const isPending = microphonePermission.status === 'pending';
    const dialogTitle = effectiveStep !== 'permission' ? 'Settings' : 'Microphone Access';
    const dialogIcon = effectiveStep !== 'permission' ? (React.createElement(Settings, { size: 24 })) : (React.createElement(Info, { size: 24 }));
    const isSaveEnabled = isDemoMode || currentDeviceId !== null;
    // Header with close button
    const header = (React.createElement("div", { className: 'flex items-center justify-between p-4' },
        React.createElement("div", { className: 'flex items-center gap-4' },
            dialogIcon,
            React.createElement("h2", { className: 'text-lg font-semibold' }, dialogTitle)),
        React.createElement("button", { onClick: onClose, className: 'p-1', "aria-label": 'Close', disabled: isPending },
            React.createElement(X, { size: 24, className: 'text-zinc-400' }))));
    // Footer only for device/channel selection step
    const footer = !isInitializing && effectiveStep === 'device-select' ? (React.createElement("div", { className: 'flex justify-center p-4' },
        React.createElement(Button, { variant: 'primary', size: 'lg', fullWidth: true, onClick: onClose, disabled: !isSaveEnabled }, "Done"))) : null;
    return (React.createElement(Dialog, { isOpen: isOpen, onClose: onClose, onOpen: handleOpen, header: header, footer: footer, closeOnBackdropClick: !isPending && !isInitializing, isLoading: isInitializing || audioInputDevices.isLoading }, effectiveStep === 'permission' ? (React.createElement(PermissionContent, { status: microphonePermission.status, errorMessage: microphonePermission.error, onRequestPermission: handleRequestPermission })) : (React.createElement(DeviceContent, { devices: audioInputDevices.devices, selectedDeviceId: currentDeviceId ?? '', selectedChannel: currentChannel, channelCount: channelCount, isMonitoring: audioState.isPlaying && audioState.activePlayerId === playerId, onDeviceChange: handleDeviceChange, onChannelChange: selectLiveInputChannel, onMonitoringChange: onMonitoringChange, isConnecting: audioState.inputMode.type === 'connecting', error: audioInputDevices.error, connectionError: connectionError, onRefresh: handleRefreshDevices, channel0Meter: getAudioNodes().channel0LiveMeter, channel1Meter: getAudioNodes().channel1LiveMeter, outputDevices: audioOutputDevices.devices, selectedOutputDeviceId: audioOutputDevices.selectedDeviceId, onOutputDeviceChange: setOutputDevice }))));
};

/** Renders the global settings dialog when open. Included once by T3kPlayerProvider. */
function SettingsDialogRenderer() {
    const { settingsDialog, closeSettingsDialog } = useT3kPlayerContext();
    if (!settingsDialog.isOpen ||
        !settingsDialog.selectedModel ||
        !settingsDialog.selectedIr) {
        return null;
    }
    return (React.createElement("div", { className: 'neural-amp-modeler', style: { background: 'none' } },
        React.createElement(SettingsDialog, { isOpen: true, onClose: closeSettingsDialog, sourceMode: settingsDialog.sourceMode, selectedModel: settingsDialog.selectedModel, selectedIr: settingsDialog.selectedIr, playerId: settingsDialog.playerId, onMonitoringChange: settingsDialog.onMonitoringChange })));
}
/**
 * Recommended provider for the T3k player. Wraps T3kPlayerContextProvider and
 * includes SettingsDialogRenderer once (shared by all players on the page).
 */
function T3kPlayerProvider({ children, engineAssets, }) {
    return (React.createElement(T3kPlayerContextProvider, { engineAssets: engineAssets },
        children,
        React.createElement(SettingsDialogRenderer, null)));
}

const SlimPlayerFC = ({ onPlayDemo, id, getData, size = 40, disabled = false, slimSize, }) => {
    const { audioState, getAudioNodes, init, loadAudio, syncEngineSettings, setPlaying, stopLiveInput, cleanup, } = useT3kPlayerContext();
    const [isLoading, setIsLoading] = useState(false);
    // Cleanup on unmount (same as usePlayerCore)
    useEffect(() => {
        return () => cleanup();
    }, [cleanup]);
    // Cache getData result so we don't re-fetch on every toggle
    const dataRef = useRef(null);
    const getDataCached = useCallback(async () => {
        if (!dataRef.current) {
            dataRef.current = await getData();
        }
        return dataRef.current;
    }, [getData]);
    const isThisPlayerActive = audioState.activePlayerId === id;
    // Listen for audio ended: reset playback when this player is active
    useEffect(() => {
        if (!isThisPlayerActive)
            return;
        const audioElement = getAudioNodes().audioElement;
        if (!audioElement)
            return;
        const handleEnded = () => setPlaying(false);
        audioElement.addEventListener('ended', handleEnded);
        return () => {
            audioElement.removeEventListener('ended', handleEnded);
        };
    }, [getAudioNodes, isThisPlayerActive, setPlaying]);
    const togglePlay = useCallback(async () => {
        if (!id)
            return;
        // Stop live input if engine is in live mode
        const { mediaStream } = getAudioNodes();
        if (mediaStream?.active) {
            stopLiveInput();
        }
        setIsLoading(true);
        try {
            const { model, ir, input } = await getDataCached();
            if (!model || !ir || !input) {
                console.error('SlimPlayer: getData returned incomplete data');
                return;
            }
            if (audioState.initState !== 'ready') {
                await init({ audioUrl: input.url, slimSize });
            }
            if (isThisPlayerActive) {
                setPlaying(false);
            }
            else {
                // Load audio if needed
                if (!audioState.audioUrl || audioState.audioUrl !== input.url) {
                    await loadAudio(input.url);
                }
                // Sync engine settings (model, IR, bypass)
                await syncEngineSettings({
                    modelUrl: model.url,
                    ir: { url: ir.url, mix: ir.mix, gain: ir.gain },
                    bypassed: false,
                    slimSize,
                });
                setPlaying(true, id);
                onPlayDemo?.({ model, ir, input });
            }
        }
        catch (error) {
            console.error('Error in togglePlay:', error);
            setPlaying(false);
        }
        finally {
            setIsLoading(false);
        }
    }, [
        id,
        getAudioNodes,
        stopLiveInput,
        getDataCached,
        audioState.initState,
        audioState.audioUrl,
        isThisPlayerActive,
        init,
        loadAudio,
        syncEngineSettings,
        setPlaying,
        onPlayDemo,
        slimSize,
    ]);
    if (disabled) {
        return (React.createElement("button", { disabled: true, className: 'p-0 focus:outline-none cursor-not-allowed opacity-50', "aria-label": 'Play (disabled)' },
            React.createElement(Play, { size: size })));
    }
    return (React.createElement("button", { onClick: togglePlay, className: 'p-0 focus:outline-none', "aria-label": isThisPlayerActive ? 'Pause' : 'Play' }, isThisPlayerActive ? (React.createElement(Pause, { size: size })) : isLoading ? (React.createElement(CircularLoader, { size: size })) : (React.createElement(Play, { size: size }))));
};
const T3kSlimPlayer = memo((props) => {
    return React.createElement(SlimPlayerFC, { ...props });
}, (prevProps, nextProps) => {
    return prevProps.id === nextProps.id;
});
T3kSlimPlayer.displayName = 'T3kSlimPlayer';

const PlayerFC = ({ getData = async () => ({
    models: DEFAULT_MODELS,
    irs: DEFAULT_IRS,
    inputs: DEFAULT_INPUTS,
}), previewMode, slimSize, demoOnly, onPlayDemo, onPlayLive, onModelChange, onInputChange, onIrChange, id, disabled = false, infoSlot, }) => {
    const [expanded, setExpanded] = useState(false);
    const [models, setModels] = useState(null);
    const [inputs, setInputs] = useState(null);
    const [irs, setIrs] = useState(null);
    const resolveData = useCallback(async () => {
        const data = await getData();
        setModels(data.models);
        setIrs(data.irs);
        setInputs(data.inputs);
        return {
            model: getDefault(data.models),
            ir: getDefault(data.irs),
            input: getDefault(data.inputs),
        };
    }, [getData]);
    const core = usePlayerCore({
        id,
        previewMode,
        disabled,
        slimSize,
        models,
        irs,
        inputs,
        resolveData,
        onPlayDemo,
        onPlayLive,
        onModelChange,
        onInputChange,
        onIrChange,
    });
    const handleToggleExpand = useCallback(async () => {
        if (disabled)
            return;
        if (!models) {
            try {
                await resolveData();
            }
            catch {
                return;
            }
        }
        setExpanded(prev => !prev);
    }, [disabled, models, resolveData]);
    return (React.createElement("div", { className: 'neural-amp-modeler' },
        React.createElement("div", { className: `bg-zinc-900 border border-t-0 border-zinc-700 text-white px-4 sm:px-6 pt-0 pb-0 rounded-xl w-full flex flex-col gap-0 rounded-t-none ${expanded ? 'pb-4 lg:pb-8' : 'pb-0'}` },
            React.createElement("div", { className: 'flex items-center gap-4 overflow-hidden min-h-[80px]' },
                disabled ? (React.createElement(DisabledPlaybar, { infoSlot: infoSlot })) : demoOnly || core.sourceMode === 'demo' ? (React.createElement(DemoPlaybar, { togglePlay: core.togglePlay, isThisPlayerActive: core.isThisPlayerActive, isLoading: core.isLoading, handleSkipToStart: core.handleSkipToStart, currentTime: core.currentTime, duration: core.duration, canvasWrapperRef: core.canvasWrapperRef, visualizerRef: core.visualizerRef, infoSlot: infoSlot })) : (React.createElement(LivePlaybar, { togglePlay: core.togglePlay, isThisPlayerActive: core.isThisPlayerActive, sourceMode: core.sourceMode, isLiveConfigured: core.isLiveConfigured, canvasWrapperRef: core.canvasWrapperRef, visualizerRef: core.visualizerRef, infoSlot: infoSlot, onOpenSettings: core.openSettingsDialog })),
                !disabled && (React.createElement("button", { onClick: handleToggleExpand, className: `p-0 focus:outline-none flex-shrink-0`, "aria-label": 'Toggle accordion' }, expanded ? React.createElement(ChevronUp, { size: 24 }) : React.createElement(ChevronDown, { size: 24 })))),
            expanded && !disabled && (React.createElement(React.Fragment, null,
                React.createElement("div", { className: 'flex flex-col gap-6' },
                    !demoOnly && (React.createElement(Tabs, { tabs: SOURCE_MODE_OPTIONS.map(o => (React.createElement("div", { className: 'flex gap-2 items-center' },
                            o.value === 'demo' ? (React.createElement(Demo, { size: 20 })) : (React.createElement(PlayIcon, { size: 20 })),
                            React.createElement("span", null, o.label === 'Live' ? 'Play' : o.label)))), activeTab: SOURCE_MODE_OPTIONS.findIndex(o => o.value === core.sourceMode), setActiveTab: index => core.handleSourceModeChange(SOURCE_MODE_OPTIONS[index].value) })),
                    React.createElement(PlayerSettings, { previewMode: previewMode, disabled: disabled, bypassed: core.bypassed, bypassedStyles: core.bypassedStyles, onBypassToggle: core.handleBypassToggle, sourceMode: core.sourceMode, onOpenSettings: core.openSettingsDialog, modelOptions: core.modelOptions, irOptions: core.irOptions, audioOptions: core.audioOptions, selectedModelUrl: core.selectedModel?.url ?? '', selectedIrUrl: core.selectedIr?.url ?? '', selectedInputUrl: core.selectedInput?.url ?? '', onModelChange: core.handleModelChange, onIrChange: core.handleIrChange, onInputChange: core.handleInputChange, isLiveConfigured: core.isLiveConfigured, currentDeviceId: core.currentDeviceId, liveDeviceOptions: core.liveDeviceOptions, inputModeType: core.inputModeType, audioInputError: core.audioInputDevices.error, isThisPlayerActive: core.isThisPlayerActive })),
                React.createElement("a", { href: 'https://www.tone3000.com', target: '_blank', className: 'hidden flex flex-row gap-2 items-center self-end' },
                    React.createElement("p", { className: 'text-zinc-400 text-xs' }, "Powered by"),
                    React.createElement(LogoSm, { width: 42, height: 14 })))))));
};
const T3kAcordianPlayer = memo((props) => {
    return React.createElement(PlayerFC, { ...props });
}, (prevProps, nextProps) => {
    return (JSON.stringify(prevProps.id) === JSON.stringify(nextProps.id) &&
        JSON.stringify(prevProps.previewMode) ===
            JSON.stringify(nextProps.previewMode) &&
        prevProps.getData === nextProps.getData &&
        prevProps.disabled === nextProps.disabled);
});
T3kAcordianPlayer.displayName = 'T3kAcordianPlayer';

export { DEFAULT_INPUTS, DEFAULT_IRS, DEFAULT_MODELS, NamEngine, PREVIEW_MODE, T3kAcordianPlayer, T3kDisabledPlayer, T3kPlayer, T3kPlayerContextProvider, T3kPlayerProvider, T3kSlimPlayer, useT3kPlayerContext };
//# sourceMappingURL=index.esm.js.map
