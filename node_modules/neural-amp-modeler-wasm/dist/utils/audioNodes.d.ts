import { AudioNodes } from '../types';
/** Clean up all live input nodes (mediaStream, source, gain, splitter, merger, meters) */
export declare function cleanupLiveInputNodes(nodes: AudioNodes): void;
/** Clean up output device workaround routing (Firefox/Safari) and reconnect to default destination */
export declare function cleanupOutputWorkaroundRouting(nodes: AudioNodes): void;
/**
 * Tear down live input audio nodes and restore the demo signal path.
 * Used by stopLiveInput and handleLiveInputUnavailable.
 */
export declare function teardownLiveInput(nodes: AudioNodes, options: {
    muteOutput: boolean;
}): void;
/** Apply output device routing with browser-specific handling */
export declare function applyOutputDeviceRouting(nodes: AudioNodes, deviceId: string | null): Promise<void>;
